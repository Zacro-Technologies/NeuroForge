# Retrieval assets — bounded implementation, scoped execution passed

Current execution: all eight asset methods passed in the 89/89 Mac scoped run on `ed785a62`; see `Retrieval_Assets_Reviewed_UI_89_Passing_Run.log`. Full suite and installed asset rendering remain separate. Original scratch record follows.

Frozen patch: `retrieval-assets.patch`
SHA256: `dfa9dd28a32703734aca6686a615ad3787d679b895bdb795bb56a6bcf5988706`

This scratch candidate changes 14 existing files: 12 production Swift files, the existing ContentImprovementRegressionTests.swift, and five compact EN/JA catalog entries. It adds no source file or project membership. `base-hashes.json` records the exact current-tree input files. Main sources and tests were not edited by this agent. Parse and current-tree apply checks passed; type checking, XCTest and installed visual/accessibility execution remain for root.

## Product behavior and recipe compatibility

New ordinary retrieval launches carry optional `retrievalAssetPolicyVersion=1`, alongside retrieval authority1. Only an actual, explicitly supported cloze/equation/figure gets schema7, generator10 and the existing `.r10` identity segment. Nil asset policy preserves current authority1/gen9/schema6. Nil both policies preserves gen4/schema2. Existing accepted requests/snapshots are not regenerated or relabeled.

Explicit cloze, equation and figure launches draw only from their compatible finite target pools. Empty selected field/form or a competing explicit format request fails unavailable before reserving a launch. General field retains its existing all-field meaning. Ordinary mixed retrieval keeps the subject target, choosing genuine short recall before publication when an actual asset does not exist. Source-derived facts receive no invented diagram or equation. Old asset-free gen9 equations remain as immutable supported legacy recipes; this package does not claim it repairs their historical content automatically.

`NFRetrievalAssetContract` binds the exact canonical target, parameters, authored locale, essential representations, typed interaction and generator/metadata versions. `compatibleRenderings(targetID:)` explicitly declares support. Future/missing/tampered/wrong-lab/wrong-evidence/protected contracts fail closed. Recipe pin is preserved through fixed factory construction, local session save/load/recovery, ordinary count alternative and same-family retry. Existing ordinary and generated snapshots retain the complete contract and typed response without new model columns. Generated parity is the retained authoritative-exercise adapter; this package does not fabricate source provenance or make the AI producer synthesize unsupported assets.

## Actual source assets

`Capacity.json` enumerates the exact 38 distinct target IDs and 98 supported target/form pairs: cloze38, equation30, figure30. Eight authored concept omissions cover one relationship per field. Thirty existing computational targets supply ten arithmetic-mean, ten coordinate-slope and ten rectangle-area contracts, each with cloze/equation/figure renderings. These are 38 knowledge targets, not 98 independent quota slots. No editorial band, reviewed quota, calibration or admission is granted.

Equations contain actual numeric operands and a missing result; their spoken EN/JA equivalents include every operand and unit. Figures are real native observation bars, a line joining two retained points, or a rectangle drawn from retained vertices. The visible figure and its equivalent accessible table share the same immutable typed points. Numerical response schemas use exact arithmetic; context/AX text excludes answer keys. The shared renderer is used by ordinary practice, generated retained questions and history. History checks protected restrictions before rendering and shows explicit compatible-version recovery for unsupported retained assets.

## Eight exact test methods

Existing `ContentImprovementRegressionTests.swift`, new `@MainActor RetrievalAssetContractTests`:

1. `testAllFiniteAssetsMaterializeThroughActualGeneratorAndNumericScorer` — all98 actual generated pairs, independent parameter oracle, adjacent incorrect values and equivalent decimals, exact target/recipe/roles, no band grant.
2. `testCapacityCountsDistinctTargetsAndFormsWithoutInventingBandsOrCrossProducts` —38/30/30, target union38, explicit missing forms, same target across forms has one semantic fingerprint and separate item IDs.
3. `testClozeAndEquationHaveRealOmissionsAndLanguageEquivalentAnswers` —all8 concept keys in EN/JA, unknown prose clarification, genuine omissions, exact equation AX operands/units without result leakage.
4. `testFiguresRetainActualGeometryOrObservationsAndTheSameAccessibleData` —actual point/vertex/bar data equals the equivalent table, bounded readable scales.
5. `testFutureMissingAlteredAndProtectedAssetContractsFailClosedWithoutMutation` —unsupported recipe/metadata/protection/lab/evidence/tampered source refusal, original bytes unchanged, protected history no representation.
6. `testExplicitUnsupportedFieldAndUnknownPolicyDoNotPublishGenericAssetPlaceholders` —producer rejection plus actual unavailable launch with no accepted reservation; legacy recipe preserved and mixed new templates never use generic asset placeholders.
7. `testActualFocusedFigureLaunchAndColdResponseKeepExactGraphHistory` —real catalog launch, recipe pin, writer release/cold draft, exact retained figure/response, committed score/history, one genuinely different Next.
8. `testGeneratedAdapterRestoresTheSameRetainedFigureAndTypedResponse` —authoritative question binding, exact generated draft/cold response, commit and retained history. Store fixtures retain the ModelContainer until the test ends.

## Boundaries and remaining engineering

This is partial CON-010/SCO-013/CON-024 implementation, not full retrieval or all58 completion. Another962 existing targets lack these new assets. Equation/figure eligibility is currently confined to general/mathematics. Additional real structures, symbolic/conditional or multiple-slot reconstruction, broader quantitative/domain diagrams, and target-specific new retention asset recipes remain engineering work. Existing retention queues are not silently upgraded to this ordinary asset policy. The finite capacities are intentionally below release quotas, and cross-form overlap must be accounted for in any future edition assignment. Human bilingual review/adjudication, reviewed demand bands, calibration and signed/admitted release assets remain separate release gates.

The patch deliberately preserves current main changes for generated lifecycle, root's ModelContainer lifetime fixes, physical duplicate/conflict exclusions, controls and previous math/data/trace/science/graph contracts. No build or execution claim is made for this new package.
