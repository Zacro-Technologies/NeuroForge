# Runtime implementation ledger — 4 September 2026

Current integration update (5 September): ordinary/source/protected and modern/legacy generated explicit Submit now persist prepared state, exact history snapshot and feedback through the shared background storage actor. The MainActor retains only the live-model receipt insertion and UI publication after verified acknowledgement. Actual iOS passes33 worker/submission regressions onf9541cc4; complete Mac passes1343 onf4c27814. Typed cold archive validation and fingerprint generation have separate non-inlined frames, fixing reproduced cooperative-worker stack overflows. Other Next/Skip/End/initial/administrative writes and bulk dashboard projections remain engineering work. Installed advanced coordinate/reflection journeys are running separately; no complete device-matrix or migration certification is implied.

This is an implementation and remaining-work ledger, not full specification or distribution certification. Test results are recorded separately in the run manifest. No signed integrations or manual device checks are inferred from unit tests.

| Requirement | Status | Evidence and remaining acceptance |
|---|---|---|
| CORE-001 | partial | Ordinary and generated adapters now share typed lifecycle, frozen intent, scoreability gate, prepared/save/feedback ordering and persist-before-publish Next commands. Source review embeds Universal with an exact private excerpt snapshot. Protected selection and storage adapters remain specialized. Parity/failure/source fixtures passed in the784-test integrated run; exhaustive transition and adapter extraction acceptance remains. |
| CORE-002 | partial | Pure authorities, shared lifecycle command coordinator, selection/evidence/workload policies, local repository, typed presentation and navigation are separated. Full module/view extraction and public command interface audit remain. |
| CORE-003 | partial | Fallback numeric keys and numeric/cube snapshots now use their explicit request locale. Retention policy3 requires an explicit calendar through due dates, evidence priority and canonical plan construction; a DST regression covers23-hour versus24-hour local days. Explicit locale/calendar additions passed in the784-test run. A complete audit of remaining legacy reducers remains. |
| CORE-004 | partial | UI state remains MainActor; competing archive file locks now return a recoverable busy error without blocking or sleeping, with real competing-FD regression. Progress error-pattern grouping now consumes immutable effective DTOs in a cancellable detached worker with generation-checked publication; its new tests await the next run. Backup bounded reading, compatibility normalization, protected redaction and typed decoding now run in a detached disposable worker; immutable prepared input prevents preview/commit file substitution. New tests await integration. Whole local-session archive encoding/decoding and other large legacy projections still require background/paginated architecture and measured performance; no broad concurrency closure is claimed. |
| CORE-005 | partial | Local envelope/checkpoint schema1, scorer8, archive18 and named editorial policy are separate. New ordinary reservation pins name UniversalPresentationV1 independently of exercise schema; explicitly supported legacy1/2 pins retain original receipts, future pins become recovery. Generated draft presentation/selection/scorer pins reject unknown versions. Daily scheduling now usesv5 while preserving supportedfrozenv4 plans; unsupportedfuture plans remainreadable and cannot execute. Newtestsawaitintegration. Complete interface registry audit remains. |
| RUN-001 | partial | Ordinary fixed focused launches now commit full reservation, canonical request, initial run/slot/snapshot and launch receipt in one atomic local file transaction with revision compare-and-swap. Exact prepared replay and deletion tombstones tested; reviewed adaptive delivery and other controllers remain. |
| RUN-002 | implemented; validation recorded separately | Explicit resumeSession(id) restores exact run before any launch reservation. New practice uses a distinct run. |
| RUN-003 | partial | Canonical launchOnly configuration, exact current snapshot, durable slot/decision identities, full fixed launch plan and actual migrated epoch positions are retained. Reviewed adaptive selector delivery and complete explicit policy-version registry remain. |
| RUN-004 | partial | Multiple suspended ordinary/generated runs, Continue, explicit End and one writer. Practice All/Focused/Review filters and pagination in batches of10 include every suspended run; independent installed continuation validation remains. |
| RUN-005 | partial | App-scoped writer arbitration checkpoints predecessor on takeover and rejects stale commands. Persistent ownership generations and real multiwindow acceptance remain. |
| RUN-006 | implemented; validation recorded separately | Owner-device UUID gates continuation; foreign imports remain recovery. No cross-device mid-question lease is claimed. |
| RUN-007 | partial | Codable phases and prepared intents exist. Paused/save-error remain orthogonal state and require exhaustive illegal-transition modeling. |
| RUN-008 | partial | Inline scoring, separate clarification guidance without an attempt or save-error alert, stable immutable prepared answer and idempotent attempt insertion. Clarification/draft survives cold reconstruction in unit fixture; installed clarification is correct; broad alert query mistook the identified modal session container for an error alert and is corrected for rerun. |
| RUN-009 | partial | Shared deterministic invitation and semantic response identity; nil defaults, inline protected requirement, timed suppression, material-edit invalidation, equivalent numeric formatting and dictionary-order retention. Generated confidence uses the same policy and survives exact cold draft restore. Full durable focus-interval telemetry remains. |
| RUN-010 | partial | Feedback precedes optional reflection, no diagnosis preselection and not-sure permitted. Later edits append local revisions linked to the immutable first reflection; history exposes current and first interpretations. Six reflection tests passed in the742-test run, including revisions, retry, backup, deletion and unsupported metadata. The missing-original-anchor guard prevents reviving an old diagnosis or reattaching imported supplements and passed the784-test integrated run. |
| RUN-011 | partial | Neutral protected UI, local receipt and portable redaction; original keys withheld. Reviewed dimensional completion and retired-form disclosure protocol remain gated. |
| RUN-012 | partial | Ordinary/generated self-checks share acknowledged reference reveal; source review now pins the original private excerpt/citation in Universal. Editable older response-only source drafts are recovery-only. Ratings have zero objective authority; private source references stay out of newly written durable answer-key columns. Integrated source/cold-resume/privacy unit tests passed; the installed source cold-replay journey passed in the20/20 run. |
| RUN-013 | partial | Progressive authored hints and worked-solution reveal persist with stage, active offset and generator version. Repeated solution reveal is idempotent; failed solution checkpoint does not disclose. Revealed outcomes retain draft separately from skips and carry no objective weight. Generated coaching disclosure also checkpoints before display; collapsing it retains recorded assistance. Full hint-flight fault matrix remains. |
| RUN-014 | partial | Stable pending skip journal, no evidence denominator, recovery and duplicate protection. Explicit reason and skip-support recommendation remain. |
| RUN-015 | partial | Next checkpoints one slot; fresh repair starts a distinct fixed launch with origin and semantic exclusions supplied before atomic acceptance, zero immediate independent evidence weight. Reviewed adaptive item selection remains. |
| RUN-016 | partial | Explicit ended-early status/summary preserves attempts; fixed-count completion is distinct from sufficient scored evidence. Protected duration stop continues the same form in a later sitting. |
| DATA-001 | partial | Ordinary exercise snapshot includes authentic content/schema/labels and digest. Protected evaluator regenerated only with digest verification. Full content-addressed asset manifest remains. |
| DATA-002 | partial | Scorer version plus snapshot digest pinned; unavailable on unsupported uncommitted evaluator. Signed invalidation predicates and compatible old scorer dispatch remain. |
| DATA-003 | partial | Exact typed draft, phase, stable IDs, assistance events, confidence, time, reflection, snapshot, selection receipts and exclusions. Complete clock-chunk sequence remains. |
| DATA-004 | partial | Injected monotonic answer time begins on acknowledged presentation; sitting time separate; resume excludes unknown gaps. Tests plus installed lifecycle verification required. |
| DATA-005 | partial | Wall time cannot add answer-solving time; nonfinite clock intervals excluded. DST/timezone/lock-screen physical matrix remains. |
| DATA-006 | partial | 500ms draft debounce and explicit pause/close/background/takeover flush; failed saves retain UI. Sudden unacknowledged keystrokes are not promised durable. |
| DATA-007 | partial | Notes/drawing bytes independent; unsupported Mac drawing retained; layer clear/undo and pinned prompt. Pencil device verification remains. |
| DATA-008 | partial | Digest/version/owner validation and pending answer/skip replay before writable continuation. Signed content invalidation/restricted evaluator history migration remain. |
| DATA-009 | partial | Saved question retains recorded locale; mixed chrome/content language has an explicit localized explanation. Full new-item locale-transition matrix remains. |
| DATA-010 | partial | Prepared checkpoint precedes immutable attempt and acknowledged feedback; same-ID typed retries compare parsed response identity while retaining original raw bytes and reject changed key/result/session. Full fixed launch is now atomic with captured-slot and presentation ledgers. Durable dual-payload conflict journal is implemented and its five tests passed in the784-test run; the complete platform crash matrix remains. |
| DATA-011 | partial | Before/after attempt crash fixtures and exact saved feedback cover recovery. Every phase/platform crash matrix remains. |
| DATA-012 | partial | Same-ID conflicting responses/results are refused before snapshot attachment. A local append-only journal retains both raw records/contracts with digests, stable retries and evidence exclusion; protected journal details are withheld from portable exports. Private archive merge conflicts reject before replacement. Five integrated conflict/retry/failure/deletion tests passed in the784-test run; peer-conflict resolution UI remains. |
| DATA-013 | partial | Existing reward constants preserved and new confidence/reflection add no attempts. All disposition-aware review/XP replay consumers require full audit. |
| DATA-014 | partial | Retry/Keep working/export answer; prepared response stays locked. Queued close and prolonged-failure recovery wording need installed fault testing. |
| DATA-015 | partial | Finite family exhaustion is explicit and never substitutes. Legacy full cursor bootstrap and later fixed reservations use the same atomic file transaction; exact position/exposure ledger and revision CAS are implemented. Ineligible positions are retained as holes and can be selected when eligible again;23 fixed-launch and12 rotation tests passed in the742-test run; reviewed adaptive selector integration remains. |
| DATA-016 | partial | Atomic local archive combines runs/checkpoints/snapshots, sets/private metadata, immutable reflection revisions, dispositions, selection decisions, exposure/outcomes and fixed launch/migration receipts. Durable review tasks and signed invalidation records remain. |
| DATA-017 | partial | New snapshots, sets, private drafts and collections are local-only, outside SwiftData/CloudKit schema; source draft checkpoint avoids legacy cloud upsert. |
| DATA-018 | partial | Original SwiftData record fields/partition policy unchanged. Existing private text scope is not widened by new local blobs; complete inventory/account checks remain. |
| DATA-019 | partial | Saved sets persist without TTL; accepted draft pins the exact set. Explicit removal retains unfinished pins. Shared snapshot garbage collection respects retained run references and revokes only deleted writers; full content-addressed asset deduplication remains. |
| DATA-020 | partial | 64MiB archive and8MiB private payload bounds, verified snapshots and typed private canonicalization. Individual malformed history snapshots now become explicit unavailable markers while valid history remains readable; exact bounded originals are preserved in private local recovery storage before rewriting, and raw recovery files are withheld from portable export. Session/ledger/receipt decoding stays strict. Malformed current typed response envelopes show recovery instead of serialization; legacy plain answers remain readable. Media decode and remaining whole-archive recovery audit remain. |
| DATA-021 | implemented; validation recorded separately | All eight typed history response formats preserve values/original labels; missing context explained without invented IDs. Unit coverage present. |
| DATA-022 | partial | Authentic snapshot prompt/representations/answer/explanation plus report/fresh practice. Protected display checks precede instructional detail; all legacy missing-asset states require device review. |
| DATA-023 | partial | Append-only historical corrections preserve original answers/results and use explicit current recommendation membership when earlier uncertainty resolves. Corrected historical accuracy is distinct from exclusions for speed, proficiency, calibration and independence. Signed rule publication remains. |
| DATA-024 | partial | Historical policy covers the nine specified defect classes plus workflow/leakage. Exact reconstructable cases receive deterministic correction; missing options/tables/aliases have explicit unresolved or precautionary exclusion outcomes. Old originals are never fabricated. All real affected-store fixture coverage remains. |
| DATA-025 | partial | Dismissible correction notice links affected originals; all history projections show effective correction/exclusion while retaining labeled original results and disputed keys. Protected display checks precede any explanation. Complete compatible-chart regime and affected real-store replay remain. |
| RUN-017 | implemented; validation recorded separately | Today/Practice/Progress/Sources/Settings with typed inner routes and Review/History in Progress. |
| RUN-018 | partial | Independent stable destination paths, safe reselection and back ownership. Nested route journeys passed in the20/20 installed run. Stable-OS and manual/physical hang-closure acceptance remains. |
| RUN-019 | partial | Cold Today with Continue, validated typed deep links, deleted-record fallback and path pruning. Disposable paths are not restored blindly. |
| RUN-020 | partial | Existing deferred dirty intent routing preserved; typed destination paths added. Complete unique intent replay/queue close across every external route remains. |
| RUN-021 | implemented; validation recorded separately | Primary changing navigation .id removed; per-session .id is retained solely to construct the selected session runtime. |
| RUN-022 | partial | Save/close across phases and durable retry/error controls. Native window close and pending-close journal recovery across all controllers still need certification. |
| MIG-001 | partial | No persistent @Model fields changed. Shipped source captured separately. Genuine frozen V1/V2 metadata plus every prerelease binary store fixture remain a distribution blocker. |
| MIG-002 | partial | Additive optional local envelopes preserve old raw history. New full-history asynchronous backfill/projection publication remains. |
| MIG-003 | partial | Legacy incomplete draft is recovery-only; it is never attached to a regenerated question. Full historical generator-backed migration classification remains. |
| MIG-004 | partial | Existing signed edition is not relabeled reviewed. Admission reports deficits; new balanced signed edition, semantic mappings and consumed-ledger migration remain blocked on content. |
| MIG-005 | partial | Archive18 added;14–17 and future rejection tested; optional new local data defaults. No backward writer promise. |
| MIG-006 | partial | Archive includes local snapshots/sets/drafts/metadata/dispositions; protected receipts redact reusable keys/outcomes. Invalid opaque private payload IDs are reported; all counts/hash references need independent validator expansion. |
| MIG-007 | partial | Version/size validation before mutation, frozen reviewed bytes and census, explicit merge policy, accepted cold journal over two SQLite stores and exact side files, fresh verification and durable completion acknowledgement.41 compiler/domain/cold/runtime/deletion tests and the installed74-second repeated-cold-restore journey pass; real shipped-store/physical crash/account matrix remains. |
| MIG-008 | partial | Existing recovery package retained and no old schema removed. A compatible recovery binary/package exercise is not yet executed. |
| MIG-009 | partial | Local additions never enter durable CloudKit model. Signed development/production-equivalent schema/device checks are not verified. |
| MIG-010 | partial | Account-scoped local files, full local deletion, linked source/generation cleanup and writer revocation are implemented. Live explicit deletion also authenticates and invalidates completed restore payload authority before cleaning known artifacts and permitting the primary delete;13 additional cleanup/guard tests pass. Existing staged whole-device purge now removes all inactive-account LocalLearning files and verifies absence again before opening stores; retry fixture includes late/corrupt artifacts. Full physical interrupted multi-store proof remains. |
