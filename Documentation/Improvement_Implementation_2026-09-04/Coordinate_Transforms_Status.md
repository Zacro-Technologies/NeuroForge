# Coordinate transformations12/9 — frozen candidate, execution pending

`coordinate-transforms.patch` is based on fresh current sources. `Verification.txt` records its final SHA256. The scratch package preserves the integrated spatial11 viewpoint/namespaced guard and the small public generator validation wrapper. No working-tree edit, build, project change, or new project member was performed.

## Implemented scope

`coordinateTransformPolicyVersion: Int?` opts into generator12/schema9 for ordinary spatial variants0 and5. It coexists with spatialStructurePolicyVersion1 for labeled-face/occupied-cell variants1/3. Other families and nil accepted legacy requests retain their prior exact recipes. New item IDs use the existing `.r12` namespace. The missing-sidecar generator check is scoped to `nf.exercise.fallback`; authored-generator12 and older authored11 remain compatible under their own identities.

The typed integer affine model retains the original point, ordered operations, centers, mirror lines and translation vectors. Actual recipes cover positive/negative90°,180°,270° rotations, origin/non-origin centers, every quadrant, axes, origin and symmetric/fixed points; translated vertical/horizontal mirrors; both diagonal mirrors; translation/rotation order; repeated reflection; and rotation/reflection order. All givens stay in a fixed x-right/y-up frame. This is active point transformation; the changed reflection prompt does not misapply a translated mirror to a free vector.

The answer is a structured x/y state with explicit exact-number domains and grid units. Each coordinate receives half the credit; both are necessary for full correctness. Equivalent numeric notation is accepted. There are no hidden-ID coordinate alternatives to collapse at a fixed point. Existing legacy duplicate-choice regression remains intact.

Native/static Canvas rendering uses only exact original givens before commitment. The axes, original point, centers and mirror lines share the saved model. Post-commit Step/Reset changes a separate worked copy; it cannot change the response or original geometry. Discrete replay announces the operation and resulting coordinate; a labeled reference triangle and its exact accessible coordinate table illustrate orientation afterward. No animation or camera transformation supplies the answer. A nonvisual coordinate-task scope statement avoids claiming equivalent visual assessment.

Both ordinary and generated snapshots preserve the same original geometry and typed response. History uses the existing exact-snapshot spatial route. Unknown versions, malformed points/operations, protected requests, missing ordinary recipe pins and altered authority refuse before grading or writable recovery. Complete exercise binding verifies representations, source/feedback text, role, provenance, family, fingerprint and key. New catalog preview request defaults and exact typed activity mapping are included; other accepted sessions are untouched.

## Finite accounting and meaningful verification

Independent `census.py` was executed using homogeneous matrix products, a separate reflection normal formula, distances and involutions. It enumerates24 ordered-operation recipes ×14 source coverage cases =336 parameterized contracts. D4 symmetry, common-anchor translation, uniform scale and one-operation fixed-point aliases collapse these to120 semantic query classes across10 operation substructures. Neither336 nor120 is a claim of independent object topologies, reviewed bands, or quota feasibility. All points/replay states fit the declared −12…12 diagram bounds.

Eight `CoordinateTransformContractTests` methods are authored in existing ContentImprovementRegressionTests.swift:

1. Entire finite producer bank against independent affine oracle; exact x/y and numeric equivalence; declared half credit; correct catalog scope.
2. Full-quadrant distance, center/line fixed-point, involution, orientation determinant, and order noncommutativity checks.
3. Sign/scale/fixed-point semantic deduplication and exact120-class census agreement.
4. EN/JA original givens, units, independent/replay/reset phase restrictions and immutable bytes.
5. Future/malformed/protected/missing-pin refusal; nil legacy4/2 and coexisting spatial11 contracts preserved.
6. Authentic ordinary launch, exact cold x/y notation, commit/history and same-family fresh Next.
7. Authentic generated-authority adapter, cold typed state, deterministic commit and exact history.
8. Generator namespace isolation preserving current authored12 and supported older authored11 authority.

Every store fixture explicitly isolates cache/documents/history/temporary artifacts, uses an in-memory model/local repository/rotation, disables widget publication, retains its ModelContainer and removes its temporary directory. App compilation, XCTest execution and installed rendering/accessibility verification belong to root and remain pending.

## Explicit remaining work

Unknown-transform inference from enough correspondences is not implemented. Orientation/fixed-point classification is explained but not separately scored. Broader authoring, demand-tier selection, exact new retention recipes and full requested-block feasibility beyond bounded old probes remain engineering work. QA-F13 solid/plane intersections and QA-F15 varied partial-fold/constraint queries remain separate concrete work.

Current NativeOrdinaryCommitProtocolV1 admits numeric/singleChoice only. This new two-field response therefore remains ordinary practice until a narrowly authenticated structured-response protocol extension has actual admitted commit/Next tests; no reviewed admission gate was silently widened. Independent editorial/visual/language review, band/calibration/admission and signatures remain separate release gates. Zero reviewed/signed slots are created here.


## Root execution update

Root execution: the first 187-test integrated scope exposes a contradictory exact-only rubric, worker-stack use during unrelated fingerprinting, and a missing preparation step in one cold test. All geometry/oracle assertions except the declared half-credit rule pass. Installed coordinate input and full-answer grading pass up to a worked-copy assertion, which remains under investigation. No final passing coordinate scope is claimed.
