# Coordinate reasoning 15: inverse, inference, orientation and fixed sets

Scratch-only bounded QA-F11 / QA-F16 addition. No source files, project membership, reviewed manifest entries, or SwiftData columns are added.

## Reachable product behavior

Coordinate Rotation and Vector Reflection retain their existing primary Start practice path and exact forward12 recipe. Each gains an explicit **Inverse and transformation reasoning (untimed)** secondary button (`train-start-coordinate-reasoning`). It calls the real catalog launch with optional `coordinateReasoningPolicyVersion: 1`; wrong activities and unknown policy values fail closed before creating a run. Existing nil policy and frozen forward/legacy snapshots are not regenerated or reinterpreted.

The new optional `NFCoordinateReasoningContract` carries schema12/generator15, namespaced `.r15` identities, exact original points and ordered operations. Its recognized finite task registry and complete exercise binding own prompt, source representation, typed response, rubric, feedback, family, identity, and provenance:

- **Inverse:** given final Q plus ordered operations, recover both original coordinates. Two exact numeric fields, half credit each. Negatives, axes, origin, symmetry, non-origin centers and inverse composition order are exercised.
- **Unknown affine map:** three actual noncollinear labeled source/target correspondences determine six coefficients. The stated class is affine maps of the plane; no arbitrary nonlinear-function inference is claimed. The generator's specific operation decomposition is absent from independent givens. Singular/duplicated/underdetermined triples have no key. Numeric fields receive equal partial credit, with all six needed for correctness.
- **Orientation and global fixed points:** select exactly the orientation preservation/reversal statement and the complete fixed-locus class (none, one point, one line, entire plane). This is a scored response. The exact original triangle may have clockwise or counterclockwise order. Fixed points are explicitly over the entire plane, not only drawn vertices. Incorrect orientation or locus yields zero credit under the complete-set rubric.

The native static drawing and equivalent coordinate text show only actual givens before saving: final Q for inverse; the six given source/target points for inference; original triangle plus named center/line givens for classification. An explicitly requested worked copy is available in acknowledged feedback/history only. Reset restores the exact original view, reused state is hidden on an independent phase, and accessible announcements describe the actual displayed result. Controls have explicit bordered styles and inner 58-point labels. Changes are discrete with no automatic motion. Numeric entry uses the existing touch/keyboard editor with visible unitful or unitless field labels. Saved typed history uses the same exact contract and labels.

## Compatibility and authority

The new request pin is copied by launchOnly, materialization, local checkpoint guards, cold archive classification, accepted snapshot checks, and ordinary retry/repair callers. Missing or future new pins cannot reopen a new contract for writable grading. Supported old schemas remain explicit, including forward12/schema9; no old scorer record is changed. The exact contract enables native protocol support only for these validated numeric/multiple-choice workflows, without widening arbitrary logic-state or multiple-choice admission. The released reviewed registry remains empty.

Generated runtime execution supports an authentic retained schema12 contract through the same typed response/scorer and original snapshot history; the authored generated fixture verifies cold restore and commit. The default AI authoring provider does not silently opt existing requests into recipe15. No source excerpts are added to cloud or generation payloads.

## Finite audit and limits

`Capacity.json` reports 334 delivery forms and 180 normalized targets: 79 coordinate targets and 101 reflection targets. The global orientation query has 44 diagram deliveries but only 9 normalized composition targets; triangle-only variation contributes zero extra targets. Unknown-map identity ignores unseen decomposition and is anchored to the visible correspondences. The independent oracle recomputes forward/inverse operations, signed triangle areas, unique orthogonal-class correspondence matches, and fixed sets on a half-unit lattice; the authored fixed points lie within its checked square.

These are development items, not reviewed B4 assignments. This recipe alone remains 88/66 targets short of a 167-per-family numerical target. The older forward recipe remains reachable, but a combined quota or cross-recipe independence claim needs a separately versioned inventory/exposure audit. Arbitrary non-quarter-turn maps, nonintegral new generator coefficients, and an unrestricted affine item registry are outside this bounded recipe; unsupported payloads are unavailable rather than assigned a guessed key.

QA-F11 engineering now adds genuine inverse/unknown-map responses to the existing forward quadrant/center/composition coverage. QA-F16 adds actual orientation/global fixed-point scoring to the existing axis/translated/diagonal reflections. Reviewer admission, empirical calibration, and full per-family quota remain open. No protected scored question gains a solving tool.

## Tests and verification

Nine new `CoordinateReasoningContractTests` cover the complete finite generated keys in EN/JA, fractional equivalents/partial fields, wrong orientation or fixed class, underdetermination/extreme values/protection/future policies, original-only rendering/reset, old recipe/pin preservation, actual secondary catalog launch → async autosave → cold async Submit → immutable history → Next in both families, generated inference/classification cold commit, missing-pin refusal without changing original file bytes, and actual detached factory reachability.

The existing cross-platform `TypedGeometryArchiveWorkerTests` gains `testAdvancedCoordinateDraftsColdStartupAndAutosaveUseRealBackgroundWorkers` for both advanced families. Its mixed test also includes them. These use actual file-backed repositories, detached startup and autosave actors, exact cold response preservation and no fabricated attempts, with fully isolated caches/widgets/rotation state. The existing iOS native-input target already includes this file; no target change is needed.

Executed here: standalone geometric oracle; syntax parse of every patched Swift file; JSON syntax; current-tree patch apply check. App compilation, nine test execution, advanced iOS worker execution, and installed secondary-button journeys are pending root verification. Prior root passes for net14 and the six earlier worker methods do not constitute evidence for this new recipe.

## Merge notes

Prepared against the current main files including root validation-frame splitting, async Submit, and adaptive V3 criterion/receipt helpers. Existing generator makeDraft/assembleCandidate and fallback factory frame boundaries remain intact. New contract validation is guarded by metadata presence in fingerprints. No production files were edited by this agent. `prepare.py` recreates the patch from a fresh working tree and may fail intentionally if a claimed hunk changed.

Final renderer review correction: the original Canvas always consumes only `drawingPlan.originalPoints`, while the optional worked Canvas is a separate view. Revealing/resetting a worked copy cannot change the original plot extent or its coordinates. The existing rendering regression now checks that immutable original plan across reveal, independent re-entry, and reset. macOS announcement safely uses `NSApp?.keyWindow`.

Actual iOS execution on sourcef9541cc4: all33 selected regressions pass (14 ordinary Submit,12 generated Submit,7 typed archive methods including advanced coordinate families). This is real simulator worker execution, not the installed interface journey. Concurrent Mac full suite was stopped after fingerprint worker stack overflows; no full-source acceptance claim.

Complete Mac execution on sourcef4c27814: all1343 tests pass281.085s, including this implementation. Root repaired the NFChoiceOption full accessibility label and split fingerprint validator frames without changing fingerprints or checks. Per-slot timing-change repair and new installed advanced-coordinate journeys are separate pending verification.
