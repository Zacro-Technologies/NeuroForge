# Restore runtime and linked deletion

Implementation candidate, 2026-09-05. This is not full migration or privacy-release certification.

The live Settings preview freezes exact permitted bytes and destination census. Staging publishes one private request and blocks study tools pending genuine process restart. Cold startup holds the application lease, reconciles accepted authority first, compiles only against the reviewed destination, applies both SQLite domains and exact side-file operations, verifies a fresh census, and writes a completion receipt. Counts survive cold launches until explicit durable acknowledgement; a completed plan never replays later legitimate changes.

Live explicit source, generated payload, generated history and annotation deletion now crosses a required AppStore guard. The controller seals the prior producer, authenticates every request/journal, rejects pending or unknown recovery, invalidates completed payload authority before cleaning known private files, retains tombstones/counts and authorizes the synchronous primary deletion only after verified cleanup. Failure remains retryable; successful cleanup renews the request producer. Account and whole-device seals remain permanent. Duplicate import replacement and cloud tombstone application use the same boundary. Ordinary completed-draft retention transitions and collection Unsave keep their existing promises.

Sixty-eight focused tests pass on source adb80cf0707caf39f893c486498f78d39f305dce08021b74da8340fdb54f9956: 41 compiler/domain/cold/runtime/deletion, ten request-store and17 localization. This includes actual two-file SQLite recovery, staged-byte identity, destination drift, cancellation before acceptance, rejection after acceptance, interrupted invalidation/cleanup, unknown/foreign artifacts, live AppStore guards, genuine source-file deletion ordering and narrow cold cleanup retry. See Run_Manifest.json for logs/results.

A UUID-isolated DEBUG installed harness now uses the production cold coordinator, two actual stores and exact side roots, real Settings preview/policy/stage screens and real completion acknowledgement. Its synthetic backup-selection button substitutes only for the system file picker. The installed74.281-second journey passed on source ad19906: reviewed staging waits for a genuine cold launch, completion counts survive another cold launch, explicit Continue acknowledges them, and a later launch neither repeats the banner nor reapplies the restore. The loaded runner hash was verified. It does not certify the system file picker, physical crashes, account transitions, CloudKit, genuine shipped-store migrations or all primary-delete compensation boundaries.
