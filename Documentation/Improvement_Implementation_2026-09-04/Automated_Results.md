# Automated execution record

The current implementation is not release-certified. Run_Manifest.json records exact logs, result bundles and environment. Verified_Coordinated_Source_Hashes.json preserves the completed coordinated run inputs; Source_Hashes.json tracks subsequent runs.

## Latest fully passing unit baseline

The 01:47 macOS run passed all 784 tests, with zero failures, in143.453seconds. It covers the shared lifecycle, exact source-review adapter, generated confidence/hint checkpoints, atomic reservations, conflict journal, granular history recovery, saved-skip retries, explicit calendar policy and independent presentation pins. The earlier784-test run had16 failed assertions in four methods; accepted-history/sequence fixtures and finite-state expectations were corrected without runtime changes before the successful run.

Additional remaining-gap changes—compatibility review timing, drawing decode/render bounds, accessibility announcements and background diagnostic projections—postdate that successful input manifest and require another integrated run. A prior full pass is not evidence for subsequent edits.

## Expanded execution after the coordinated baseline

The expanded Mac suite executed **820 tests**, with **818 passing and two failing source-style checks**, in 144.735 seconds. Both failures flag the same multiline slider tint because the source scanner recognizes its continuous-control exception only on the declaration line. This is a failed run, not a new green baseline. New timing, reminder, frozen-plan version, archive-preparation, history, scratchpad-safety and diagnostic tests otherwise passed. Three earlier compile attempts stopped on synthetic test constructor/type mismatches before application tests ran; their exact logs and input digests remain in the manifest.

The expanded iOS build-for-testing succeeded. Its exact installed app, debug library and runner hashes were checked before execution. Xcode subsequently moved the runner to another live container; the injected binary hash still matched. The expanded 22-test run completed **21 passing and one failing**, in 904.966 seconds. The searchable source-chooser journey and all prior20 journeys passed. Energy Cancel failed because of an ambiguous wrapper selector. Its original failure and screenshots are retained; current capture export is in progress. The corrected selector must execute in a later compiled run before that journey is claimed passing.

Safe-exit recovery, responsive contextual scratchpad, effective chart/ability projections, chart-to-history inspection and further mental-math adapter changes postdate those compiled binaries. They require their own integrated verification.

## Delivery and recovery execution

The next integrated Mac run executed **852 tests: 832 passing methods, 20 failing methods and 32 failed assertions**, in 145.981 seconds. The new progress projection/inspection tests, mental-math adapter tests and most safe-exit/scratchpad tests passed. A real adaptive-launch identity error rejected valid materialized questions by comparing their transformed seed with the bank input seed. Two protected fixtures assumed a numeric first assessment item, one historical self-report fixture retained a protected source marker, one scratchpad canary violated the user-facing text validator, and two pagination strings lacked catalog entries. These failures are being corrected; this run does not replace the green baseline. All intermediate compiler failures are retained in the run manifest.

The corresponding iOS app and 23-journey runner built successfully. A first installed attempt injected an obsolete bundle from `containermanagerd/Dead` (verified by its different SHA256 and missing chart test), even though the explicitly installed files matched the build. That attempt was interrupted and is excluded from current verification. Both test apps were removed from the dedicated disposable simulator and it was rebooted before the next installation.

## Installed UI verification

The corrected20-journey run passed20/20 with zero failures in843.368seconds at02:03:04. Claim/evidence, every typed-answer and finite-capacity journey, English/Japanese/source exact cold continuation, settings and nested navigation passed. Current captures were exported and visually inspected; the capture manifest maps them to the finalized result bundle. This binary predates subsequent accessibility, timing, scratchpad, history and asynchronous preparation changes.

The previous coordinated simulator attempt was interrupted because Xcode injected an obsolete test bundle from containermanagerd/Dead despite rebuilding. That attempt cannot certify current code. Both apps on the dedicated disposable simulator were uninstalled, the simulator rebooted, and current binaries explicitly installed. Built/installed test binary SHA256 hashes matched; the live session log confirmed the new bundle loaded from its current container, and renamed finite-capacity tests executed.

The earlier complete19-journey run passed14 and failed5 in770.419seconds. Original failure logs/screenshots remain as pre-fix evidence. A Swift6.3.3 IRGen crash in an earlier build was fixed with an explicit Binding setter closure; it is not an application-test failure.

## Independent content and policy evidence

The specification validator passes all256 normative requirements,58 mapped families,60 adaptive fixture specifications and62 cross-chapter acceptance specifications. It validates documentation only.

All16 deterministic synthetic golden files reproduce. All6 GoldenArchiveFixtureTests passed in the784-test run, including actual10,000-attempt restore and seeded runtime phase recovery. These are synthetic fixtures, not genuine frozen shipped binary-store fixtures.

See the content, reservation, adaptive and experience ledgers for scoped evidence. Editorial approval, physical devices, signed services, manual assistive technology and human learning benefit are not established by these runs.
