# Mixed reviewed ordinary practice

All 14 new mixed fixtures passed inside 51 controller tests; the complete focused scope passed 146/146 on 65f7773d. Source is integrated; the older scratch-only instructions below describe the handoff, not its current integration status.

Scratch only; apply after writer fence and reviewed controls UI. Four existing files; no project generation. Full-file parser and current patch apply checks are the only verification performed by this agent. Root owns compilation and execution.

## Live path and compatibility

`NFEditorialAdmissionContext.sessionPin` creates schema2 `EditorialMixedControllerV1` only for NEW mixed native ordinary requests with two or more in-process trusted compatible families at B1. The released registry stays empty. Explicit starting bands remain a single-family request; old schema1 pins and receipt schemas1–4 retain their original protocol. Unknown mixed versions remain recovery-only. No catalog/generator/exercise version changes.

The existing AppStore prepare/accept path ranks one proposed exact item per hard-feasible family. The selected receipt is schema5 and retains its selected family’s existing controller decision plus `NFEditorialMixedDecision`. Only one cursor position/slot is accepted. Full candidate materialization, quarantine/evidence, family state and anonymous exposure counts are recomputed at acceptance under the existing CAS/publication lock.

Each family references its own most recent accepted controller receipt and at most one unprocessed outgoing observation/support event. Other families neither receive that response nor advance automatic allowance counters. The global support-offer window advances across all displayed Skip/Reveal/help events while independent evidence remains family-local. Same-family explicit Next choices and Replace override variety preferences; the current question never changes in place.

Family ranking is lexicographic: current session coverage deficit, declared due/repair priority, run/share/structure preference, declared goal/weak-criterion match, stable length-delimited hash. Counts are from actual first display acknowledgements, including skipped/replaced/assisted slots. Reservations without display are excluded. The prior99 exposed slots determine the prospective100-item family share. Separate semantic/structure/representation/family counters and infeasible preferences are retained. Current ordinary native candidates have neutral declared due/goal priorities; this does not manufacture review contracts or learner weaknesses.

The frozen exposure-count digest contains anonymous aggregate facts, not live cross-session references or raw responses. Within-run family/deferred-event references are structurally validated against receipt lineage and the ledger. Exact export/import strips local mixed receipt authority using existing full receipt pruning; linked deletion retires the entire affected run and preserves anonymous consumed positions.

## Narrow presentation hook

`NFEditorialChallengePresentation` adds optional `mixedFamilyTitle` (exact frozen exercise.title), `mixedFeasibleFamilyCount` and `varietyUnavailableReasons`. When title is nonnil, show it and `This activity mixes reviewed families. Each family keeps its own challenge.`; for count<4 additionally show the reduced-scope key in catalog-entries.json. Pending/Keep explanations automatically describe this family. No timer or native editor changes are needed. Experience owns the Universal view hook.

## Verification

Fourteen new fixtures in existing EditorialLiveControllerPersistenceTests cover actual mixed10/four families/one-slot publication, giant pool and array order, sparse scope, manual next-family scope, global help window with Skip/Reveal, genuine disk reopen and deferred lineage, correction CAS, Replace/exact retry, future protocol refusal, failed publication/same command, prospective rolling100 quota and explicit infeasibility, corrupt cross-family event, own-family-only automatic promotion, portable authority/deletion. These are synthetic trusted manifest fixtures over actual materialized native keys, not editorial review signoff or empirical learning validation.

## Remaining limits

Shipping reviewed admission remains externally blocked pending a real signed reviewed manifest. This package does not open the 7,000 legacy catalog, supply missing reviewed families, invent transfer/review eligibility, make across-device active ownership guarantees, or finish the wider generation-bearing legacy command migration. It does not synthesize unavailable goal/criterion relevance. Full background selector execution and release performance benchmarking remain part of CORE004 rather than a claim of this patch.
