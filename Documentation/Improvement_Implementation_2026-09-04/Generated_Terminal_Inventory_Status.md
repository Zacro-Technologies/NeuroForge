# Terminal generated inventory release

Integrated and executed: all nine new terminal inventory methods passed within20generated lifecycle tests in the170/170 scope on2a891389. Actual iOSbuild succeeded4e90; complete new native journey remains separate.

Scratch only. `terminal-retention.patch` changes six existing files; no @Model columns, new project files or localization keys. Parent owns builds and test execution.

Ending and completion now create a deterministic compact terminal proposal before the private checkpoint saves. It retains the exact question prefix through the accepted current slot, all slot/attempt IDs and outcomes, current response, confidence, assistance, optional typed working/inspection state and unfinished work. It removes future questions and request source-chunk text outside the retained exercise reference closure. An optional versioned record preserves original planned count and original result/request digests. The current result/request changes only after acknowledgement; save failure keeps the prior full run/editor. Summary totals continue to describe the original workload.

The retained source closure includes authored citation IDs, authoritative provenance, sourceContext, exercise citations and chunk locators, and grounding references. Missing exact chunk identity retains the relevant document's supplied excerpts; unresolved grounding identities conservatively retain the original input chunks. This never resolves old references through a current mutable source. Per-attempt snapshots remain the history citation authority. Original generation provenance, saved collections and immutable attempts/snapshots remain unchanged.

Repository CAS permits only the exact deterministic transform of the authenticated predecessor. An imported compact flag/digest cannot authorize changed questions, altered original count, source substitution, or re-expansion of an existing compact terminal. Active/nil legacy encoding is unchanged. The only request-model change is value-type `sourceChunks` changing from let to var, so the local copy can prune that array without reconstructing/normalizing other historical request values.

Existing payload maintenance now scans authenticated inactive terminal records and atomically replaces eligible private payloads. It skips the current owned presentation, foreign owners, unknown/ambiguous legacy state and invalid original records. Modern revision increments invalidate stale cached writers. Original study timestamps stay fixed. Inline payloads are released in the same archive transaction; there are no detached file deletions. A failed write keeps all original archive bytes for retry. An accepted suspended run remains fully pinned after temporary inventory expires. End is not deletion and explicitly saved sets remain reusable.

## Nine new tests

Appended to the existing GeneratedLifecycleParityTests class via extensions:

1. Real file-backed ending and reopen retain exact unfinished current question/response and original 1-of-4 scope; existing receipt/snapshot bytes match.
2. Failed terminal write retains full inventory/editor; retry accepts compact 0-of-4 terminal.
3. Completion retains all attempted questions, immutable receipts and original scope; repeated terminal actions are idempotent.
4. Repository CAS rejects altered totals, extra future questions, changed request metadata and terminal resurrection.
5. Maintenance skips active/owned/ambiguous records, updates inactive known terminal revision once and remains idempotent.
6. Authentic source-grounded fixture drops only future source chunks and preserves exact current citations and the complete explicit saved collection.
7. Day-eight cache expiry removes new-launch inventory while accepted suspended work resumes exactly; explicit End then releases the unused prefix.
8. Real filesystem obstruction preserves all original terminal payloads until storage is restored; retry and cold reopen verify the compact result.
9. Lowercase UUID document-only references retain the same source identity; unresolved legacy document references conservatively retain original request excerpts.

## Verification performed

All six full changed Swift files passed frontend parse. Current `git apply --check` passed. The complete generated runtime, new draft/retention policy, repository save/CAS extension and all nine tests passed strict Swift 6 typecheck against the compiled app. The isolated harness substitutes declarations for the repository's private writer-field/replace members and an equivalent complete typed request copy because the compiled module still exposes the old let array; it is not persistence execution. The actual maintenance implementation remains in NFLocalSessionRepository.swift where those private members are accessible.

No app build, unit test execution or installed UI test was performed by this agent. Suggested root run: GeneratedLifecycleParityTests, generated/source/continuity suites, archive export/restore and locale tests. No new external/manual claim is made.

## Remaining architecture work

This package does not migrate synchronous whole-archive writes off the main actor or finish generation-token ownership for all ordinary/generated commands. Those remain separate concrete engineering work. It does not certify every device/window/accessibility acceptance path.
