The 17:07 installed run fails both methods in the common page reveal helper. Final recording frames show the coordinate y field visible above the keyboard/footer and the cube still inside the orbit surface after repeated center drags. These are diagnostic images, not acceptance evidence. The revised helper scrolls via the margin and checks actual full-target visible bounds. Native replay is required.
