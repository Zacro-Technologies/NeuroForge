# Installed f4 coordinate and net review

Source f4c2781437bfb5c61e4bb04689d24d312671c3c2bb181fcbee4c9dd2bf5394a5, verified actual runner ecfd60c612dad7282f5e6bc53381af39d459007cd632190108f2372846515509. Four-test result at /tmp/nf-advanced-coordinate-five-installed-20260905.xcresult; the filename includes an unexecuted misspelled fifth selector, not five passing cases. Correctly named generated test passed separately on the same installed binary.

The reviewer inspected the actual coordinate, reflection and net worked screenshots. Coordinate positions and fixed axes match the independent public-givens answers (-3,2) and (0,0); coincident P/Q are combined. Both installed first questions are inverse tasks. These images do not establish installed affine-inference/orientation-classification coverage. The original source text remains unchanged through reveal/reset according to assertions, but it is outside these worked-image crops.

Finding: the coordinate center C1 overlaps the adjacent x-axis tick1 and can read C11. Root moved centers below tick labels and combined coincident center names in a later source; new installed image verification is pending. The worked explanation reaches the fixed footer in the captured coordinate crops; complete unobscured-paragraph layout still needs the newer native capture/AX-boundary inspection before claiming visual closure.

The net screenshot proves displayed completed-frame text and fold controls, with all six normals/arrows checked against an independent shared-edge oracle. It does not show the actual cube drawing. Root added a named accessible net-model container and explicit full-model screenshot to the subsequent test; geometry visual acceptance remains pending that capture. No manual VoiceOver or large-text matrix is claimed.
