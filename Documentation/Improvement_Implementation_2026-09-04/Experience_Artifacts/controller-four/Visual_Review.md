# Installed four-flow review on source536f

The exact source/build/loaded runner and every failure are in Run_Manifest.json.

Transfer feedback image visually verifies original committed relationship, expected unit-bearing35litres and visible Next. The installed test separately passed first-stage no-score/clear/Undo, cold saved relationship, one final scored answer and History.

Spatial video frame verifies restored object copy x3,y17 after the real post-feedback transform/reset. The test passed those object mutations, then failed because the original selected answer was below the visible region. Whole flow is not yet a pass.

The attached actual Data AX tree confirms both expanded original rows are independently represented with exact counts43and21, but both inherit `session-data-table` from the enclosing DisclosureGroup despite the explicit child IDs. The keyboard is absent. Root is scoping the toggle identifier to its actual header. The video has a stale final frame before expansion, so no expanded-table visual capture is claimed for that frame.

Graph has progressed through real focus/count selection; the actual launch button remains34.5points despite its surrounding44point layout. A new label-internal minimum has been authored and awaits installed remeasurement.
