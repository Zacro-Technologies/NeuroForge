# Improvement implementation — 4 September 2026

This working tree implements substantial reliability, grading, navigation, personal-study and evidence-policy changes. **The complete specification is not finished or release-certified.** All 256 normative requirements are accounted for in [Requirement_Traceability.json](Requirement_Traceability.json). Source presence does not substitute for acceptance evidence.

Implemented areas include exact local continuation, prepared-save recovery, one-writer takeover, typed history, inline confidence/feedback, optional reflection, progressive authored hints, explicit ending, protected sitting continuation, corrected scoring authorities, five independent destinations, saved sets/collections/notes, shared editorial policy, archive 18, atomic fixed-launch reservation/exposure records, a shared ordinary/generated/source session lifecycle, append-only reflection revisions, dual-payload conflict recovery, granular history snapshot recovery and local data lifecycle handling. New local snapshots are scoped to the existing durable-store account namespace. Original persistent model fields remain unchanged.

Recent implemented slices add durable estimate-before-exact math work, prediction-before-data inspection, bounded pseudocode stepping with saved first predictions, a linked claim/evidence/experimental-choice science study, exact spatial demonstration-copy transforms, native ordered-step drop/undo handling, a cold restore coordinator over two SQLite stores and exact side files, durable completion acknowledgement, and restore-artifact cleanup before explicit private-data deletion. These remain unsigned development content and do not establish reviewed learning outcomes. New opt-in recipes have separate identities; legacy recipe IDs and payloads remain unchanged.

Latest full Mac run: **1,362/1,362 tests passed** on source `aaae840e`, including three dashboard worker tests. Actual iOS worker execution passes 13 generated Next/End cases on that source. Earlier iOS execution passed 33 submission/archive regressions. The newer full run on `b7fe7284` passed 1,396/1,397 methods with no worker stack crash; one due/legacy setup method failed. Generated unscored, spatial assembly and dashboard refresh cases passed within that partial run. Actual iOS execution separately passed 21 generated-unscored/typed-geometry worker cases. The last fully passing baseline remains 1,362 until the remaining failure is resolved.

Installed iOS source `f4c27814` passes four advanced-coordinate/reflection/net/source journeys and, in a separate run with the same verified binary, generated-set continuity. Both advanced first questions were inverse tasks; no installed affine-inference or orientation-classification coverage is claimed. Earlier intermittent Resume failure remains retained. A newer verified `8dd5232a` binary passes public-coordinate, generated-set and weekly-chart/history journeys. Three other methods failed: two before secondary launch in a test helper and one after cube folding/reset on an off-viewport choice lookup. Those helper fixes and explicit affine/orientation fixtures await a separate installed run. The new coordinate/model captures are under visual review.

Async autosave uses the storage actor for ordinary and modern/legacy generated debounced saves. Ordinary/source/protected and generated explicit Submit use awaited prepared-checkpoint, retained-attempt and feedback protocols. Generated Next/End also await verified storage before publishing the next item or ending. Reference and feedback publication wait for verified storage, including exact repair after a rename whose directory verification fails. Other transitions and administrative writes still require migration. The Overview evidence/calibration/chart reducers now run on a worker; incremental and time/calendar invalidation follow-ups pass their actual-store regressions. All-time engagement and filtered mental-math reductions are now integrated into that worker and await execution. All 58 catalog activities have unscored task-structure examples, with admitted demand/duration projections where supported. Each result retains its own source digest and failed runs remain recorded. No complete-specification or device-matrix certification is claimed.

Review [Product](Product_Status.md), [Experience](Experience_Status.md), [Content](Content_Status.md), [Adaptive](Adaptive_Status.md), [Runtime](Runtime_Status.md), [QA and delivery](QA_Delivery_Status.md), [protected exports](Protected_Portable_Archive_Status.md), [historical corrections](Historical_Content_Correction_Status.md), [reservations](Reservation_Implementation.md), and the [run manifest](Run_Manifest.json). The [actual candidate census](Content_Corrective_Inventory_Census.json) records what was examined; it is not a reviewed balanced content edition.

The remaining gates are explicit: 58-family authored/reviewed B1–B4 demand progression and balanced admissions; reviewed adaptive selection and complete eligibility-aware reservation delivery; complete transition/adapter acceptance and background persistence/projection architecture; affected-store historical correction validation and successful completion of all installed lab journeys; genuine frozen shipped-store migration fixtures; physical account-cleanup and interruption proofs; large-data performance; physical and signed integration runs; manual VoiceOver/Pencil/keyboard/large-text review; stable-OS hang closure; formative usability rounds; and a separate learning study before efficacy claims. See each requirement's ledger row for the precise gap.

Retrieval's listed family quotas total 900 against the required 1,000. No replacement allocation is silently considered approved. User clarification remains pending, and the admission gate reports the 100-question deficit.

All destructive tests use disposable synthetic state. The UI harness has UUID-scoped files and preferences and CloudKit disabled. The current unit host also bypasses production bootstrap; test caches, documents, artifacts and widget publishing have explicit isolation. Earlier runs predate the tightened widget guard. Existing user edits to project/release documents are preserved. This bundle makes no claim that automated tests establish human learning benefit or signed external-service readiness.
