# Adaptive and evidence implementation — 4 September 2026

Current integration update (5 September, sourcef4c27814): the full1343-test Mac suite passes. Production launch/Next/Replace now use exact reviewed admissions and retained controller receipts, explicit controls, mixed-family quotas, separately scoped starting targets, demonstrated historical starting bands, current quarantine and actual observed-criterion tie preference. All58 catalog activities have task-structure examples and supported reviewed prelaunch/duration projections. The released admission registry remains empty. Per-slot timing-change persistence is a separately reviewed repair awaiting integration; actual authored repair/due-role and goal-relevance selection are not yet live. Full reviewed content, remaining native adapters and empirical acceptance remain. Older sections below retain earlier checkpoints; the current run manifest and focused status ledgers take precedence for execution claims.

This record distinguishes implemented deterministic policy from app integration, reviewed content, and empirical release evidence. It does **not** certify the complete specification. The legacy catalog has not acquired reviewed B1–B4 contracts by relabeling old difficulty values. Existing history is retained; unsupported ability/calibration/speed/improvement claims are suppressed.

## 5 September live controller integration

The actual ordinary one-objective/family native numeric/single-choice path now invokes the shared policy at launch, Next and Replace. Trusted full-digest admissions, controller pin, effective input digests, evidence summary, support presentations, before/after control, exact bank position and selected snapshot are durably published in one existing archive transaction. Acceptance re-fetches all physical immutable answer rows and re-ranks under predecessor/revision checks. Scored locks authenticate captured observations; skipped/revealed slots update support without inventing a scored answer. Local import cannot confer reviewed authority. Current quarantine and append-only corrections affect future selection without rewriting prior receipts.

The released admission registry remains empty. All twelve live-controller store tests and all sixty current editorial policy tests pass within the scoped116/116 run on `536f7dcc7ca4db8bdf41e4f2183f957bd3618d7a4ebb94a7a74edb2a7f0fce63`. A real conflicting duplicate-row fixture now verifies that scalar Progress DTOs, metrics and historical summaries agree with controller exclusion before UI winner selection; physical duplicate removal restores the retained original. Test admissions are synthetic and do not establish content review, clinical validity, clean speed or calibration authority.

Explicit Easier/Harder/Fixed controls, mixed-family coordination, all remaining adapters, background controller preparation, reviewed manifests and empirical acceptance remain in progress. The earlier table below is the initial policy audit; this paragraph supersedes its claim that no live control state or observation producer exists, without closing the remaining gates.

## Implementation and verification

- `Sources/Adaptive/EditorialBandEvidenceV1.swift`: typed immutable demand contract, explicit conditions/provenance, single live/replay projection, effective corrections, semantic deduplication, exact recent and demonstration windows, coverage flags, separate calibration and clean-speed eligibility.
- `Sources/Adaptive/NFEditorialPracticePolicy.swift`: cold starts and explicit overrides, bounded staircase, actual-band candidate filtering, deterministic lexicographic selection, recorded path, feasible pool/variety/shortage diagnostics, timed-fluency gate.
- `Sources/Assessment/NFEditorialSchedulingPolicy.swift`: retention/repair state, 1/3/7/14/30-day policy, time workload and review cap, protected seven-performance-dimension protocol, persistent form identity/cap/supplement model, honest completion predicate.
- `NFHistoricalPracticeProjection` preserves descriptive legacy counts/credit and personal-study histories separately, supports append-only correction dispositions and validated supersession chains, and classifies audited self-check ratings from actual response format. Its three new tests cover preservation, duplicate application and original-credit immutability.
- Existing `AdaptiveEngine`, `NFAssessmentDimensionReducer` delegate to the shared model through optional `AttemptDTO.editorialObservation`; unknown legacy band evidence does not produce theta or standard confidence. The old protected session data remains in its old protocol bucket.
- Existing improvement badges are suppressed pending a validated comparison protocol. Legacy speed records do not meet the new provenance predicate. The mental-math adapter no longer infers a learner's strategy or a scored unit criterion from template names/aggregate credit. Legacy retention scheduling ignores confidence and nominal difficulty, uses explicit day intervals and avoids arbitrary response-format shifts.
- Executed `swift test --package-path /tmp/NeuroForgeAdaptivePolicyTests` against copies of the four exact new production policy files plus the production FNV-1a hash and a minimal compile stub for the DTO convenience boundary: **54 tests passed, zero failures**, 4 September 2026, 22:02 EDT. This checks deterministic pure policy, not device persistence/UI/content validity. The test source is `Tests/EditorialBandEvidenceTests.swift`; executed output: [Adaptive_Policy_Test_Run.txt](Adaptive_Policy_Test_Run.txt).
- The coordinating full-app run executed all **54 editorial policy tests with zero failures** at 22:18 EDT, plus **nine session integrity review tests and five session budget tests with zero failures** at 22:20 EDT. The whole 652-test app suite still had 12 assertion failures in other tests; this is evidence for these test classes only. No learner pilot, signed content edition validation, accessibility equivalence review, or psychometric calibration has been performed in this task.
- `NFAssessmentEngine.nextStep` now calls the shared workload policy for actual next-item admission, reserving six seconds of confidence overhead. Existing legacy duration forecasts are labeled advisory; this does not admit legacy band evidence. Complete final coverage is recognized before a time or presentation-cap stop. The runtime bridge exposes actual-candidate planning with zero allowed when nothing fits; ordinary runtime integration and sitting-active clock accounting are owned by the coordinator.

## Every normative chapter-03 requirement

“Policy implemented” means executable model/code exists; the remaining integration/content gate is explicit in the last column. Nothing in this table claims pilot validity.

| Requirement | Status | Implementation evidence | Remaining gate |
|---|---|---|---|
| ADP-001 | in progress | Typed requested/delivered bands and receipt; independent target versus evidence summaries | Reviewed production demand contracts and integrated controls |
| ADP-002 | in progress | `NFEditorialDemandRecord`, compatibility IDs, optional reviewed durations; no float mapping | Catalog admission supplies truthful signed records |
| ADP-003 | in progress | `NFDemandVector`; timing is outside demand | Authored solution graphs and typed family parameter validators |
| ADP-004 | in progress | B1–B4 shared enum and titles | Editorial family review determines supported bands |
| ADP-005 | in progress | Fixture candidates change dependency/representation demands and chosen delivered band is asserted | Paired production examples/answer validators for every admitted transition |
| ADP-006 | in progress | `initialControl`; explicit reasons, B1 default, refresh probe | Live optional starting-check route and compatible recent/protected suggestion selection |
| ADP-007 | in progress | Separate challenge/timing types; fixed/adaptive controls; timed-fluency predicate | Runtime timing-only freeze test; approved fluency catalog |
| ADP-008 | in progress | Explicit override policy; ordinary mixed Replace now atomically retains original draft and accepts new identity at the same question index | Reviewed Easier/Harder next-demand control and fixed-block amendment remain; new Replace integration tests pending |
| ADP-009 | in progress | Typed selection reasons and rule-credit receipts | Localized actual-demand explanation from approved content |
| ADP-010 | in progress | Codable `NFEditorialPracticeControl`, counters, selected path and support window | Persist this state atomically with each accepted item |
| ADP-011 | in progress | Exact up/down thresholds, authoritative flags, adjacent probes, cooldown and caps | Live reviewed pool integration |
| ADP-012 | in progress | Deterministic capped eight-item set; sessions/days/formats/credit; historical date and hysteresis | UI exposes per-objective scope and historical support |
| ADP-013 | in progress | Leakage/exposure/accessibility predicates fail closed | Visual/alt-text/editorial review of every protected contract |
| ADP-014 | in progress | `NFEditorialProtectedPolicy`, seven performance IDs, distinct calibration lane, staircase/starting suggestion | Admit new form protocol; old forms remain legacy; durable coverage-chain integration |
| ADP-015 | specified | Stronger claims remain disabled | Consented representative-learner pilot; no contact/telemetry authorized or conducted |
| EVD-001 | in progress | Typed lanes, single IDs and attributed objective; raw observation plus pure derived state | Store observation provenance and decision/queue events in local durable envelope |
| EVD-002 | in progress | Last 20 in 60 stored canonical days; exact 0/1 flags, finite payload validation, descriptive planning score | Catalog/version support registry and effective day passed by runtime |
| EVD-003 | in progress | Independent predicate, assistance/unknown/source exclusions, 30-day semantic dedup, structure cap | Global local exposure/correction input integration |
| EVD-004 | in progress | Assisted events excluded from independent means; support window and pending repair | Durable hint/worked-step sequence and observed explanations in UI |
| EVD-005 | in progress | Salted one-in-five invitations; pre-outcome only; categorical version; 12 samples/two sessions; required/optional split | UI lock/invitation provenance, complete optional-decline history |
| EVD-006 | in progress | Fractional score retained once; exact authoritative full/zero flags; no inferred strategy | Content criterion IDs and scorer correctness contract |
| EVD-007 | in progress | Coverage enum/counts, no theta/psychometric uncertainty | Updated detailed Progress presentation |
| EVD-008 | in progress | Observation/semantic/family validity projection, derived corrected score, affected decision receipts | Append-only authorized dispositions and atomic publication of rebuilt summaries |
| EVD-009 | in progress | Monotonic chunk input, complete-duration/interrupt/resume guards; legacy speed suppressed | Runtime clock/focus interval accumulation and checkpoint fault injection |
| EVD-010 | in progress | Personal/source lane cannot populate practice/protected/calibration/retention skill observations | Local personal-study reminders and source UI |
| EVD-011 | in progress | Formal improvement badge disabled; compatibility groups explicit | Reviewed comparable-form protocol before stronger claims |
| EVD-012 | in progress | Protected model and result policy do not publish reusable keys; disclosed form invalid for resume | End-to-end history/export/a11y redaction and exceptional retirement transaction |
| EVD-013 | in progress | Full pure evidence state Codable replay equality tested; stable event IDs | Durable decisions/exposure/queue side effects replay alongside reducer |
| EVD-014 | in progress | One `EditorialBandEvidenceV1` version; unknown old band/conditions fail closed | Deployment version registry, migration and correction publication boundary |
| EVD-015 | in progress | Deterministic tests added; stronger claims disabled | All ten chapter release gates, especially production content and human pilot |
| SCH-001 | in progress | Coverage separate from sitting budget; 16 presentations; explicit linked supplements | Runtime resume of new reviewed protocol and actual presentation acknowledgements |
| SCH-002 | in progress | Validity, accessibility/language, scope/prerequisite, exposure/ownership, review and duration predicates | Runtime supplies durable ownership/reservation ledger and frozen snapshot |
| SCH-003 | in progress | Coverage/due/family/structure/goal/stable ranking; dominant-pool fixture | Rolling-100 real presentation telemetry stored locally; infeasible quota UI |
| SCH-004 | in progress | Explicit no-content result and familiar/lower/other/end options; upward requires three semantics | UI choice and reviewed alternative inventory |
| SCH-005 | in progress | Deterministic salted tie plus candidate secondary key and Codable path receipt | Atomic adaptiveItem/fixedBlock reservations, <=2 advisory prewarms, concurrent owners |
| SCH-006 | in progress | Review keyed objective/family/band/relation/comparability; familiar item not a novel observation | Reviewed sibling inventory and exposure contract |
| SCH-007 | in progress | Exact 1/3/7/14/30 ladder, due-only one rung, help/no-response handling, reset after repair | Persist queues and personal reminder policy separately |
| SCH-008 | in progress | Criterion-linked mistake status and explanation/repair/delayed progression policy | Durable explanation events and fresh sibling affordance in UI |
| SCH-009 | in progress | <=30% duration and <=5 items; unselected backlog retained | Today backlog UI and exact duration metadata |
| SCH-010 | in progress | Existing canonical-plan model retained; no evidence reducer rewrites blocks | Runtime amendment/save tests for underway plans |
| SCH-011 | in progress | Reviewed duration range and >=8 compatible median; mode overhead; one reflection | Runtime replaces blanket count generation and records phase medians |
| SCH-012 | in progress | Separate participation/planned completion predicate; checkpoint failure never complete | App completion transaction/retry route |
| SCH-013 | in progress | Stored canonical ordinals, no wall-clock elapsed reinterpretation; frozen selected path | Travel/offline/catalog update integration fixtures |
| SCH-014 | in progress | Existing adapters and policy modules changed as above | Reviewed catalog/runtime/UI connect the shared projection everywhere |

## All 60 mandatory fixtures

The isolated harness does not validate SwiftData or the DTO bridge; full app compilation remains separately required. A pure test may cover the policy part of a fixture while leaving its app/content portion unverified. `Covered policy` below references the named test in `EditorialBandEvidenceTests`; `Integration required` is not a passing test.

| Fixture | Status / evidence |
|---|---|
| ADP-F01 | Integration required: production B1/B4 paired demand and independently correct keys; fixture candidate demand assertion in ADP-F05 only |
| ADP-F02 | Covered value policy: `testADPF02TimingConditionDoesNotAlterFrozenDemand`; real UI/draft freeze integration remains |
| ADP-F03 | Covered policy: `testADPF03ColdStart` |
| ADP-F04 | Covered policy: `testADPF04ExplicitHarderStart` |
| ADP-F05 | Covered policy: `testADPF05StrongTrajectory` |
| ADP-F06 | Covered policy: `testADPF06WeakTrajectory` |
| ADP-F07 | Covered policy: `testADPF07MixedTrajectory` |
| ADP-F08 | Covered policy: `testADPF08PartialUpBoundary` |
| ADP-F09 | Covered policy: `testADPF09PartialDownBoundary` |
| ADP-F10 | Covered policy: `testADPF10ZeroFlagsMatter` |
| ADP-F11 | Covered policy: `testADPF11FixedBand` |
| ADP-F12 | Covered policy: `testADPF12UpwardThrottle` |
| ADP-F13 | Covered policy: `testADPF13FoundationFloor` |
| ADP-F14 | Covered policy: `testADPF14AssistedSequence` |
| ADP-F15 | Covered outcome policy: `testADPF15SkipsAreNotZeroCredit`; real exposure/replacement path remains |
| ADP-F16 | Covered policy: `testADPF16SparseNextBand` |
| ADP-F17 | Covered policy: `testADPF17EmptyActivityAndADPF18StableTie` |
| ADP-F18 | Covered policy: `testADPF17EmptyActivityAndADPF18StableTie` |
| ADP-F19 | Covered selected-path round trip: `testADPF19SelectedPathSurvivesCodableResume`; physical checkpoint/relaunch remains |
| ADP-F20 | Covered policy: `testADPF20UserOverrideDoesNotResetLimits`; timing UI integration remains |
| EVD-F01 | Covered policy: `testEVDF01SupportedBand` |
| EVD-F02 | Covered policy: `testEVDF02OneStructureGaming` |
| EVD-F03 | Covered policy: `testEVDF03SameItemGaming` |
| EVD-F04 | Covered policy: `testEVDF04OneSessionStreak` |
| EVD-F05 | Covered policy: `testEVDF05ConfidenceInvariance`; XP transaction test is runtime-owned |
| EVD-F06 | Covered policy: `testEVDF06InvitationReplay` |
| EVD-F07 | Covered policy: `testEVDF07CalibrationMinimumAndRetrospection` |
| EVD-F08 | Covered policy: `testEVDF08PartialConfidenceUsesBinaryFullCredit` |
| EVD-F09 | Covered reducer invariance: `testEVDF09EquivalentResponsesHaveIdenticalEvidence`; content equivalence scoring integration remains |
| EVD-F10 | Covered policy: `testEVDF10And11ResumeTiming`; real checkpoint path remains |
| EVD-F11 | Covered policy: `testEVDF10And11ResumeTiming`; real checkpoint path remains |
| EVD-F12 | Covered policy: `testEVDF12ClockDoesNotMeasureActiveTime`; runtime monotonic clock injection remains |
| EVD-F13 | Covered policy: `testEVDF13DuplicateCommit`; queue/state equal, runtime XP/exposure commit remains |
| EVD-F14 | Covered policy: `testEVDF14QuarantineAndEVDF16Reinstatement`; current visible item transaction remains |
| EVD-F15 | Covered policy: `testEVDF15FaultySiblingFamily` |
| EVD-F16 | Covered policy: `testEVDF14QuarantineAndEVDF16Reinstatement` |
| EVD-F17 | Covered policy: `testEVDF17PersonalSource` |
| EVD-F18 | Covered policy: `testEVDF18LegacyCannotBecomeBandEvidence` |
| EVD-F19 | Covered policy: `testEVDF19ReplayCompleteStateEquality`; full app path/XP replay remains |
| EVD-F20 | Covered policy: `testEVDF20UnobservedStrategyRemainsUnknown` |
| SCH-F01 | Covered policy: `testSCH01ProtectedLeakageRejected`; visible/a11y actual stimulus review remains |
| SCH-F02 | Covered policy: `testSCH02SittingBudgetAndSCH03NoAutowrong`; UI/save path remains |
| SCH-F03 | Covered policy: `testSCH02SittingBudgetAndSCH03NoAutowrong`; draft UI path remains |
| SCH-F04 | Covered policy: `testSCH04CoverageCapAndLinkedSupplement`; linked selection/exposure transactions remain |
| SCH-F05 | Integration required: all completed protected result/key surfaces |
| SCH-F06 | Covered eligibility invariance: `testSCH06AccessibleEquivalentHasNoAbilityPenaltyAndSCH07MissingForm`; reviewed actual form remains |
| SCH-F07 | Covered fail-closed selector: `testSCH06AccessibleEquivalentHasNoAbilityPenaltyAndSCH07MissingForm`; UI state remains |
| SCH-F08 | Covered policy: `testSCH08IntervalAdvanceAndSCH09EarlyRepeat` |
| SCH-F09 | Covered policy: `testSCH08IntervalAdvanceAndSCH09EarlyRepeat` |
| SCH-F10 | Covered policy: `testSCH10FailureWaitsForIndependentRepairAndSCH11RepairLoop` |
| SCH-F11 | Covered policy: `testSCH10FailureWaitsForIndependentRepairAndSCH11RepairLoop`; explanation UI remains |
| SCH-F12 | Covered policy: `testSCH12ReviewBudget` |
| SCH-F13 | Covered policy: `testSCH13OneMinuteReflection`; actual reflection activity remains |
| SCH-F14 | Covered policy: `testSCH14And15MixedFamilyBalance` |
| SCH-F15 | Covered policy: `testSCH14And15MixedFamilyBalance` |
| SCH-F16 | Runtime canonical plan transaction fixture required |
| SCH-F17 | Runtime amendment/completed-block fixture required |
| SCH-F18 | Covered policy: `testSCH18SaveFailureDoesNotComplete`; failure injection remains |
| SCH-F19 | Runtime travel/day/unfinished-form fixture required |
| SCH-F20 | Runtime catalog boundary/frozen item fixture required |

## Independent runtime review

`Tests/SessionIntegrityReviewTests.swift` now contains fourteen real store/runtime tests for imported digest tampering, foreign-owner recovery, conflicting drafts, nested protected export redaction, failed commit-tail checkpoint gating, private draft partition and same-device window takeover. These tests are not part of the isolated policy harness; the coordinator executes them with the app test target. Review findings sent to the coordinator include pending skip recovery, prepared-response edit conflicts, nested export redaction, duplicate dispositions during decode, per-item interruption reset, full sitting-time accounting and whole-archive rewrite cost.

## Explicit limitations

Policy APIs cannot manufacture missing reviewed question editions, empirical difficulty calibration, protected leakage review, accessible form equivalence, or a consented human pilot. Consequently this chapter must not be called fully implemented or release certified based on the pure test run. No evidence was fabricated to close these gates.

## Durable ordinary selection receipts added

The new `NFSelectionReservationLedger`, `NFLegacyRotationMigrationBridge` and production `NFLocalReservationBridge` add consumed-position and separate-exposure receipts, exact snapshot/attempt identities, idempotent decisions and actual old-cursor migration APIs. Repository save now publishes the current ordinary receipt atomically with the run. See [Reservation_Implementation.md](Reservation_Implementation.md) for implemented invariants, exact migration namespaces, the still-separate legacy launch transaction and remaining reviewed-policy integration. Eleven isolated core tests passed at 23:08:14 EDT; six app bridge fixtures await the coordinating app run. These receipts reduce ADP-010/DATA-015 integration gaps without changing the status of unavailable reviewed B1–B4 inventory.

The 23:07–23:09 EDT coordinating 685-test app run also executed **55 editorial evidence tests, 11 reservation core tests, 14 integrity review tests and five workload tests with zero failures in those classes**. The whole run had one session-completion assertion failure; the coordinator corrected completion versus sufficient-evidence semantics and is rerunning the complete target, including all six bridge fixtures. These are scoped passing results, not a whole-suite pass claim.

## Fixed-launch transaction follow-up — 5 September

The previously separate UserDefaults launch write has now been removed from production `beginSession`. A pure exact-plan proposal is accepted with the local cursor, full plan, first checkpoint/snapshot and run/decision receipt in one revision-checked file transaction. Bootstrap preserves the actual legacy ledger and subsequent launches use only the new local authority. Command replay, stale proposals, corruption, failed publication, source changes, deletion and export boundaries have dedicated source fixtures pending the coordinator's run. Minute-only choice cancellation consumes nothing; accepted five-question alternatives use canonical launch. Exact fixed descriptors no longer substitute another seed when excluded. See [Reservation_Implementation.md](Reservation_Implementation.md) for the still-open eligibility-hole/global adaptive-controller and cross-version concurrency boundaries. No reviewed-content signoff is invented to close those requirements.

Verified in the 5 September app run: all **55 editorial evidence tests**, **20 then-current focused rotation integration tests**, and **six local reservation bridge tests** passed (00:07–00:08 EDT). The new unreceipted-command restoration guard/test was added afterward and awaits the next run. See [Atomic_Fixed_Launch_Test_Run.txt](Atomic_Fixed_Launch_Test_Run.txt) for the scoped actual output; no whole-suite pass is asserted from those subsets.

The later fixed-mixed launch change closes the production quarantine-hole limitation in DATA-015/SCH-005: the unchanged epoch permutation is scanned for feasible exact descriptors, skipped positions remain unconsumed, and full-block capacity plus run-wide semantic exclusions are checked before atomic acceptance. Optional sparse positions preserve old prefix decoding and migrate without losing holes. A reinstated question retains its original position/identity; a depleted epoch with unresolved holes fails explicitly rather than rerolling. Fixed requested count and original/canonical retry identity remain intact. The reviewed adaptive next-item controller, explicit amendment/Replace UX, and external reviewed-content gates remain open. The 23 focused integration and 12 rotation test sources include new regression coverage; the coordinator has not yet executed this code. See [Reservation_Implementation.md](Reservation_Implementation.md).

CORE-003 materialization audit found that globally formatted numbers were entering estimate/exact answer aliases, and globally localized numeric placeholders/cube descriptions were entering saved question digests. The fallback generator now uses canonical rational numeric keys and the saved request locale for display text; cube description materialization accepts that explicit locale. Three added `ContentImprovementRegressionTests` cover region-independent numeric contracts, rejection of thousandfold scale errors, saved-locale digest stability and a serialized protected numeric resume under different UI language. Preference fixtures use only synchronously restored volatile defaults. Swift parse and diff checks pass; app execution of these new fixtures is pending. Changing future generation does not rewrite historical snapshots or recreate an old missing locale input.

The frozen-contract follow-up now rejects contradictory numeric exact aliases only for the explicit `estimate-then-exact.v1` contract. An alias such as German display `9.504` cannot authorize 9504 under the canonical decimal grammar; the scorer returns `invalidItem`, without an objective learner error or answer key. Declared estimate alternatives, judgment aliases, unit-bearing label contracts and distinct outputs in unrelated logic tasks remain supported. `NFContentCorrectionPolicy` emits an append-only `contradictory-estimate-exact-alias` exclusion only from the authenticated original snapshot, including a SHA-256 digest of that exact retained contract. It suppresses the legacy equivalent-state credit repair for the malformed contract. AppStore reconciliation includes scorer-version-8 attempts only when the narrow predicate applies; other modern results remain outside this historical audit. Original question, response, score and activity remain unchanged.

Five additional source regressions cover bad German frozen aliases, legitimate alternatives, authenticity/missing-snapshot boundaries, backward decoding of the optional contract digest, and real AppStore disk reload/idempotent exclusion. The content class now has 19 tests and the historical-correction class 21. Swift parsing and `git diff --check` pass; these five fixtures await the coordinator's app run. This bounded fix does not infer absent historic locale data, retrospectively assign a band, or reinterpret arbitrary unit/prose aliases as numeric answers.

CORE-004 follow-up removes an unbounded file-lock wait from the MainActor repository: `withPublicationLock` now acquires `LOCK_EX | LOCK_NB` and returns recoverable `RepositoryError.busy` for contention, before transaction work. No retry, sleep, cancellation or partial publication is introduced. The 24th focused integration fixture holds the real publication lock through a competing descriptor, verifies exact predecessor preservation for both prepare and accept, then releases the lock and retries the same command idempotently. Swift parse and diff checks pass; the new fixture awaits the coordinator's app run. CORE-004 remains partial: catalog construction, whole-archive encoding/decoding and bulk history projections still require async immutable-value pipelines, revision revalidation at publication and measured Release responsiveness. Existing 10,000-attempt correctness fixtures do not establish latency budgets.

Verified subsequently from `/tmp/neuroforge-reviewed-recovery-unit-tests.log`: all 23 eligibility-hole/fixed-launch integration tests passed at 00:41:36 and all 12 pure rotation tests passed at 00:42:51 EDT on 5 September. That enclosing 742-test run reported two failures (one unexpected); neither these scoped results nor earlier 20/20 output certify the newer code.

DATA-012 now has a separate append-only local conflict journal. It captures immutable value snapshots of all 49 shipped AttemptRecord columns, their exact raw payload digests, stable semantic commit digests, the original authenticated snapshot when available, the explicitly proposed snapshot and the frozen proposed score. The stable conflict identity ignores fresh retry timestamps/device IDs, preserves the first captured proposal and never creates another AttemptRecord or XP event. Record/snapshot comparison happens before `retainSnapshot`, so a rejected retry cannot attach its new question as an absent original. A separate pending-conflict queue and the runtime's frozen prepared draft retain unsaved proposals when publication fails; retrying the queue saves diagnostics rather than acknowledging a new answer. Original records stay unchanged. Durable/in-memory conflicts are excluded from derived evidence through `effectiveAttemptDTO`; full conflict bodies with protected content stay local, and portable exports carry only protected conflict references. Import validates versions, identities and payload digests; linked deletion removes diagnostics and private history backups.

Five `AttemptConflictJournalTests` were added in the existing `LocalLearningLifecycleTests.swift`, covering duplicate/reordered retry behavior and unchanged XP, the missing-original snapshot ordering defect, failed-write queue/reopen/delete behavior, malformed/future import rejection and actual full portable protected-key canaries. Content's generated early-receipt callback uses the same journal API and retains the frozen draft on failure. Generated and ordinary source self-checks keep source references in private snapshots while new cloud-capable answer-key columns remain empty. The latest source parses and has a clean diff check; the current unit and installed-UI runs (`/tmp/neuroforge-session-recovery-unit-tests.log`, `/tmp/neuroforge-session-recovery-installed-ui.log`) are still pending. No conflict-resolution decision or historical score replacement is invented by this diagnostic mechanism.

The subsequent coordinated app run (`/tmp/neuroforge-coordinated-recovery-unit-tests.log`) executed all **five AttemptConflictJournalTests with zero failures** at 01:38:22 EDT on 5 September, including failed-write retention/retry, raw-digest import rejection, protected portable output and lowercase imported source-identity deletion. The new competing-file-descriptor lock regression also passed. All **21 historical-correction tests** and **six granular history recovery tests** passed at 01:39:36. All **19 content-improvement regression tests** also passed at 01:38:24. These are scoped results: the enclosing run completed 784 tests with 16 assertion failures (one unexpected) at 01:40:36, including existing focused-selection assertions for the newly retained-feedback exhaustion state. Installed UI verification and corrective reruns remain separate. No full-suite or responsiveness benchmark claim follows from these passes.

The coordinator subsequently completed `/tmp/neuroforge-final-recovery-unit-tests.log`: **784 tests, zero failures**. This is the verified input before the following reminder correction; it does not certify later edits or installed UI behavior.

SCH-007/EVD-008 follow-up found the actual daily-plan builder bypassing the effective projection: it set retention repetitions to the raw number of attempts and anchored the interval to the latest raw timestamp. That could turn eight early successes (or wrong/assisted history) into a 30-day interval and keep excluded conflict/correction records in review selection. `NFCompatibilityReminderPolicy` now accepts immutable effective values, rejects conflicting duplicate IDs and invalid/future payloads, excludes protected/personal/self-rated/skipped/revealed/assisted results, and derives only a one-day compatibility reminder from full historical credit. Early/familiar work does not postpone it; a due fresh full response refreshes the same one-day reminder. Actual retained semantic snapshots and all historical seeds prevent known-repeat novelty claims. It does not infer independence, an editorial band, retention mastery or higher interval rungs from missing legacy conditions. Reviewed interval progression remains the separate `NFEditorialRetentionPolicy`; its production reviewed-content/controller integration is still open.

Five new source regressions in the existing `AssessmentSchedulerTests` and `PersistenceRuntimeTests` cover raw-count invariance, due/early/familiar behavior, invalid/personal/protected/conflicting inputs, real AppStore planning after eight early answers, and exclusion of corrected/conflicted attempts without rewriting a previously committed plan or raw responses. Parsing and diff checks pass; these five tests await the subsequent coordinator run. The active raw mental-math timing gate, review assignment cap, and neutral familiar-review copy were reported as separate bounded candidates rather than silently folded into this change.

ADP-007/SCH-009 follow-up separates display timing from fluency evidence. New launches carry optional versioned `NFSessionTimingCondition`; nil retains the previous saved Boolean behavior. New explicit `isTimed` display requests resolve to elapsed-only, while reviewed timed fluency requires a compatible admitted family/band scope. `reviewedFluencyReadiness` is shared by AppStore planning and Train, reads only current effective observations whose reviewed demand matches the retained original snapshot, and never uses template IDs as observed strategies. Its pure reducer requires eight recent independent untimed full successes in one compatible family/band across two sessions, applies corrections, and blocks repeated unresolved scored criteria. The current metadata-only catalog supplies no such reviewed observations, so it cannot unlock fluent-performance claims. Reviewed catalog admission and durable observation ingestion remain necessary; this guard does not invent either.

The Train chooser separates Untimed, Elapsed only and Timed fluency from the optional Transfer mix focus. In-session Untimed/Elapsed changes are stored as a draft timing override while the original launch configuration/selection receipt remains unchanged. They preserve exact question, answer, scratchpad and confidence; elapsed display uses normal practice overhead and never sets `wasTimed` for fluency. Unknown condition versions/modes and nonfinite, negative or unsafe huge saved durations remain recovery-only, with original payloads retained. Integer conversion/clock formatting cannot reinterpret a corrupt `1e300` duration as a measured answer time. Daily review assignments now cap at five, while explicit review access preserves the unselected backlog; the description no longer promises a response-format shift.

Nine more source fixtures were added after the five reminder fixtures: three reviewed-timing/version policy cases, three real runtime timing/replay/duration cases, one review-backlog cap, one actual AppStore legacy-unlock rejection/zero-consumption/elapsed launch, and one catalog forwarding case. The new total is fourteen fixtures beyond the verified 784-test baseline, before other agents' additions. All edited Swift files parse and diff checks pass; integrated app execution is still pending. These changes do not close reviewed-band inventory, live adaptive-item control, explicit Replace/amendment UI, approved protected-form admission or human/empirical validation gates.

### Canonical plan policy upgrade after the reminder/timing closure

New daily plans now pin `NFDailyScheduler.policyVersion == 5`, identifying the five-item compatibility reminder cap and reviewed-only fluency input rules. A decodable prior-day policy snapshot is not reinterpreted merely because the current scheduler changed: same-day and active travel-window lookup preserve the complete stored plan. Explicit writable compatibility is limited to the inspected complete v4 block contract and v5. Other versions remain readable and unchanged, with plan-linked launch and replacement rejected before reservation; versions 1–3 are not guessed to be writable. Readiness can regenerate an unstarted current-v5 plan, but cannot delete or replace a v4/future frozen plan. A new learner day builds v5 normally.

Two additional regression tests cover pure scheduler policy preservation/new-day construction/unknown-version rejection and actual AppStore payload identity through unstarted readiness, a saved v4 checkpoint, travel, relaunch, unknown-version launch rejection, and the next day. Source parsing passed; these tests have not executed yet. The 784-test green baseline predates this change and the preceding 14 reminder/timing fixtures. The first two expanded build attempts stopped on compile errors before those fixtures ran; the coordinator owns the next integrated run.

### Effective mental-math detail adapter

The remaining Progress mental-math adapter now receives immutable effective DTOs through `AppStore.mentalMathMetricObservations(from:)`, with a batched map of retained snapshots. Corrected credits and conflict/content exclusions apply before reduction. Legacy accuracy remains practice history; template names, an activity's retention/transfer route, and answer-summary text cannot manufacture reviewed independence, delayed retention, unfamiliar transfer, strategy, unit success or clean speed. Composite estimation detail uses the exact saved estimate-then-exact contract and canonical numeric grammar only after item/prompt/template/seed association; missing, mismatched, protected or unsupported snapshots leave component detail unavailable without erasing valid historical practice accuracy.

Three additional tests in `ProgressEvidenceTests` cover actual AppStore correction/conflict projection with unchanged original answers, authenticated versus mismatched estimation snapshots, and invalid raw values/self-reports plus unreviewed lane labels. Two preexisting adapter fixtures now assert authenticated contract use or honest missing criteria. Swift parsing and diff checks passed; these changes postdate the expanded Mac input and await subsequent integration tests. The dashboard helper call and learner-facing practice wording are coordinated with the root owner.

Read-only follow-up: `NFAbilityEvidenceSnapshot` and its detail/transfer-gap consumers still used raw record scores and weights at audit time; the root owner is handling those separately. `NFSpeedEvidenceEngine` currently grants no legacy eligible durations, so its raw-record input only affects the unavailable-status label, not a numerical speed claim. Live reviewed observation ingestion, one-item bank reservation and persisted staircase/replacement adapters remain separate implementation obligations.

Adapter review follow-up adds the full history protection projection before any retained contract is supplied, covering legacy assessment source flags and marker-only protected conflict imports even when the exercise's own flag is false. Pure batch inputs now share the effective Progress eligibility predicate, accept an explicit decision date, deduplicate identical IDs, and omit a whole ID when any effective field, typed response, contract, protection state, or retained original-record payload conflicts. Nonfinite/future timestamps are excluded. The AppStore clock remains an explicit default injection boundary, and the future-dated projection fixture passes its fixed virtual clock. A fourth new adapter fixture tests duplicate ordering, response/contract/raw-prompt conflicts and future timestamps; the actual snapshot fixture additionally checks assessment-source and marker-only protection. Parsing and diff checks pass; integrated execution remains pending.


### Live ordinary one-item transaction and protected practice adapter follow-up

The default new mixed-practice delivery now uses the durable `adaptiveItem` transaction described in [Reservation_Implementation.md](Reservation_Implementation.md). It is integrated through actual AppStore launch/count acceptance and Universal Next. It accepts exactly one eligible bank position and frozen snapshot per revision-checked publication, preserving the fixed requested count, unconsumed eligibility holes, prior slot outcomes, separate presentation acknowledgment and legacy nil/fixed behavior. Ten new actual integration/adversarial fixtures were added to the existing focused suite (34 total); parse/diff checks pass, execution is pending the coordinator's next full run. This closes the one-item bank-delivery seam previously listed as open; reviewed demand selection, overrides/Replace, named mechanic adapters and protected exposure remain separate work.

The mental-math practice adapter now omits protected input flags and assessmentHoldout/nearTransfer classes entirely, including legacy baseline/reassessment source classification. The source-marker test now expects no practice observation, and raw protected classes have explicit omission assertions. This prevents a protected individual result being relabeled as descriptive practice accuracy. The effective-input/canonical-contract adapter tests remain awaiting the same integrated run; no new reviewed learning authority is inferred.


### Ordinary mixed Replace transaction — source added after the verified 852-test baseline

ADP-008/SCH-005 now have a live ordinary mixed Replace action with schema-2 operation/decision receipts, unchanged promised question count, original consumed-position retention and a new exact slot/attempt. An append-only `itemReplacedByUser` retired-draft record preserves the original response, confidence, scratchpad, assistance and known time, linked to its immutable snapshot. It is neither a learner-wrong result nor an academic observation. Actual presentation remains independently acknowledged. Atomic acceptance rechecks authentic materialization, current eligibility, full predecessor/digest and history/timing carry under CAS and the nonblocking lock; failed saves/finite shortage leave the original work intact. Exact retry returns the saved replacement, including later edits, and stale editors cannot reactivate old slots.

Schema-1 receipt paths remain writable under their known launch/next semantics; unknown operation or retired schema retains the run in read-only recovery. Portable imports retain readable retired drafts and exact snapshots without writable device authority. Deletion by any retired or current attempt traverses the whole run and removes private draft/snapshot payloads while retaining consumption/tombstones. Seven new focused integration tests (41 total) and one bridge test (nine total) cover these invariants, including explicit injected timing and no score/XP. Parsing/diff checks pass; the eight added fixtures await the coordinator’s next app run. The earlier complete **852-test pass** precedes these source changes.

Reviewed Easier/Harder next-demand selection, fixed-block remaining-work amendments, authentic reviewed observation ingestion and external content admission remain separate obligations. This closes the bounded ordinary Replace path without claiming those requirements or human validation are complete. See [Reservation_Implementation.md](Reservation_Implementation.md).
