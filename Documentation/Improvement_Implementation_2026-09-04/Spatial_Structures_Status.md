# Spatial structures 11/8 — candidate awaiting root execution

Scratch directory: `/tmp/nf-spatial-structures-20260905`
Patch: `spatial-structures.patch`. Exact frozen hash is recorded in `Verification.txt` after the final review.

No working-tree source or test edits, build, xcodegen, model-column change, or new project member was performed by this agent. The patch changes 14 existing files: 12 production Swift files, existing ContentImprovementRegressionTests.swift and three compact additive EN/JA catalog keys. Exact base file hashes are saved in base-hashes.json. The template/generator baseline remains4/2 for nil policy; the frozen10/7 retrieval package and root's later protected coordinator, generated terminal retention and Today preview changes are preserved by fresh base copies.

## What is implemented

`spatialStructurePolicyVersion: Int?` is an opt-in ordinary spatial recipe. New actual labeled-face and occupied-cell tasks use generator11/schema8 and `.r11` item identities. New ordinary launches opt in by default. Legacy accepted requests retain nil, their original exercise bytes, and their original scorer. Protected purposes, source-derived requests, future policy values and conflicting format/policy selectors reject before publication. Other spatial families retain their original recipe under the same ordinary request; the two affected old families cannot be substituted into a pinned new request. New sidecars without the matching request pin fail unavailable on ordinary restore.

The typed `NFSpatialStructureGeometry` and `NFSpatialStructureContract` govern source geometry, all candidate alternatives, exact scoring, semantic identity, instructions, feedback, accessibility and native/static figures. All new contracts bind the complete source and feedback; a tampered context, answer, geometry, assistance text, provenance or version is refused. Saved ordinary/generated questions and history retain the exact sidecar, typed choice and original outcome using existing snapshot envelopes. A failed/future/missing-pin recovery does not overwrite the original bytes. Existing ID and semantic quarantine exclusions still apply in the factory.

- QA-F12 / EX-C102: a genuine A–F labeled cube (A+x, B−x, C+y, D−y, E+z, F−z), explicit positive-axis viewpoint, and single or composed turns about fixed original axes. All six face choices have exact destination feedback. Proper signed permutations preserve handedness; equivalent composed transforms share semantic identity. The native figure has actual labeled panels and axes; the static figure, angle/direction cue and initial-normal table use the same saved contract. An ordinary post-answer copy can step and reset without changing camera, source or response. Independent mode never performs the requested object rotation.
- QA-F14: connected, grounded occupied unit cells replace arbitrary box dimensions. Seventeen free tetromino/pentomino footprints supply real 3D-source/top-view choices. Four symmetry-reduced ambiguous pairs of front/side silhouettes supply candidate-assembly tasks. Every matching proposal must be selected, with either two or three valid alternatives; no first-valid-only key. Native cells, static exposed surfaces, silhouettes and exact coordinate descriptions derive from the same cell lists. The top-view question keeps its object/camera orientation fixed before commitment so a Top command cannot supply its answer. Post-answer camera views open normally.

The local renderer explicitly distinguishes camera pose from replay state. Rendering and camera menus, camera Reset, and object Step/Reset put a 58-point minimum inside the actual native control label, with named accessibility IDs; installed target-size verification remains pending. Reduced Motion uses a fixed camera and discrete object steps. Initial and demonstration normals are labeled separately. Figures carry coordinate conventions, and descriptions remain explicitly described as geometry practice support rather than a validated equivalent visual assessment. Direct bilingual EN/JA contract strings follow the retained question locale; the three app catalog keys cover revised activity descriptions and unsupported saved-structure recovery.

## Exact capacity, without quota inflation

`census.py` is an independent Python finite enumerator (no app/user stores). Its executed results are in `Capacity.json`:

- 63 authored one-/two-axis paths collapse to23 nonidentity proper orientations; with the authored state, all24 cube orientations are represented. Six fixed query directions give138 canonical face-query contracts. This is one cube topology and two operation substructures, not138 independent object topologies.
- Five free tetrominoes and12 free pentominoes give17 canonical top footprints. Height changes, rotations and reflections do not create new footprint identities.
-81 grounded2×2 height assignments give35 labeled projection pairs. Ten ambiguous pairs collapse under plane symmetry tofour classes. Candidate sets retain all valid proposals. The independent census audits all ten encountered labeled pairs before symmetry filtering: each has at least three valid assemblies, and the constructor additionally checks the exact two/three-alternative layout before indexing.

The resulting159 query contracts carry zero reviewed bands or signed/admitted edition slots. They do not establish psychometric independence or quota feasibility. `Capacity.json` states the domain and remaining engineering separately.

## Eight XCTest methods authored, not yet executed

Existing `ContentImprovementRegressionTests.swift`, new `SpatialStructureContractTests`:

1. `testEveryFaceContractHasOnePhysicalKeyAndAllTwentyFourProperOrientations`: all138 actual producer contracts and every choice checked with an independent row-matrix oracle, determinant, bijective normals and noncommutativity.
2. `testWorkedEXC102UsesActualLabeledNativeFacesAndNeverReplaysBeforeCommit`: actual EN/JA EX-C102, real RealityKit label/axis entities, correct face A, guarded copy, native quaternion result, camera preservation/reset and original immutable bytes.
3. `testAllOccupiedFootprintsMatchIndependentProjectionAndNativeCellPositions`: all17 actual producer contracts, set-projection key, exact native cell positions, no height-only novelty.
4. `testTwoViewQuestionsAcceptEveryValidProposalAndRejectEveryIncompleteOrExtraSet`: all four question classes and all15 nonempty answer subsets, with an independent occupied-cell projection oracle; both2-/3-answer multiplicities.
5. `testMalformedUnsupportedAndProtectedGeometryFailsBeforeScoringOrRendering`: floating/disconnected/duplicate/oversized cells, invalid rotation inputs, protected/future/missing sidecars and altered context/feedback refuse; legacy4/2 unchanged.
6. `testActualLaunchColdChoiceQuarantineAndHistoryKeepTheExactNewStructure`: real catalog launch, checkpoint/writer release/cold choice, objective commit/history, request pin, ID/fingerprint exclusions and Next.
7. `testGeneratedRetainedAssemblyUsesSameTypedChoiceAndSavedHistory`: authentic authoritative question adapter, exact generated draft/cold choice/commit and retained history.
8. `testSemanticAliasesAndMissingPinnedPolicyCannotMintOrOverwriteAQuestion`: equivalent composed-turn and planar-symmetry identities, actual missing-policy cold recovery refusal, no original envelope rewrite, future request unavailable.

The memory fixtures explicitly retain ModelContainer lifetimes and disable shared-widget publishing. Root owns compilation, actual test execution and installed visual/assistive verification.

## Remaining substantive engineering

This slice does not close all58 families or all spatial demands. QA-F11 still needs non-origin/all-quadrant/mixed transforms. QA-F13 still needs actual noncentral/oblique/degenerate plane-solid intersection constraints. QA-F15 still needs varied adjacency/orientation/partial-fold and multi-constraint queries beyond existing11-net opposite-face work. QA-F16 still needs translated/diagonal reflections and compositions. QA-F12 still needs asymmetric object/occlusion tasks. QA-F14 still needs broader inverse-construction spaces and constraints beyond this explicit small proposal set.

Target-demand selection over the new substructures, new exact retention recipes for these new identities, and full requested-block capacity/CAS guarantees beyond the retained finite-probe fallback still require engineering. These are not recategorized as external approval gates. Independent bilingual/visual editorial review, reviewed demand bands, calibration/admission and signed release approval are separate release gates.

## Root execution addendum

The integrated producer initially failed all eight new tests because metadata and difficulty parameters used different viewpoint text. A one-line producer correction (supplemental SHA256 9fb72ed52df4a52268afdfcef68d6c9b5d493f0fa60f64857917752a0bda1fd1) preserves the strict validation rule. The rerun passes79/79, including all8 spatial methods, on source2e5438f284ec1d2ae16a6112429c6bb3aaa553a664f89fe1a1cd06decb575ef0. See Run_Manifest.json for original failure, exact logs and bundles. The earlier05fdef iOS build succeeded, but its three installed tests cover other flows. No installed spatial11 or editorial admission signoff is claimed.
