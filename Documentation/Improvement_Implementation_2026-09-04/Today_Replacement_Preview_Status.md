# Exact Today replacement preview

Executed: all seven new actual-store preview methods passed within11plan-history methods in170/170 scope on2a891389. A fresh installed preview/cancel/cold/apply/acknowledgement test is now authored with explicit44pt native reason/preview/apply controls; its execution remains pending.

Integrated root slice for UX-010. The five current reasons are Too easy, Too hard, Already familiar, Want variety and Accessibility issue. Earlier raw reasons and saved labels remain readable. Preview uses the current persisted scheduler snapshot and shows its original/replacement title, exact minutes, proposed evidence class and effects. It does not consume the replacement or write a plan/travel update. Cancel drops only the value.

Apply compares the original persisted payload and independently recomputes the complete proposed plan. Started/accepted local sessions, active launch, actual attempts and legacy checkpoints block replacement. One captured date controls read-only learner-day lookup. Changed bytes/plan/reason fail before mutation. The existing one-replacement limit, historical audit/undo and unchanged other sections remain.

Seven authentic store tests are authored: read-only preview/cold cancel; exact apply/other blocks/one audit; stale bytes; legacy started; actual accepted local run without legacy mirror; captured future day without materialization; current reasons/legacy raw compatibility. Source parses; focused Xcode execution pending. Independent Experience review identified captured-date, local-run and stale-view-label gaps, all corrected before integration. This does not claim reviewed harder/easier content from merely choosing a reason; preview explains its scope. Existing two-save plan/audit persistence is unchanged.
