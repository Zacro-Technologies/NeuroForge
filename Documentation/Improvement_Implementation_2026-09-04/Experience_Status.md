# Experience implementation ledger — 4 September 2026

> **v1.1 direction update:** this ledger retains prior implementation evidence. [AI-centered requirements](../Improvement_Spec_2026-09-04/README.md) supersede earlier AI/privacy restrictions; [changed contracts](AI_Direction_Revision_Status.md) are specified pending implementation reassessment. No new AI capability is established by this documentation change.

This ledger covers every normative requirement in chapter 01. “Implemented” means code is present; it does not certify device, accessibility, language, or usability acceptance. Root owns integrated builds and records final executed results. No manual or distribution evidence is fabricated here.

Shared changes: five stable root hosts; typed Codable per-destination paths in `Sources/App/NFNavigationState.swift`; user reselection clears only that path (no navigation `.id` replacement); exact item-label history rendering; source/generated self-report authority; device-local generated drafts with owner validation, pinned exact content, durable prepared attempt, explicit resume and writer takeover; sample-first entry; Today continuation hierarchy; exact activity search; separated task difficulty/timing; searchable settings; saved-set retention controls. Existing user changes in release documentation and project configuration were preserved.

| Requirement | Current status | Implementation and evidence; remaining acceptance |
|---|---|---|
| UX-001 | implemented | Today/Practice/Progress/Sources/Settings labels; explicit Progress Overview/Review/History. Root destination enum renamed Today. Typed root routes and source context returns retained. |
| UX-002 | in progress | Today rewards moved into disclosure; resume leads; activity Start footer; source creation secondary; history versions technical disclosure. Remaining grayscale and complete action-hierarchy inspection. |
| UX-003 | in progress | Root ordinary/protected policies consumed by session; generated confidence optional and self-check explicit. Reviewed-band delivery depends on content/adaptive integration, not UI relabeling. |
| UX-004 | implemented | Per-destination typed navigation paths with safe reference resolution, retained roots, cold empty paths, reselect reset, route-prune function. Generated draft owner check is read-only on foreign device and writer arbitration supports checkpointed takeover. Installed iPhone SE repeated history/detail→source document→Settings→history return passed; reported Mac hang still requires Mac UI replay. |
| UX-005 | in progress; scoped native verified | Ordinary/source/generated exact acknowledgement, pending receipt recovery, explicit discard and export pass the recorded runtime regressions. Shared per-window registration, explicit teardown, Quit and scoped Command-W are integrated. Actual copied-app native Cmd-Q/Cmd-W, blocked-save retention/retry, exact cold draft and scratchpad child-sheet exclusion passed; see Native_Mac_Quit_Verified_Manifest.json and Native_Mac_Close_Verified_Manifest.json. Complete multi-window/child-sheet/recovery-action coverage remains separate. |
| UX-006 | implemented | Fresh welcome primary Try a sample; untimed authored math strategy sample with feedback and no ability evidence; secondary setup and Restore a backup. Sample-completion Try another skill switches to an authored data-interpretation sample, preserving the same untimed answer/feedback loop. Installed sample choice, visible Correct explanation and completion-to-setup passed in the finalized 23:10:37 bundle. |
| UX-007 | in progress | Three-stage setup retained with explicit Start practice / Take a short skill check and next-session explanation. Root sets five-minute untimed defaults and starts ordinary practice after setup. The original maximum-size test supplied an unrecognized raw value and was invalidated. The corrected UIKit accessibilityExtraExtraExtraLarge.rawValue test passed with its active-layout assertion; the real enlarged screenshot was inspected. Full first-session and device matrix still require verification. |
| UX-008 | in progress | Existing optional block-based starting check retained under Today and onboarding. Root exact protected resume. Requires final scope/timing copy and device journey evidence. |
| UX-009 | implemented | Date, exact saved work or daily task, due-review route, daily sections, optional activities; rewards and extra panels disclosed. |
| UX-010 | in progress | Energy choice stages a pure preview from the displayed immutable plan, with fixed work and honest pacing consequences. Apply alone saves. The installed Cancel/cold-relaunch journey passed in subsequent23 runs; the latest SE capture is reviewed. The full Adjust/replacement pre-apply preview remains a separate obligation. |
| UX-011 | in progress | Today continuity, due/empty reviews, stable existing completion retained. Runtime loading/save/no-content states root-owned; device failure evidence pending. |
| UX-012 | implemented | Activity text/translated title/summary/keywords match exact selectable result; direct route to preselected activity; root path preserves search and browsing state. Catalog supports complete current families. Favorite control on exact activity details and Recent/Favorite sections use local metadata; recents are recorded only after an accepted launch. |
| UX-013 | implemented; broader QA pending | Typed Untimed / Elapsed only / Timed fluency are separate from practice/Transfer focus. Reviewed-family readiness gates speed pressure, global Untimed remains respected, and matched request policy is persisted. Integrated unit coverage exists; the full timing setup/device matrix remains unexecuted. |
| UX-014 | implemented; broader QA pending | Searchable 20-row source chooser, selected-only review, stable UUID selection and source-detail recovery for exclusions replace the inline library. Missing/incompatible selections block silent partial creation. Task/lab/field stay visible; detailed settings are under More options; explicit sharing consent remains. The installed search/clear/reopen/remove journey passed in the verified 21/22 run, and its actual SE screenshot was reviewed. Full locale/device/editorial validation remains separate. |
| UX-015 | in progress | Save set uses local durable repository; saved result recovery precedes temporary cache; accepted generated run pins exact result/request; expired in-memory new starts disabled without saved or unfinished run. Sources has Saved and collections, optional named/described collections, source/set membership, explicit membership-only deletion, supported JSON export and learning links. Remove saved copy describes temporary expiry, unfinished runs and retained history; root separately fixes full reusable-inventory deletion. Local metadata/export roundtrip and unsupported-schema no-overwrite tests pass. Root explicitly rejects conflicting imported metadata so original import bytes remain recoverable; automatic conflict merging is not claimed. Clock-boundary integration tests pending. |
| UX-016 | in progress; installed ordinary/source paths verified | Ordinary/source/generated item/comparison/feedback share one prompt/response subtree, retaining locked submitted controls. Sticky Submit/Next, direct44pt Save & close and safe Escape are integrated. Actual installed ordinary shape/source paths verify locked inputs and exact continuation. Generated-specific/full AX and width acceptance remain. One latest ordered-feedback screenshot has an empty-looking footer label although Next later worked; fresh visual verification is still required. |
| UX-017 | implemented | Root inline confidence policy; generated optional inline selection with nil default; source recall confidence optional inline. Legacy mandatory generated confidence page and response-edit entry point removed. |
| UX-018 | implemented | Typed saved answer rendering; generated self-check neutral match label without numeric credit; history protected item neutral result, no key or scoring color; actual snapshot explanation used. Root session handles immediate teaching correction and protected acknowledgement. |
| UX-019 | in progress | Root hint ladder and repair IDs integrated; generated coaching remains one authored cue. Full hint/repair validity and support telemetry verification requires root/content results. |
| UX-020 | in progress | Root optional reflection after correction, no preselected learner diagnosis; old source/generated mandatory confidence removed. Required-reflection save/failure device evidence still required. |
| UX-021 | in progress | Root timing/exit/checkpoint lifecycle; generated monotonic active segment excludes reference/feedback/pause and resumes explicitly; summary after attempt write. Exact generated draft/owner test passes; installed English/Japanese ordinary Q3 draft and saved-feedback cold relaunch pass. Same-device concurrent-window UI evidence still required. |
| UX-022 | in progress; implementation integrated | Bounded scratchpad recovery retains/export originals and oversized unsaved drafts. Exact independent question context includes permitted table/diagram previews, >=900 split and compact/AX pinned expansion. Native notes/drawing Undo/Redo and stable editor identity are integrated. Policy/native tests exist; actual native scratchpad child-sheet preservation passed. Full compact/wide/AX/physical Pencil acceptance remains. |
| UX-023 | in progress; chart capture verified | Effective-authority charts expose date/activity/count/value, point inspection, accessible50-row data pages and exact matching-history routes. Latest screenshot shows Aug31/Mental Math/0%/1scoredanswer; final AX confirms row navigation reached its original-answer history. A transition-time cached scroll query failed before detail completion and now waits for the destination. Full installed chart-to-detail acceptance remains pending. |
| UX-024 | in progress; implementation integrated | Local deferral pins exact memory/anchor/return instant, filters only eligible reminders/new starts, preserves accepted runs and separates bookmarks from due practice. Queues are paginated; lifecycle/review tests are integrated. Installed defer/expiry/review-return acceptance remains separate. |
| UX-025 | implemented; broader QA pending | All eight response schemas render against exact snapshots; protected keys/results remain withheld. Original logic-state inputs/rules/transitions, actual captured hints and committed citation actions now exist. Changed/deleted/unverifiable sources and malformed typed payloads stay explicit recovery; notes/bookmarks/report/repair preserve originals. Full installed history/source-policy matrix remains pending. |
| UX-026 | implemented | Existing search/import phases/cancel/duplicate/retry/empty recovery preserved; new Sources path isolated from Settings. Needs actual source import journey after refactor. |
| UX-027 | implemented | Source learning actions lead metadata; Read source / Review from memory / Create question set; optional confidence before reference; Matched/Partly/Not yet stored as typed self-report with exact recall and stable attempt ID. Original privacy and separate sync controls preserved. |
| UX-028 | implemented | Searchable six categories with English/Japanese synonyms; category matches reveal controls; export deep route opens Data and sync; grouped underlying editors and destructive flows preserved. No-match explanation, Clear search, and matched-category highlighting implemented; installed focus→filter→Done→root return with query→no-match→Done→Clear search passed. |
| UX-029 | in progress | Work followed correctness/navigation → answer loop → discovery → durable resources → interactions. Scope does not remove existing useful capabilities. |
| UX-030 | in progress | This trace ledger and `Tests/ExperienceImprovementTests.swift` added. Root owns app/test logs. Manual widths/locales/VoiceOver/keyboard and independent usability sessions are still unexecuted; no certification claimed. |
| INT-001 | in progress; bounded workflow integrated | Fresh math sidecars save the estimate before exact/judgment, capture optional calculation working and first-divergence feedback, record durable compensation support and preserve family repair. Eight MentalMathWorkflowTests passed in Restore_Data_Trace_Math_Typed_Run.log. Installed staged-math/working layout and broader math families remain unverified. |
| INT-002 | in progress | Native 3D/static 2D preserved; Reset/authored/front/side/top camera controls added, no scored-state mutation, no automatic motion. Fold/unfold/object-transform practice and actual nonvisual equivalence remain content/UI scope. |
| INT-003 | in progress; bounded workflow integrated | Authentic observed-proportion counts share chart/table/inspection and verified visible numeric values. Latest installed quantitative journey passed84.370s. A subsequent typed data sidecar durably captures prediction before denominator support, saves inspection state and records assistance; six focused tests passed. New overlay/prediction installed acceptance and broader construction families remain. |
| INT-004 | in progress; integration package ready | Same-study two-stage evidence mapping then experimental-design choice is authored in /tmp/nf-linked-science-20260905. It freezes the first mapping durably without scoring, uses scoped final validation, readable controls/competing predictions, changed-context same-family repair and protected-first history. Optional policy pins preserve legacy recipes. Nine authentic tests plus46new EN/JA keys are ready; full app compilation/execution and installed linked-stage acceptance are pending. |
| INT-005 | in progress; bounded workflow integrated | Ordinary/generated exact typed trace renderer now uses the retained restricted AST/program, captures prediction before durable reference support, preserves step/reset/undo predictions and recorded assistance, and replays original history. Ten CodeTraceInteractionTests passed. New500-character bounds/recovery and first-save/mirror-failure guards are integrated pending their combined run. Native/installed trace interaction and all code skins still require acceptance. |
| INT-006 | in progress | Exact private source recall/citation/reference and neutral save use the common coordinator; cold draft/feedback journey passes. Live committed citation context and explicit insufficient-excerpt reporting are integrated, preserving original source pins and policy. Complete source-retention interactions and installed reporting/citation matrix remain. |
| INT-007 | in progress | Existing deterministic transfer content and root optional post-feedback reflection retained; no generic transfer mandatory-reflection trigger in new ordinary loop. Coherent multi-skill slice/content review depends on content engine. |
| INT-008 | in progress; shared editor integrated | Ordinary/generated ordering uses shared stable-ID/permutation controller, preview-only private typed drag, explicit/native Undo/Redo,44pt moves, focus-local arrows and position announcements. Installed ordered Undo journey and actual native arrow/Undo pass. Native destination/controller regressions pass, but actual grip drag entered the destination and required Escape without a drop. The conventional mouseDragged-start lifecycle patch is ready; actual drag/VoiceOver acceptance remains open. |
| A11Y-001 | in progress | Width-adaptive existing grids retained; primary footers safe-area inset; Progress overview stacks at compact/accessibility size; source and claim lists reflow. Full specified width/window matrix not executed. |
| A11Y-002 | in progress | Existing semantic contrast-tested palette retained and used by new controls; new cards use 16/20 radii and 44pt controls. Whole-app token consolidation and measured rendered contrast remain. |
| A11Y-003 | in progress | Safe-area footers, responsive Progress overview, source/claim text wrap, named Editors preserved. True UIKit AX5 onboarding passed and its enlarged screenshot was inspected; full largest-size coverage outside onboarding remains required. |
| A11Y-004 | in progress | Ordinary/generated/shared-source UI now focuses actual prompt/result/reference headings after acknowledged transitions and visible clarification after semantic errors. This does not move keyboard focus during draft editing. Existing commands remain. Parsing passes; full hardware-keyboard journeys and integrated accessibility-focus verification remain. |
| A11Y-005 | in progress | Explicit ordinary short-text/recall/reflection labels and generated recall names added; acknowledged heading focus announces only visible policy-safe content. Typed history hides internal IDs. Actual VoiceOver operation, table/selection/reorder announcements and complete first-session acceptance remain unverified. |
| A11Y-006 | in progress | No automatic model motion; camera controls discrete under Reduce Motion; sample/recovery understandable without sound/haptics. Whole transition audit still required. |
| COPY-001 | in progress | Primary vocabulary Today/Practice/Sources, source actions, Question Writer, local saved labels. Existing deeper legacy terminology being normalized; methodology may retain precise technical terms. |
| COPY-002 | implemented | Main check/continue/save/pause/recovery/self-check/expiry strings introduced consistently; local reports preserve existing actual quarantine copy. |
| COPY-003 | in progress | English/Japanese catalog additions now cover routes, collections, sample alternatives, report recovery, sitting budgets and runtime additions. Compiler extraction performed and catalog content lint passed in the 23:18:36 regression run. The ordinary Japanese Q3 journey and visible copy were inspected; manual Japanese editorial review and the full locale matrix remain external acceptance work. |

## Automated evidence added

`Tests/ExperienceImprovementTests.swift` verifies exact numeric/choice/multiple/order/prose/self-check/claim/logic history text, missing-label no-ID fallback, independent path state/reset/prune/Codable, scratchpad opaque drawing preservation, and generated exact draft restoration plus foreign-owner no-write. Existing generated-runtime tests updated by the content owner for the new optional inline confidence contract. Swift frontend parse was run across owned feature and navigation files; root runs the integrated platform compiler and XCTest suite.

## Material limits requiring real evidence

A build cannot establish VoiceOver usability, narrow-window layout, reduced-motion comfort, Japanese educational tone, beta-macOS responsiveness, signed integrations, or a human pilot. Those gates remain explicit. No empirical or device verification is marked complete here without a recorded run.

## Executed evidence update

The integrated macOS run on 4 September 2026 logged all original five `ExperienceImprovementTests` passing at 21:50:39 (full-suite log `/tmp/neuroforge-improvement-tests.log`, lines 1458–1469). This confirms typed display, navigation value state, scratchpad byte preservation and exact generated draft owner recovery; it does not certify the full release. Three more tests now cover local-resource roundtrip and history separation, future-schema overwrite refusal, and quarantined saved-set commit refusal with pin release. All eight tests passed in the subsequent actual macOS run at 22:18:16.638 (`/tmp/neuroforge-improvement-final-tests.log`, lines 1262–1279).

`NavigationSmokeUITests` now also contains English/Japanese installed-app Q3 draft and feedback cold-relaunch journeys using an isolated persistent DEBUG fixture, plus a real saved-answer detail→Sources→Settings→Progress repeated-return journey. These are executable UI tests, not source-text assertions; the first iOS run and its failures are recorded below. Stable session identifiers are present for prompt, question-position accessibility value, numeric answer, submit, next, resume and Save & close. The nested journey also opens a synthetic source document and verifies its detail survives Settings/Progress reentry. No screenshot or accessibility pass is asserted before the run.

The first installed iOS run completed nine real journeys on iPhone SE (3rd generation), iOS 26.5. Question Writer declined-handoff recovery, quick mental-math launch, and maximum-accessibility-size onboarding passed. Six journeys failed; the run is not certified. Failure artifacts are in `/tmp/NeuroForge-improvement-ios/Logs/Test/Test-NeuroForgeUISmoke-2026.09.04_22-27-13--0400.xcresult`, exported to `/tmp/nf-ui-live-attachments`. Movie frames and synthesized touch records showed a sample choice tap without selection and a Settings field tap without focus; the controls now define their full hit surfaces and the search field has an explicit focus binding. Root fixed session identifier inheritance and made Submit reachable above the software keyboard. The repeated-navigation failure remains subject to the rerun; its Settings tap used the correct visible coordinates. UI assertions retain exact prompt, position, typed value, saved phase and separate navigation-path checks. Targeted rerun is required before any pass claim.

The subsequent full macOS run passed all 654 tests, including all nine experience tests and the protected-receipt/self-report neutral-filter regression (`/tmp/neuroforge-verified-unit-tests.log`, experience lines 691–710, full-suite summary lines 1628–1638). The ninth test validates that imported legacy-class protected receipts withhold answer keys and cannot reveal a result through correctness filters, while the immutable original response remains intact. This unit result predates the compact hit-target fixes above; root is recompiling those separately.

Representative unedited installed-app screenshots from the first run are retained in `Experience_Artifacts/`: Practice and Progress roots, compact onboarding with the maximum-accessibility launch setting, and the Sources width failure before its responsive fix. These captures establish only the depicted state and viewport.

The six-journey installed retry passed four journeys: all five roots, repeated nested history/source/Settings paths, English Q3 exact draft+feedback cold relaunch, and Japanese Q3 exact draft+feedback cold relaunch. Two failures remain: the sample opener tap did not present the sheet, and Settings attempted a root switch while the keyboard covered the tab bar. Search focus and filtering themselves passed. Settings now has Search-key submission, interactive dismissal and an explicit Done toolbar; the test uses Done before switching roots. The isolated two-journey rerun is in progress. Retry bundle: `/tmp/NeuroForge-improvement-ios/Logs/Test/Test-NeuroForgeUISmoke-2026.09.04_22-44-44--0400.xcresult`.

Root’s explicit solution-reveal persistence now renders as “Solution viewed — no score” with its original typed draft, distinct from Skipped and excluded from objective outcome filters/credit presentation. A tenth experience regression covers this boundary and awaits the current integrated run.

An audit of the onboarding screenshot caught that the old maximum-size test used a spelled-out category name instead of UIKit’s raw accessibility-size constant. Its prior pass is not accepted as AX5 evidence. The test now obtains `UIContentSizeCategory.accessibilityExtraExtraExtraLarge.rawValue` from UIKit and asserts the larger-control branch is active before checking containment. A new actual AX5 run is required.

The later installed usability run passed the complete Settings search→Done→Today→return-with-query→no-match→Done→Clear search journey (32.274 s) and primary navigation (32.413 s). Its Sources screenshot confirms the narrow hero now stacks and its pills retain whole words; `Experience_Artifacts/SE-sources-responsive.png` is the unedited capture. Sample choice/Check answer now work, but the Make this fit me tap left the sheet presented. The footer now uses the same explicit plain full-width hit surface as the verified choice control, with dismissal-to-setup deferred until onDismiss. Sample feedback also now scrolls into view; the UI assertion requires the Correct heading to be hittable before capture. These final sample changes require rerun.

The current full macOS closure run passed all ten experience tests, including explicit solution reveal versus skip (`/tmp/neuroforge-closure-unit-tests.log`, lines 1449–1468, 23:07:20.475). Its full685-test suite retained one unrelated session-flow assertion failure under root investigation, so this is an experience-test pass, not a full-suite certification.

The corrected UIKit AX5 onboarding test passed in the installed console run (16.355 s) with its active accessibility-layout assertion. Xcode stalled finalizing that result bundle and was interrupted, leaving its screenshot unavailable; the final full UI rerun must replace that artifact. Sample’s50ms XCTest taps were intermittent at different controls despite explicit hit surfaces. The pending final test uses one150ms touch per sample control, with the same strict selection, visible teaching feedback and setup-path assertions and no automatic retries.


The finalized installed run at 23:10:37 passed eight of nine journeys: sample selection→visible teaching feedback→setup (15.545 s), all five roots, quick practice, Question Writer recovery, repeated nested history/source/Settings paths, true UIKit AX5 onboarding (18.483 s), Japanese Q3 exact draft+feedback cold relaunch (82 s), and complete Settings search/no-match recovery. The English Q3 run encountered an actual external app launch before its first input. Its immutable failure hierarchy shows a correctly paused session with disabled background response controls and an enabled Resume; its movie shows another app in the foreground with a return link to NeuroForge. The test now uses the existing visible Resume path after initial presentation as well as after cold relaunch, records an interruption screenshot, and retains all exact response/prompt/position/phase assertions. This is a test recovery fix, not a bypass of the session state. The parent is rerunning EN/JA against the final binary. Bundle: `/tmp/NeuroForge-improvement-ios/Logs/Test/Test-NeuroForgeUISmoke-2026.09.04_23-10-37--0400.xcresult`; exported attachments: `/tmp/nf-ui-final-attachments`.

Current unedited screenshots have been visually inspected and retained as `Experience_Artifacts/SE-sample-visible-feedback.png`, `SE-onboarding-verified-AX5.png`, and `SE-Q3-exact-feedback-cold-relaunch-ja.png`. The sample capture includes Correct, the complete worked explanation and the setup action. The actual AX5 capture shows the enlarged multiline primary/footer controls, with its UIKit category and size assertion confirmed in the passing test. The Japanese Q3 capture shows the authentic answer 17, question 3/5 and the persistent Next action above the viewport edge. Its previous capture is preserved with the `-before-sticky-next` suffix. The external interruption frame is retained separately as `SE-external-interruption-paused-first-input.png`. English's current screenshot still awaits its rerun; the earlier passing artifact has not been relabeled as current.

All eleven experience tests passed in the final regression run at 23:16:30.830 (`/tmp/neuroforge-final-regression-tests.log`). The added calibration fixture proves a historically misgraded self-check ceases contributing to the derived calibration summary after authority reconciliation while its original confidence, score and response remain unchanged. Progress's store-backed improvement, error-pattern, evidence-count and calibration claims now consume effective attempt authority. Standalone record-only legacy evidence/speed/insight APIs remain a documented boundary; this change does not certify every historical visualization or rewrite raw history.


The fresh isolated simulator run finalized at 23:29:21 passed Japanese Q3 exact draft and saved-feedback continuation (104.634 s). English reached the real first response field but a standard center tap did not acquire keyboard focus. The synthesized touch was inside the field at 187.5,486.0, and its AX frame was only 35pt high; the movie showed an active question without a keyboard or interruption overlay. This was a distinct failure from the earlier external app interruption. Numeric value/unit fields now expose a 44pt rectangular hit surface and explicitly focus the existing field binding through a simultaneous tap gesture. The UI test keeps the standard tap and exact typed-value assertions, adds an actual 44pt accessibility-frame check, and does not retry touches or seed app state. The final full installed rerun must verify the fix. Bundle: `/tmp/NeuroForge-improvement-ios/Logs/Test/Test-NeuroForgeUISmoke-2026.09.04_23-29-21--0400.xcresult`; failure frame: `Experience_Artifacts/SE-numeric-input-focus-failure-before-44pt-fix.png`. The current Japanese Q3 artifact is the fresh light-mode capture; its earlier dark-mode capture is retained with the `-dark` suffix.

The content owner's final immutable-history correction presentation adds 11 English/Japanese strings for original result/credit, correction reason, excluded accuracy, disputed original key/explanation, and assistance/timing authority. A separate localized content-language notice explains when a saved question retains its original language while controls follow the current preference. These catalog additions preserve existing entry formatting and passed JSON/whitespace validation; root owns their final compiler and test verification.


The installed suite now contains 19 native journeys: the previous nine plus ordinary seeded spatial, quantitative, science, logic, retrieval and transfer responses, ordered proof steps, claim/evidence mappings, typed logic state, and strict short-text clarification. Together with numeric mental math, these target seven labs and eight response schemas. The DEBUG launch allowlist accepts only ordinary catalog practice families in disposable fixture stores; it supplies no answer-key UI hook and never seeds responses or outcomes. File-backed fixtures isolate documents, rotation, presentation cache, local sessions and temporary exports; widget publication is disabled. A stable test profile makes focused rotation deterministic. Unit-test hosts use the isolated DEBUG startup without changing production onboarding/shortcut defaults.

All twelve experience tests passed at 00:07:48.535 on 5 September in `/tmp/neuroforge-atomic-golden-unit-tests.log`. The new pinned short-text fixture verifies the actual resolved launch seed, reviewed-prose authority and a specific unsupported response that must remain ungraded. Runtime checks establish editable item phase, no saved attempt/result/error alert state, retained guidance and exact cold restoration of the original response and clarification.

The expanded installed run completed 19 journeys with eight passes and eleven failures at 00:21:42 (`/tmp/neuroforge-expanded-installed-ui.log`). Actual passes include number-plus-unit response submission/feedback/advance and typed logic fields plus invariant selection, alongside sample, primary navigation, Settings, true AX5 onboarding, quick math launch and declined handoff recovery. The newly exercised controls and resumed-session paths are not certified: failure recordings and accessibility trees are being reviewed before fixes. One journey per lab/schema does not cover all 58 families, modes, protected assessments, physical-device input or manual accessibility acceptance.


The expanded run finalized at 00:31:43 after Xcode spent 600 seconds collecting simulator diagnostics; that delay was after all tests ended. Attachments are exported to `/tmp/nf-ui-expanded-attachments`. Actual movies and full app hierarchy logs confirm that the choice grid was lazy/offscreen, and the old helper incorrectly treated controls under the persistent footer as tappable. The ordered-step touch submitted the response by hitting the overlaid Submit action; the revised helper requires the entire intended response target above the measured footer before a single standard tap and scrolls absent lazy options into view. It retains exact selection/order/feedback assertions. Real AX target checks also found a34.5pt self-check rating and42.5pt claim evidence control; these now have explicit minimum-size rectangular hit surfaces. The broad short-text no-alert query was incorrect because SwiftUI exposes the intentionally modal session itself as AX Alert even before submission. The check now excludes only the identified session container, still prohibits a Save interrupted alert, and retains the exact clarification/noNext/draft/position assertions.

Three additional movies show standard50ms native touch down/up events at the visible centers of Resume, Next and an answer-history card without the corresponding transition. The session stayed paused in English; Japanese stayed on the original saved Q1 feedback; History stayed on its original section. Their retained frames are `SE-expanded-EN-resume-short-touch-failure.png`, `SE-expanded-JA-next-short-touch-failure.png` and `SE-expanded-history-short-touch-failure.png`. These are failure evidence, not cold-continuity passes. The already-proven sample uses one150ms touch; any corresponding bounded native-gesture adjustment requires another actual run and must retain every exact transition assertion.


The bounded gesture adjustment is now applied only to explicit paused Resume, feedback Next and the saved-history link: each uses one150ms press with the same exact state assertions and no retry. All other ordinary response touches remain standard. The observed50ms behavior is limited evidence from this simulator/host; its underlying cause is not claimed to be a confirmed framework defect, and physical-device/stable-OS acceptance remains open. The next full19 installed run is required.


The next full installed run (`Test-NeuroForgeUISmoke-2026.09.05_00-39-37--0400.xcresult`, `/tmp/neuroforge-reviewed-installed-ui.log`) completed19 journeys with14 passes and5 failures. English Q3 cold draft+feedback continuation passed68.002s, Japanese passed75.857s, repeated nested paths passed58.365s, and all sample/root/Settings/AX5 existing journeys passed. New typed logic, quantitative value+unit, retrieval self-check, short-text clarification and spatial choice journeys passed; the feedback-heading assertion now ensures the actual result is visible before advancing. The current EN/JA Q3 images are replaced with these visually reviewed captures showing the authentic answer17, question3/5 and persistent Next; the old English image is retained with `-before-sticky-next`. Four new retained images show visible typed-state, number+unit, neutral self-check and spatial feedback.

The five failures are bounded: the new claim accessibility wrapper lost its combined claim/evidence label under the drag modifier (now fixed by applying the full semantic Button wrapper after drag), and four static semantic families exhausted their genuine fresh pool after the first response. Logic conditions, ordered proof, scientific confound and transfer conditions all showed correct original feedback then incorrectly published2/5 with an unavailable/save alert. Content/runtime owners are correcting Next to retain1/5 and the immutable feedback, expose a specific shortage reason and avoid a save-error claim. UI now displays the shortage, disables repeated Next and offers explicit End session. Those four fixtures pin that finite-capacity expectation and require actual Ended after1of5 summary; other fixtures continue to require an editable second question. Unit capacity probes and installed verification are pending.

Visual inspection also caught that the short-text test’s original capture left its correct inline guidance below the software keyboard. The UI now releases response focus when clarification arrives, and the strengthened test requires the actual guidance to be visible above the action footer before capture and explicit Skip. Original draft/position/clarification-authority assertions remain. `SE-clarification-before-visible-guidance-fix.png` retains that pre-fix visibility gap rather than presenting it as a completed layout.


## Exact source-review adapter — 5 September

The old independent source reveal/save flow was removed. `NFSourceReviewExactAdapter` pins the original excerpt, citation digest, source document/chunk IDs, locale, stable session/slot/attempt IDs, and typed recall in a local `NFLocalItemCheckpoint`. `SourceReviewView` now embeds the common `UniversalSessionView`; its shared coordinator owns reveal, lock, optional confidence, prepared attempt, neutral self-check, pause, retry, next, and writer arbitration. A missing selected chunk cannot silently select a different excerpt. A resumed run does not read the current chunk to construct its reference. Response-only legacy source drafts remain read-only with an explanation that the original excerpt was never stored; starting a new review uses a new identity and leaves the old draft intact. Foreign-owner and corrupt exact snapshots remain recovery-only.

Source rotation and exact saved-review route resolution accept typed `selfCheck` records only with document-practice provenance, while preserving legacy `sourceSelfCheck` recognition. Deletion continues to use the exact exercise's original document/chunk references. Source drafts bypass the SwiftData checkpoint path; the parent persistence boundary also withholds the private reference from the legacy answer-key column. Three new `ExperienceImprovementTests` exercise exact replay after changed source content, idempotent neutral save/zero evidence, local source deletion, failed durable reference reveal/retry, and foreign/corrupt recovery. They are authored and parsing clean; integrated execution is pending.

The installed suite now has 20 journeys. The added source journey uses the existing disposable source fixture, extracted through `NFSourceExtractor`, then enters a visible recall, saves and closes to its document, cold-relaunches through Today Continue, checks the same prompt/count/answer with the source still hidden, reveals the exact synthetic excerpt, saves a neutral rating, and cold-relaunches the same feedback. No production source/answer hooks or hidden evaluator keys are exposed. Execution and screenshots remain pending; earlier 14/19 passing evidence remains separately captioned. This still does not certify all 58 families, protected modes, every device, or manual accessibility acceptance.

Generated question UI now renders saved semantic clarification separately from structural validation, binds optional confidence through the response-aware runtime setter, invalidates stale confidence after semantic edits, and checkpoints coaching disclosure through its shared runtime action. New recovery/source text has English and Japanese catalog values.


The first integrated source-recovery build attempt stopped before tests on both macOS and iOS with a Swift IR-generation crash at the generated confidence binding's actor-isolated bound method. The setter is now an explicit closure with identical runtime behavior; parsing and whitespace checks pass. These stopped builds provide no unit/UI acceptance. Source replay, visible generated clarification/coaching, and the 20-journey installed rerun remain pending until the rebuilt binaries execute.


The shared item UI also distinguishes a durably saved skip or solution-viewed record whose advance is waiting for storage. A persistent footer states the saved activity accurately, offers Retry advance and End session, and keeps response/confidence/hint controls disabled. The pending marker is owned by the common runtime; these buttons reconcile the existing receipt rather than creating another answer. English/Japanese recovery copy is present. Unit/storage-failure and installed verification are pending with the rebuilt suites.


The rebuilt coordinated macOS run passed all 18 `ExperienceImprovementTests` at 01:38:28.696 on 5 September (`/tmp/neuroforge-coordinated-recovery-unit-tests.log`, lines 1470–1510). This includes all three exact-source tests, generated confidence tied to the semantic response, durable coaching disclosure, malformed typed-history recovery, and the pinned short-text authority. The unit run contains separate fixed-plan/family-capacity assertion failures under parent/content triage; the experience pass does not certify the whole suite. The 20 installed journeys have started and remain pending.


Read-only inspection found that the coordinated installed run was executing a stale test runner despite compiling the current tests. The built `NeuroForgeUITests` binary contains the new source journey and `EndsAtFiniteCapacity` selectors, while the running log invokes old `ShowsFeedbackAndAdvances` selectors and reports the old selection-helper line 558 (current line 665). At 01:38:34.516 its session log loads `NeuroForgeUITests.xctest` from the simulator's `com.apple.containermanagerd/Dead/...` directory. Parent triage must correct that deployed-runner mismatch before the run can certify current UI behavior; its early claim/logic failures are not treated as proof of regressions in the current helper. No simulator action was taken by the experience agent.

## Requirement re-audit — 5 September

[Experience_Gap_Audit_2026-09-05.md](Experience_Gap_Audit_2026-09-05.md) records concrete unfinished product work separately from human/device/release acceptance. Several earlier “implemented” rows described only a component; their current status now reflects the remaining obligation. In particular, a code-trace sample, unused data-inspection component, claim mapper or static citation does not establish a complete rich interaction slice. The current clean installed run already passed the exact source cold-relaunch journey and visible clarification; its final bundle and screenshots are still pending. Accessibility heading-focus changes were authored after that binary was built and require their own integrated regression.


The correctly installed runner completed all 20 installed journeys with zero failures at 02:03:04 on 5 September (`/tmp/neuroforge-current-installed-ui.log`, finalized `Test-NeuroForgeUISmoke-2026.09.05_01-48-51--0400.xcresult`). This includes all seven labs/eight supported response shapes, source exact cold draft and neutral-feedback replay, strict visible semantic clarification, claim mapping, and four honest finite-capacity endings. EN/JA Q3 cold continuation passed in 67.521 s / 72.806 s; source passed in 79.339 s. Finalized attachments were exported and visually reviewed; current source/clarification/claim/finite screenshots and updated Q3/sample/true AX5 images are retained with a file mapping in `Experience_Artifacts/`. Earlier failed/stale-runner evidence remains separately recorded. The later accessibility-focus, timing-separation and scratchpad-recovery source changes require the next integrated run; this 20/20 result does not certify those changes or the still-open interaction/device/editorial requirements.


The parent’s complete macOS reliability rerun passed all 784 tests with zero failures at 01:49:57 on 5 September (`/tmp/neuroforge-final-recovery-unit-tests.log`, lines 1861–1871; `Test-NeuroForge-2026.09.05_01-47-20--0400.xcresult`). This result precedes the later accessibility-focus, timing, history-context, scratchpad and creation-chooser patches.

The bounded UX-014 creation update is now authored and parsing clean. The source chooser searches filename/status/route, keeps selected IDs through search and pagination, allows removal of excluded selections, and opens the existing source detail for preparation/privacy recovery without closing the creation draft. Detailed form/count/objective/difficulty settings moved under More options; topic/task and lab/field stay visible. Selection/creation guards reject unavailable or incompatible selected material instead of silently using a subset. One new 45-row unit fixture tests search/pagination selection preservation and changed-policy removal; one installed test uses the existing synthetic source to select, search to no results, clear, close/reopen and remove. The installed suite now contains 21 authored journeys; the prior verified result remains 20/20 until the new binary runs.


The bounded UX-010 energy preview now captures the existing displayed `DailyPlan` without generating a proposed plan. It keeps the persisted energy picker value unchanged until explicit Apply, shows current remaining titles/minutes and fixed completed sections, and explains timing/shorter-session availability without promising an invented duration or challenge increase. Started and earlier-version plans have a fixed-list explanation; only eligible unstarted plans are described as possibly updating on Apply. Cancel and sheet dismissal have no persistence callback. The new installed preview→Cancel→cold-relaunch test asserts that the original energy preference remains saved. Swift parsing and whitespace checks pass; the installed suite now has 22 authored journeys, with this test pending execution.


The expanded installed suite completed 22 journeys with 21 passes and one selector failure in the energy preview (`Test-NeuroForgeUISmoke-2026.09.05_02-42-38--0400.xcresult`; `/tmp/neuroforge-expanded-final-installed-ui.log`). The actual injected runner SHA-256 matched its installed-binary manifest. The new source chooser journey passed, and its finalized screenshot was exported and visually reviewed. Energy preview reached the concrete effect and Apply screen; a generic accessibility query matched the Cancel wrapper and button, so no Cancel event was sent. The test now targets the native Button explicitly. The preview's long title was visibly truncated on SE and is now shortened to localized “Energy preview.” Their next-run acceptance remains pending.

UX-005/RUN-022 exit work is now authored and parsing clean. Ordinary and generated sessions pause timing, attempt the actual write and compare the exact current checkpoint with acknowledged local storage before closing. Local-first/legacy-second failure can therefore preserve a truthful saved/pending status. Both expose retry, readable recovery export/copy, safe close for acknowledged work, and explicit discard only for unacknowledged memory. Discard suppresses subsequent debounce/background/disappearance writes and retains the original saved checkpoint. A native window-delegate proxy preserves the prior delegate, and the application termination callback synchronously cancels quit if any session cannot acknowledge its state; the callback rechecks every window after a cancelled quit. Actual macOS window-close/quit behavior has not yet been executed and remains a manual/integration acceptance item.

Pending source self-check retry now commits the frozen rating/response rather than revealing the reference again. Protected pending state uses a versioned minimal local receipt containing only pinned identity/scorer and bounded outcome values; it cannot represent expected keys, normalized answers, worked feedback or error strings, and portable export explicitly removes it. New protected commits use the same policy-safe score object. Older pending or committed journals reconcile an existing authoritative AttemptRecord only after exact descriptor/catalog/digest/response/provenance/scorer verification, preserving original record bytes; missing or mismatched authority remains recovery-only rather than invoking a fresh evaluator. Seven new tests in `ExactSessionContinuityTests.swift` exercise blocked real storage, secondary-write failure, discard, source/generated pending cold replay, protected minimal receipt replay, original receipt reconciliation, and repeated native-close decisions. They are authored but not yet executed.

The ordinary adaptive-item runtime adapter now accepts the prepared exact checkpoint atomically before publishing the next item, preserves the accepted decision ID on later checkpoints and keeps duration-summary decisions from consuming the candidate. The count-based fallback forwards the original reservation strategy. Adaptive owns transaction/selection authority and its separate tests. A new installed journey commits a real ordinary numeric response, opens the weekly chart's accessible data row and follows its exact matching history to the original answer; 23 installed journeys are now authored, with this additional chart path pending execution.


Exit audit refinement: a persisted original attempt with an exact prepared journal may safely close as *pending recovery* when only the feedback acknowledgement write failed. This fallback compares the exact response, scratchpad, learner reflection, assistance and frozen score/receipt, and rechecks the authoritative original record. A new reflection after that commit remains unsaved and blocks close; the real protected-file fixture now covers both states. Registration identity is per view rather than per run, so a second window viewing the same run cannot overwrite or remove the writer's Quit check. The registry test explicitly removes the secondary saved registration while the first remains blocked. Native close/quit and new recovery-sheet layout still require execution; parsing alone is not acceptance.

The final pre-continuity macOS baseline passed852 tests with0 failures in143.604s (`/tmp/neuroforge-delivery-recovery-full-unit-tests.log`, source input `abbba93277e523edc0dd6bd3000c611c1a03b95656980d6fc0fc95c2d9ae59ac`). All20 exact-continuity tests passed, including complete numeric+required-unit protected responses, minimal-receipt cold recovery, legacy authoritative receipt reconciliation, blocked feedback writes, later unsaved reflection protection and actual presentation dismissal between resume boundaries.

The finalized installed23 run passed22 journeys in958.800s. This includes corrected energy Cancel/relaunch, source chooser selection/search, EN/JA question3 cold draft/feedback, all seven labs and supported response shapes. The weekly-chart journey failed before navigating to Progress: its fully visible Submit button highlighted under a standard tap, but the editor retained0 and no feedback appeared. No chart acceptance is claimed. The original movie remains in `/tmp/nf-delivery23-attachments`; the highlighted-button frame, visually reviewed energy/chooser captures and exact result scope are recorded in `Experience_Artifacts/delivery23-capture-manifest.json`. The revised chart journey uses the visible native keyboard Done action and preserves its strict result/history assertions; the highlighted Submit observation remains unresolved rather than labeled a confirmed framework bug.

The next authored batch has not yet run: one shared native proxy per window with token-specific checks; explicit Cmd-W and Quit routes that prepare every affected presentation before dismissing any; one-shot sheet-teardown continuations that preserve original native delegate decisions; DEBUG event-only diagnostics confined to UUID test storage; continuous question/response/feedback surfaces, safe Escape, and isolated test preference domains. Three real NSWindow/native-command tests and one preference-isolation test bring ExactSessionContinuityTests to24. The existing quantitative UI journey now inspects authentic chart/table counts while retaining its separate typed percentage; existing shape journeys also verify submitted editors remain available and disabled. Adaptive replacement publishes only the complete accepted exact checkpoint and preserves prior work in the repository transaction; its regressions are owned by Adaptive.


## Current interaction and native exit verification — 5 September, 05:40

The subsequent full installed23 run finished18/23 with five concrete failures; its exact scope and reviewed failure captures are retained in `Experience_Artifacts/replacement23-capture-manifest.json`. The logic-state failure was a real post-commit editor guard defect, now fixed by separating editable recall/draft from legal self-check comparison rating. Quantitative data-table reachability was corrected with a44pt product control and bounded, non-inertial test scrolling. Transfer selected the same intended options in their visible top-to-bottom order. The source's then-unacknowledged Resume press was not bypassed; on the native-iOS target rerun, exact cold recall/reference/feedback passed.

The targeted installed rerun in `/tmp/neuroforge-native-citation-five-installed-ui.log` passed Logic58.486s, Quantitative84.220s, Source85.697s and Transfer51.387s. Weekly-chart navigation now reaches an actual data Button labeled “Aug 31, 2026, Mental Math, 0%, 1 scored answer”; its test incorrectly searched for “1 answer”. Only that substring has been corrected. Exact matching-history and original-detail assertions remain, and chart completion is still pending. The05-28-09 xcresult was still finalizing when this entry was written; captures will be exported only after it closes.

Root's copied-app native verification now proves Command-Q teardown, actual Command-W routing, exact cold draft recovery and preservation of scratchpad child-sheet behavior. `Native_Mac_Quit_Verified_Manifest.json` and `Native_Mac_Close_Verified_Manifest.json` hold that scoped evidence. Earlier inert-key observations remain in the chronological QA record. The three new key-routing NSWindow fixtures initially crashed their host because the helper retained windows that native Close released; the helper now explicitly disables native release, consistent with existing fixtures, and a targeted rerun is pending. This correction does not change production close authority.

The read-only INT001–008 audit confirms that several larger domain slices still need engineering: durable estimate stages/optional working checkpoints; discrete spatial object transformation/folding; a same-study claim-to-experiment sequence; persisted interpreter trace/prediction/help state; explicit insufficient-source reporting and source-retention integration; and a linked two-skill relationship/solve/debrief contract. Existing generated response schemas and independent questions are not labeled as those completed slices. Live committed source-citation actions and authentic count inspection now exist and supersede the older missing-component wording.

A shared INT-008 ordered-response implementation is ready in `/tmp/neuroforge-ordered-editor` but has not yet entered the app source. It unifies ordinary/generated ordering without changing the response schema or scorer. The controller validates complete stable-ID permutations; drag only previews until an opaque current-drag token and unchanged predecessor order are verified; Undo, native UndoManager callbacks and arrows share the same guarded response path. Seven meaningful tests and an installed Undo/reapply amendment accompany it. Standalone Swift6 strict-concurrency typechecking and an isolated executable native-undo/drop harness passed; app compilation and actual pointer/keyboard/VoiceOver execution are still required.

## Current integrated interaction evidence — 5 September, midday

The finalized23-journey run `Test-NeuroForgeUISmoke-2026.09.05_06-17-04--0400.xcresult` passed21/23 in1052.634seconds. Exact source/binary scope and reviewed captures are recorded in `Experience_Artifacts/journal-ordered23-capture-manifest.json`. The quantitative image visibly contains43/21counts; source chooser and Energy preview captures are current. The Japanese continuation failure retained the saved Q3 card after a single visible50ms Continue touch without entering the session; its original movie/AX record does not establish why the action was unacknowledged. The weekly data row showed the expected date/activity/count/credit and actually opened matching history, but its test resolved a transient old scroll-view index before the navigation finished. That helper now waits for the destination; detail completion is not yet certified. An ordered-feedback image also captured a blank-looking indigo Next label despite later successful navigation; it remains a visual observation requiring a fresh capture.

The parent integrated durable math, observed-proportion data and exact interpreter-trace workflows. The scoped app run in `Restore_Data_Trace_Math_Typed_Run.log` passed all8math,6data and10trace tests; its broader run had an unrelated compiler-fixture assertion and is not labeled a zero-failure full suite. The later trace input bounds and shared local-save-before-summary-failure fix preserve exact saved stages instead of rolling them back after authoritative acknowledgement. The linked science package uses that same helper, includes nine actual runtime/recipe/recovery tests and46new EN/JA entries, and remains pending parent app integration/execution.

Native ordered QA now proves focus-local arrow movement and native Undo. The private-payload destination bridge passed its isolated app regressions and received a real drag-enter event, but the recorded OS session remained active until Escape and made no order change. The new source lifecycle records click on mouseDown, starts NSDraggingSession on actual mouseDragged after the movement threshold, and clears click-only state on mouseUp. Its bounded event fixture is authored/typechecked; no native successful-drop claim is made before the rebuilt app is exercised. Earlier source-only and failed-native evidence remains in the chronological record rather than being overwritten.


## Current linked interaction evidence — 5 September, 13:30

The latest full Mac baseline is1,044/1,044 on source ad19906. Installed science221.262s, trace159.182s and actual repeated-cold restore74.281s pass on that source; the same four-journey run failed data because ordinary feedback hid the optional prediction editor. The editor is now retained and locked, matching generated practice; the next installed test includes a nonblank prediction. Persistent visible labels now remain beside typed logic fields. Transfer's coherent relationship-save/target-solve contract passes all8 new tests after primary Transfer skill attribution was corrected; its installed cold/one-attempt journey is authored. Spatial6 tests pass and its installed journey now checks independent-stage gating, exact object rotate/reset and immutable saved choice.

The new graph construction route is integrated behind an explicit secondary Scaling Law action; primary scaling variety remains. It uses exact unitful typed coordinates across plot, Steppers, Undo, feedback and history. Initial integration exposed two private feedback-state accesses and an empty declared initial-state schema; the existing interfaces and public measured baseline are now used. The learner's draft remains blank. Graph execution is still pending the corrected run. Each source/failure/result has a separate manifest binding; no later source is certified by an earlier green run.
