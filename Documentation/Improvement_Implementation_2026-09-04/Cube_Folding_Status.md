# QA-F15 / INT-002 — retained cube-net and folding contract

Frozen scratch candidate: `cube-folding.patch`.
SHA256: `569535a8458a5d39de78a3b7dbc21aff6f26ab6d3f595b61bb6371f154562274`.

No main source/test edits, builds, xcodegen, new source files, or project-membership changes were made. The patch changes 16 existing Swift files and the localization catalog. `base-hashes.json` records its exact repository inputs. It preserves the root's generator/factory worker-frame splits, section13 and coordinate12 recipes, protected guards, command-authority changes and archive work by rebasing from the current files.

## Delivered behavior

New ordinary spatial launches and reviewed preview request construction opt into `netFoldingPolicyVersion = 1`. Only the existing cube-net family uses generator14/schema11 and `.r14` item identities. Nil policy retains the previous generator4/schema2 net bytes. Saved sessions must match their pinned recipe; a new contract with a missing/future policy becomes unavailable, and a new policy cannot silently accept the old net question. Protected and source-grounded generation remain excluded.

`NFNetFoldingContract.make(exercise:)` authenticates the complete retained net, query, partial given, prompt, instructions, exact single/multiple-choice key, rubric, representations, feedback, language, provenance, family and semantic identity. The new family mapping is explicit because the new `folding.retained-net` slug does not prefix-match the older catalog slug. Schema and scoring guards call the same validator. The catalog description now names adjacent faces, arrows and proposed-pattern validity, with EN/JA translation.

Concrete QA-F15 demands are present:

- All 11 free cube-net shapes and marked shared-edge creases, with exact face labels and original printed arrows.
- Opposite-face and select-all-four-adjacent questions. The latter accepts the complete set in any order and does not select only the first valid face.
- A real partial-fold given: one named non-root leaf is already hinged through 90 degrees; every other hinge remains flat. Each descendant/face retains an exact oriented coordinate frame. Fixed-axis normal and printed-arrow questions ask about completing that fold with the anchor unchanged.
- Two simultaneous face constraints, with all four truth partitions (both hold, first only, second only, neither). Failure of either condition makes the proposal false.
- All 35 free connected six-square proposals, including 24 invalid nets. Invalid candidates are explicitly described as proposals to evaluate, never as valid nets, and cannot open a fabricated completed cube.

## Native/static rendering and privacy

The original flat crease pattern, printed arrows, coordinates, anchor axes and any named partial fold remain visible. RealityKit and the static projection use the same exact face frames. Face centers and vertices use integer doubled coordinates; render floats never decide the key. Camera controls only change the view. Fixed axes remain anchored, and full-fold arrows/normals match the retained orientation.

Independent input has no computed Fold/Unfold action. After committed feedback or in saved-worked-solution history, explicit buttons show the completed fold, unfold all hinges, or restore the exact original given (including a partial fold). These are discrete endpoint demonstrations, without a continuous fold animation or a claim about collision-free animation trajectories. Reset preserves the response and original bytes. Invalid proposals retain the flat source and explanation and never acquire a completed-cube scene.

Each fold/reset button uses its own bordered style and an actual inner 44×58 point label frame. System and app reduced-motion preferences are forwarded to native orbit controls. Generated views use the actual generated phase (`stage == 2`), not the ordinary runtime's private/absent properties. The shared history renderer receives the complete retained exercise under the existing protected-first history guard. Unsupported/protected payloads cannot reach this renderer's full contract.

Ordinary and generated input, checkpoint/cold restore, scoring and history retain the exact contract. Inspection/camera state is a post-response view state; the saved task and submitted typed set stay immutable. No scorer version, model column, original legacy result, admission signature, or reviewed band is changed.

## Verification actually executed

`python3 verify.py` executes pure Swift geometry from the actual cube engine plus this candidate. `python3 census.py` independently enumerates the 24 oriented faces of a closed cube and propagates **directed shared-edge endpoint constraints**, rather than using the generator's fold-update recurrence.

The independent oracle passed all 35 patterns, all completed/partial frames, cube closure at eight corners, and all exact response sets for all 469 delivery forms. It confirms exactly 11 valid free nets. It also verifies every original shared hinge and that only the stated leaf departs from the flat given.

All 16 candidate Swift files passed syntax parsing. Catalog JSON and current-tree `git apply --check` passed. These are not an app module typecheck, XCTest execution or installed native verification. Root owns those remaining runs. No independent UI-review or approval claim is made.

Nine new methods in existing `Tests/ContentImprovementRegressionTests.swift`, class `CubeFoldingContractTests`, are ready:

1. `testAllThirtyFiveFreeProposalsHaveExactlyElevenValidCubeClosuresFromIndependentFaceAssignment`
2. `testEveryRetainedTaskUsesExactAllFaceAuthorityAndExclusiveLocalizedChoices`
3. `testPartialGivenMovesOnlyItsLeafAndPreservesEveryHingeEdgeAndPrintedArrow`
4. `testRoleSymmetryAndFaceRelabelingDoNotManufactureAnotherNetTarget`
5. `testUnsupportedProtectedAndContradictoryRetainedContractsFailClosedWithoutChangingLegacy`
6. `testFoldUnfoldResetAndNativeFacesRequireCommittedPhaseAndRetainExactOriginal`
7. `testActualOrdinaryColdCommitAndNextRetainExactNetFamilyAndOriginalArrows`
8. `testGeneratedMultipleAdjacentFacesUseExactRetainedSetThroughColdCommit`
9. `testDetachedFactoryReachabilityKeepsFiniteQueryCapacitySeparateFromElevenTopologies`

The generated fixture uses the existing legacy wrapper's supported answer-string surface; automatic correctness and saved history come from the complete retained multiple-choice schema and all four selected IDs. All fixtures retain their in-memory ModelContainer and use unique disposable repository/cache/history/document/temp/rotation dependencies with shared widget publishing disabled.

## Capacity and remaining requirements

`Capacity.json` is regenerated by the independent census. There are 469 delivery forms and **417 semantic targets**, across only **11 valid net topologies** plus 24 explicitly invalid proposed patterns. Counts by form: 35 validity, 52 opposite, 52 all-adjacent, 110 normal/arrow orientation and 220 two-condition proposals. Opposite and all-adjacent forms share 52 target IDs; relabeling and planar symmetries collapse unoriented face queries. Fixed-axis questions retain their actual anchor/arrow convention. These are query targets, not additional cube nets or a band assignment.

The proposed family quota is 166, but reviewed admission remains **zero**, leaving an admission deficit of 166. A count of authored query targets alone does not prove a balanced admitted edition. The patch does not rewrite the signed manifest or existing bank positions. The last test reports actual focused-50 and all-1,000 mixed-bank reachability; that result is pending root execution and must be recorded before claiming availability. Curated band/structure quotas, reviewed comparability, a versioned expanded bank, and actual native/VoiceOver acceptance remain outstanding.

This package supplies bounded forward folds and explicit proposed-net/face-constraint evaluation. General inverse net reconstruction, continuous fold collision trajectories, unrestricted arbitrary user-authored layouts, and additional solid topologies are not implemented or guessed. Existing typed-recipe retention scheduling remains a separate follow-up: this opt-in is ordinary practice/document practice, preserving explicit unavailability where an old retention recipe cannot faithfully reconstruct a new task. Coordinate inverse/unknown-transform and scored object-orientation gaps remain on the parent task's list. The other family obligations are not marked complete by this package.

## Executed development verification

All nine CubeFoldingContractTests pass on c0c4fc58. The actual factory produces 50 distinct focused questions and 60 semantic targets through the retained mixed bank. The full run passes 1,287/1,288 methods; its one failure is the older cross-activity fixture rejecting the new exact retained-net family. A narrow fixture correction is integrated and needs execution. This is engineering verification, not reviewed admission or a newly balanced bank.

## Installed valid-fold path

On3db5, the actual native journey passes192.632s. It asserts that the visible seeded task is a valid cube, solves from public grid/constraints using independent directed-edge assignment, and checks every completed normal/arrow. Camera Top/reset and Static2D preserve givens; computed controls are absent before commit. Complete fold, Unfold and Restore given all execute after Correct feedback, retain the locked answer and advance to2/5. This covers this real valid query, not every469 delivery form or full physical/VoiceOver acceptance. Source continuity is the sole failure in the same four-journey run and remains separately investigated.
