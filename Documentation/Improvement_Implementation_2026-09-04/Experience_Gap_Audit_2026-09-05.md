# Experience gap audit — 5 September 2026

## Latest verified corrections

This addendum supersedes older pending-execution rows below. Full1158/1158 passed mixed reviewed-family coordination and saved protected Next/Skip/sitting/End publication. The newer170/170 focused run passed all20generated lifecycle/terminal inventory methods and all11plan-history methods, including7exact replacement preview/cancel/stale/local-run/day-boundary fixtures. The reviewed registry remains empty.

Quantitative original data43/21, real44pt outcome menu, number/unit, Correct feedback and Next passed on58324687. Linked science, trace, transfer, spatial transforms/reset and cold restore have separate earlier installed passes recorded in Run_Manifest.json. Graph has genuine typed construction/coordinate controls; current native work fixes scroll-vs-plot gesture contention. Generated native setup and retrieval actual-figure oracle are under rerun. Actual Today replacement preview/cancel/cold/apply/acknowledgement is now authored and awaits execution.

Source retention identities, all-family substantive expansion, wider generation-bearing command fencing/off-main durable writes and the documented physical/accessibility/release matrices remain implementable or external obligations as specified; no partial journey closes them.

This audit rechecks the chapter 01 requirements against the current implementation. It distinguishes unfinished product work from unexecuted acceptance checks. A passing seeded journey demonstrates that journey only; neither unit totals nor the installed smoke suite establish completion of every interaction, content family, mode, accessibility path, or release gate.

The implementation ledger is the per-requirement index. The following findings supersede any earlier wording that described a partial requirement as implemented in full. Source line numbers are intentionally omitted because the active implementation is changing.

## Active bounded corrections

| Requirement | Confirmed gap and current action | Acceptance still needed |
|---|---|---|
| A11Y-004 / A11Y-005 | Ordinary/generated/source editors have explicit names and saved-transition accessibility-heading focus; semantic clarification focuses visible guidance. Draft editing retains keyboard focus. | Compiled in expanded installed22; visible response journeys passed. Actual VoiceOver announcements and full hardware-keyboard journeys remain unexecuted. |
| UX-013 / D04 | Persisted Untimed / Elapsed only / Timed fluency are separate from activity/Transfer mix. Optional display timing does not change difficulty or create speed evidence; reviewed readiness gates timed fluency. | Final integrated zero-failure suite and installed setup/timing-mode inspection remain pending; current response journeys do not cover every timing choice. |
| DATA-020 / UX-022 | Content owner implemented bounded malformed/future/oversized scratchpad handling with retained-original export and separate oversized unsaved export. Contextual layout and native Undo/Redo are now integrated. | Execute the current combined byte/geometry, native editor, failed-save and installed contextual-layout checks. Recovery/export alone does not certify responsive editing or physical Pencil input. |

## Remaining implementable obligations

| Priority | Requirement | Observed implementation | Required completion |
|---|---|---|---|
| Active verification | UX-005 / RUN-022 | Exact pending/acknowledged exit and protected receipts passed the852-test baseline. Shared window registration, explicit teardown, Quit and narrowly scoped Command-W routing are implemented. Root verified real copied-app Quit and Command-W, exact cold draft recovery, and the scratchpad child-sheet guard; its native manifests retain the evidence. | The corrected native fixtures subsequently passed in the integrated regression; retain the actual native manifests with the implementation evidence. Full multi-window, all recovery actions and all child-sheet combinations remain separate; the scoped actual native passes are no longer an unexecuted claim. |
| Active verification | UX-025 | Content owner has added original logic-state variables/transitions/rules, permitted used-hint replay, and a source-citation history sheet. It distinguishes saved content revision from extraction-format version and gates protected history. | Integrate and execute the new history tests and inspect the citation/hint UI. Do not replace missing or changed historical source content; unsupported versions remain recovery-only. |
| Active verification | INT-006 / UX-025 | Exact source recall/reference and neutral save use the common lifecycle. Live feedback now opens citation context through a committed attempt/session/exercise digest, with changed/deleted-source recovery; this supersedes the earlier missing-action audit. | Verify the live citation action and Escape child-sheet boundary. Explicit insufficient-excerpt reporting is now integrated; source-retention integration remains engineering work. |
| Active verification | UX-010 | Energy selection now stages a pure current-plan preview with explicit Apply/Cancel, exact current remaining sections, fixed completed sections and honest pacing effects. No alternate plan is generated for preview. | Cancel/cold-relaunch now passes and its latest SE preview is visually reviewed. Separately complete the full Adjust/replacement title/time/consequence preview; the bounded energy fix alone does not certify that path. |
| Implemented; broader QA pending | UX-014 | Searchable 20-row chooser with selected-only review, stable selection, explicit exclusions and source-detail recovery; detailed settings are under More options. Consent is unchanged. | Projection regression authored; installed search/clear/reopen/remove journey passed in verified 21/22 run and actual SE screenshot was reviewed. Full locale/device/editorial and source-policy matrix acceptance remains separate. |
| Active verification | UX-024 | Local scheduler-backed deferral now pins the exact memory/anchor and return instant, preserves accepted runs, refreshes eligible reminders, and separates bookmarks from due practice. Queues are bounded and paginated. | Finish current integrated deferral tests and installed queue actions; shared day-boundary/DST correction is owned by Adaptive. No due result may be inferred from merely hiding a row. |
| Active verification | UX-022 / A11Y-001 | Scratchpad now receives the exact NFExercise. Content owner added independent-given-only context with table/diagram preview, a >=900-wide split and compact/AX pinned expandable preview, plus native Undo/Redo while preserving the same editor identity. Universal is wired to the new initializer. | New policy/native editing tests are authored. Installed compact and wide layouts, AX clipping, keyboard editing and physical Pencil behavior still require execution; no claim of those checks is made from source inspection. |
| Active verification | UX-023 | Root implemented point inspection with date/activity/count/value and an exact matching-history route, accessible data actions and 50-row paging. Summaries use corrected/effective practice observations and withhold protected individual results. | Execute new projection tests and the authored installed real-response → weekly data → matching history → original detail journey. View and retain actual chart point/data screenshots; the earlier aggregate-only table is not the current implementation. |
| Active verification | UX-016 | Continuous ordinary/source/generated prompt, response and feedback are implemented. A real installed logic-state feedback-editability failure led to separate editable-draft/self-check-rating guards and controller regressions. The latest targeted installed logic, quantitative, source-cold and transfer journeys pass. | Finish all current-source installed regression and generated-specific coverage. VoiceOver, wide/AX and physical keyboard matrices remain distinct from the demonstrated simulator paths. |
| Medium | A11Y-003 / COPY-001 | Current numeric fields use full-size targets and responsive value/unit composition; verified true AX5 coverage is still onboarding rather than every response schema. UX-014 now says Import in Sources. | Inspect actual AX5 numeric, claim/state and creation flows before declaring responsive completion. Audit remaining context-specific terminology without relabeling precise methodological terms. |

## Rich interaction scope still incomplete

| Requirement | Implemented component | Remaining product or validation work |
|---|---|---|
| INT-001 | Integrated saved estimate-before-exact stage, optional calculation-chain working/first divergence, durable compensation support and same-family repair. Eight focused app tests passed. | Installed stage/working/feedback controls and native/AX layout remain unverified. This bounded math policy does not establish every math family or all instructional variants. |
| INT-002 | Native spatial view plus authored/front/side/top/reset camera controls; camera does not mutate the scored response. | Discrete object rotate/reflect actions distinct from camera, controlled permitted fold/unfold instruction, and state announcements or a validated honest alternative. Protected tasks must not gain solution-performing tools. |
| INT-003 | The observed-proportion contract shares exact numeric counts between chart/table/inspection; actual numeric43/21capture and installed independent-answer journey passed. A new typed sidecar saves the initial numeric prediction before a denominator overlay and records support; six focused tests passed. | Execute the new prediction/overlay installed path. No claim of arbitrary graph construction, every data family or protected explanatory-overlay authority is made. |
| INT-004 | Same-study evidence mapping → experimental-design choice, durable frozen firststage, scoped final scoring, controlled assignment/measurement explanations and changed-context repair are authored in /tmp/nf-linked-science-20260905; nine tests and EN/JA values are ready. | Parent integration/app tests and installed two-stage ordinary/generated parity are pending. The package intentionally covers synthetic reviewed-by-code teaching cases, not general live-source study generation or all science families. |
| INT-005 | The exact restricted AST/program now drives ordinary/generated trace rendering, saved initial/next-state prediction, step/reset/undo and durable reference assistance; history replays original input/support. Ten focused app tests passed. | Latest500-character guards and acknowledged-local-write recovery need combined verification; installed/native code editor/step controls and all interpreter skins remain unexecuted. |
| INT-006 | Exact private recall/reference/neutral self-check, committed source citation, explicit insufficient-excerpt reporting and unavailable-source recovery are integrated. Source cold draft/feedback journey passes. | Complete source-retention policy integration; inspect live reporting/citation/retention flows under changed, deleted and malformed source variants. |
| INT-007 | Deterministic transfer content and optional reflection. | Verify and implement the bounded two-skill relationship-selection plus solve/debrief slice. A story-themed multiple-choice item alone is insufficient; the contract must establish the shared structure and transfer distance. |
| INT-008 | Shared ordinary/generated stable-ID editor/controller, private typed preview/drop, Undo/Redo,44pt controls, focus-local arrows and position announcement are integrated. Installed Undo and actual native arrow/Undo passed. | Native drag entered but did not finish; Escape cancelled without order change. The conventional mouseDragged-start lifecycle patch is ready for actual rebuilt verification. Full manual VoiceOver/reorder acceptance remains separate. |

These entries supersede the earlier sample-only descriptions. Each new sidecar preserves accepted legacy nil state; neither a component-level test count nor one installed seeded journey certifies the full domain requirement.

## Acceptance work that cannot be inferred from code

- Actual VoiceOver and hardware-keyboard completion across response families, including pause/resume/report/finish, changed-value announcements and focus after failed save.
- The full width/window/locale/text-size matrix, including 320-point compact content, wide Mac/iPad split panes, landscape and all response screens at AX5; true AX5 onboarding is only one tested state.
- Physical interruptions, locking/backgrounding, supported minimum OS, reported macOS navigation responsiveness, and same-device concurrent-window UI operation.
- Independent Japanese educational review, learner usability sessions and the specification's content review/admission requirements. A semantic inventory shortfall is product/content work, not a waiver supplied by a passing test count.
- Independent content/demand review and empirical calibration, retention and transfer evidence before stronger claims.
- Signed CloudKit/widget/Shortcuts distribution paths, TestFlight and release/App Review checks.

These gaps are retained as outstanding work, not reclassified as “manual QA” to make the implementation appear complete.
