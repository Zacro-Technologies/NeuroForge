# Reviewed fluency routing repair

Scratch only. No repository edits, project generation, or app/test builds were performed by this agent. The five new tests have parsed but have not executed. Parent owns execution and source manifests.

## Scope

Apply `reviewed-fluency-routing.patch` to the current tree, and merge the single EN/JA entry from `catalog-entries.json` entrywise. The patch contains five source files plus existing LocalLearningLifecycleTests. It does not touch forthcoming coordinate recipe pins/typed mapping or the NativeOrdinaryCommitProtocol supports switch. It preserves the integrated generation-bearing writer commands and checked revision helper. No persisted field or schema version is added.

## Authority

- `NFSessionTimingCondition.accepts(_:reviewedDemand:)` is a pure comparison, not a manifest authority. The optional demand defaults to nil. A raw embedded editorialDemand cannot authorize timed fluency.
- `NFEditorialAdmissionContext.acceptsTiming` verifies exact exercise digest, current scorer/manifest, semantic identity, content locale, lab and optional session/catalog scope before supplying the demand. Selection and preview reuse their already verified entry to avoid a second manifest scan.
- `reviewedFluencyReadiness` consumes effective immutable observations and authentic original capture, exact admission, current profile, owner/run/slot authority. Corrections, quarantine, raw/conflicting payloads and protected exclusions remain effective. Exact catalog activity matching replaces the absent legacy mechanic string for reviewed exact-bank attempts. Callers can constrain the family, band or complete compatible group.
- Pure eight-success/two-session/untimed criteria and clean-speed exclusions are unchanged. Untimed prerequisite readiness does not imply that device latency or clean-speed evidence exists.
- New timed launch verifies the requested compatible readiness group and ordinary adaptive route. Initial and accepted checkpoint builders receive the selected admitted demand; acceptance still rematerializes/recomputes under the existing transaction.
- `admittedTimingDemand` checks current owner/profile, unchanged delivery/configuration, exact run/slot/attempt/decision, receipt/manifest/digest and ledger linkage. This is used for runtime timing authority; no observation is minted from a timer.
- Before a repository is attached, runtime checks supported timing shape but remains not durably prepared, noneditable and unable to submit/acknowledge a timed question. The view renders the existing preparation gate; the timed header/title and timer are also suppressed until this authority succeeds. The first checkpoint must authenticate the saved timing against the attached repository/current profile before the gate opens. Changed or released-empty manifests fail with the original draft retained and no clock publication.
- New ordinary nil timing retains prior Boolean compatibility. Untimed/elapsed-only still accepts ordinary unknown metadata. Existing snapshot/draft bytes are not rewritten to embed a demand. Unsupported or unauthenticated timed condition payloads remain unavailable.

## Native setup

Timed fluency is enabled only for the explicitly shown reviewed family/band choice. If readiness becomes invalid, Start is blocked rather than silently converting the request to untimed. The generic recommendation starts untimed; a saved legacy Speed focus preference is not treated as consent to an unlabelled timed run. The timer remains independent of requested band and original contract.

## Tests appended

1. Actual external admissions: eight full successes across two four-question sessions, timed Next, private-file cold reopen, same prompt/draft, two wasTimed receipts, and no extra untimed prerequisite credit.
2. Wrong family/band/current profile and tampered original response exclude readiness; restoring the exact original restores eligibility without score mutation.
3. Wrong-scope launch consumes nothing; changed-manifest cold run cannot publish an editor/clock/checkpoint, retains draft/bytes; released empty cannot unlock.
4. Exact activity filter succeeds with genuine retained contracts when the legacy mechanic label is nil and rejects unrelated activity.
5. Pure matcher rejects missing/embedded-only demand, changed prompt digest, unknown scorer and wrong locale; untimed/elapsed remain supported.

## Still separate

`Next_Implementation_Checklist.md` preserves the three next bounded tasks (demonstrated initial target, unavailable-choice retention/reasons, substantive setup preview). Production admissions remain empty. No real content signoff, learner pilot, clinical/ability claim, clean-speed protocol or protected-check authority is invented.

Independent read-only review: Content found no additional critical authority bypass in the external-demand/receipt/profile/capture path. A final own pass additionally suppressed the timed header/title and showsTimer before authority; tests explicitly assert those presentation gates. This is source review, not test execution.


## Root execution update

Root execution: first actual eight-commit tests expose incompatible canonical-day numbering in the AppStore readiness caller. A narrow correction uses NFEditorialCapturedDay and adds a non-Gregorian display-calendar regression. The original localized key was integrated in the wrong JSON shape by root; corrected to the catalog schema. Corrected execution pending.


## Root executed evidence after canonical-day correction

All six routing regressions pass in the full1,237-test run; its two failures are unrelated retrieval worker crashes. This includes actual eight-success/two-session timing eligibility and a non-Gregorian display calendar using the same immutable Gregorian training-day capture. No reviewed production content was admitted by these synthetic tests. V2 demonstrated starting-target work and unavailable-choice preservation are separate subsequent slices.
