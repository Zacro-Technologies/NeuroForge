# Generated Next and End: accepted asynchronous transitions

This three-file delta follows the integrated generated explicit Submit package. It adds the shared coordinator `advanceAsync`, preserving the same legal command/phase predicate as its synchronous adapter. Generated production Next, explicit End and their command-center/retry paths use the existing storage actor. Initial launch and unscored commits remain named migration slices.

## Complete immutable endpoint

A Next proposal contains the complete retained run prefix plus the exact next authored exercise, new slot/attempt IDs, blank response and initial typed sidecars. An End proposal retains the complete current unfinished response/sidecars and actual receipts/counts, then runs the existing authenticated terminal inventory/source-closure transform. The endpoint itself contains all required state, so one revision- and generation-checked actor transaction replaces the former save-current then save-endpoint sequence. The same pure proposal builder is reused by the remaining synchronous compatibility adapter, removing its unchecked next-revision addition.

No live AppStore or SwiftData object crosses actors. No new archive schema/model fields are introduced. Existing modern revision CAS and legacy exact private-payload CAS remain authoritative. Source/result/recipe bytes are copied from accepted authored inventory; no question is generated or guessed during Next.

## Pending and acknowledged states

The runtime retains an immutable source plus complete proposed endpoint and captured writer command. A failed or pre-commit-cancelled write leaves the old bytes and visible question/feedback intact, and retries reuse the same proposed slot/attempt IDs. Once rename commits, cancellation cannot retract the receipt. A directory-verification failure adopts its accepted revision but keeps the old presentation noneditable behind recovery until the exact endpoint is reverified. Old editor autosaves/close cannot overwrite that accepted next/terminal state. A released generation cannot publish or retry it; authentic cold restore or explicit refreshed takeover reads the accepted archive.

Publication restores fields through the same exact draft restoration helper used by cold recovery, including compacted result/request inventory. The next answer clock stays stopped until actual presentation acknowledgement. A pause during persistence keeps its interruption metadata. Only a queued checkpoint can follow an exact source-to-target continuation; ordinary delayed question commands retain their original identity. Explicit End may follow the same accepted continuation, so it is not silently lost while Next finishes. It ends with the unpresented current item retained and no invented completion or evidence.

## Authored verification

Twelve tests use the actual injected storage worker and its real encoding/staged/renamed/committed events: held Next; modern/legacy stable-ID pre-commit cancellation; modern/legacy post-commit cancellation plus cold restore; Next and unfinished-End verification failures/repair; End unfinished-work compaction; post-commit End cancellation with original receipt unchanged; writer release before and after commit plus explicit takeover; queued Pause/checkpoint; late native field retention; and End/duplicate-Next contention. No test result or native pass is claimed here.

Three full candidate files pass Swift frontend parsing and current-main apply check. Isolated Swift 6 complete-concurrency typechecks compile the actual coordinator/runtime and all twelve test bodies against the current app module. Root still must compile and execute the actual app suite and installed paths.

## Remaining actor migration work

Generated Skip/worked-solution preparation, immutable unscored receipt and feedback still use the synchronous unscored coordinator. Skip's automatic advance therefore also remains synchronous until that coherent follow-up calls `nextAsync`. Initial run acceptance/presentation, intermediate teaching/support locks, ordinary/protected Next/Skip/End/Replace, reviewed/admin/restore writes, SwiftData receipt insertion and legacy projections remain separate. This package does not describe those engineering tasks as external validation.

Actual Mac verification on2ba96fe3: complete1359-test suite passes284.558s. Includes all13generatedNext/End and3timingreceipt/target-change tests. Actual iOS new transition workers and new installed Next/End implementation remain pending.

Actual iOS worker verification now passes all 13 generated Next/End cases (0.892 seconds, source `aaae840e`). Installed Next/End verification on this newer source remains pending.
