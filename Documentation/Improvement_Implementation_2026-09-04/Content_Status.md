# Content and scoring implementation status — updated 5 September 2026

Current retrieval asset update (5 September): all eight `RetrievalAssetContractTests` passed in the 89-test Mac scope on `ed785a62`. The real generator/scorer exercised 98 compatible renderings of 38 distinct existing targets, including numerical equations and native figures with equivalent retained tables, EN/JA cloze, corruption rejection and ordinary/generated cold/history preservation. This is a bounded unreviewed inventory. See `Retrieval_Assets_Status.md` and `Retrieval_Assets_Capacity.json`; 98 renderings do not count as 98 independent targets. Generator10/schema7 is opt-in and nil policies retain earlier recipes. Full-suite and native rendering acceptance remain separate.

This is an **interim corrective implementation**, not completion of the complete specification or certification of an expanded four-band edition. Every one of this chapter's 53 normative requirements is tracked below. No requirement has been silently dropped or marked deferred by an invented scope decision.

Versions: fallback generator/template4; scorer8; offline bank3; semantic fingerprint2; optional metadata schema1. New code preserves the existing signed descriptor gate and its public key. The corrected dynamic bank is **not covered by a newly signed content manifest**. `Content_Admission_Release_Gate.json` is a local unsigned development/release-blocker report, not a signature or deployment approval.

The implementation adds bounded response authority, known-defect repairs, role-aware independent rendering, explicit outcome/components, deterministic quota assignment and exact editorial approval deficits. New shared code is in `Sources/TrainingEngine/NFRestrictedSymbolicAuthority.swift`, `NFResponseAuthority.swift`, `NFRetrievalResponseAuthority.swift`, `NFExerciseContractMetadata.swift`, `NFContentAdmission.swift`, and `NFContentCorrectionPolicy.swift`. Existing generator/scorer/model/fingerprint/bank adapters are changed. No signing material or production manifest was generated.

## Executed evidence and acceptance limits

- A standalone `xcrun swiftc` production-parser probe compiled and ran (see `Content_Parser_Probe.txt`): `4x`, `4*x`, `2x+2x`, `4x^1`, and nested grouping matched; `2x` differed; variable denominator and exponent99 were unsupported; grouped F(b)−F(a) matched, f(b)−f(a) differed, F*b−F*a was unsupported.
- `Tests/ContentImprovementRegressionTests.swift` contains 14 executable regression methods for QA-S01–10 and QA-B08, including all 1,000 uncertainty parameter pairs and generated contract checks. All 14 methods passed in the latest root-owned Mac Xcode run at 00:40:53 on 5 September 2026, including the corrected catalog-ID fixtures and the added transfer, typed-state and quota-oracle probes.
- Updated pre-existing content/catalog tests distinguish self-report from objectively correct canonical responses; protected-key tests assert refusal; claim-evidence omission/extra-edge tests now validate syntax and leave semantic scoring to the rubric. Updated AIAndSourceTests follows inline confidence and immediate self-check comparison; no generated-runtime call sites existed in NextDayEnhancementCacheTests.
- The current concrete inventory suite passed at 00:09:45 on 5 September: 7,000 distinct contracts, 1,000 per lab, zero schema-invalid contracts, and 7,000 unreviewed demands. The newly emitted census matches the committed census byte for byte. Actual family/structure/objective/response/target/band counts and strict admission deficits are in `Content_Corrective_Inventory_Census.json`. Counts are highly skewed (e.g.990/1000 logic state traces,986/1000 transfer rate products), so numerical capacity is not a balanced edition.
- The production admission algorithm compiled and passed a standalone independent exhaustive oracle:64 candidate sets ×729 assignments plus a feasible variety case missed by raw quota assignment. See `Content_Admission_Probe.txt`.
- No independent subject review, 28 human sessions, English/Japanese editorial approval, physical VoiceOver run, signed manifest census or psychometric evaluation was performed by this subtask. Those are release blockers, not completed checklists.

## Requirement ledger

| Requirement | Status | Implemented evidence | Remaining acceptance work / limitation |
|---|---|---|---|
| CON-001 | in progress | Optional versioned NFExerciseContractMetadata now serializes semantic/objective/family/structure identity, revisions, roles, canonical seed and explicit unreviewed status. | Full typed stimulus/provenance/reviewer/oracle/canonical-parameter records and editorial demand adoption for every family remain. |
| CON-002 | in progress | Existing exact arithmetic, restricted interpreter and cube-fold engine retained. Corrected science derives numeric geometry before prose; spatial duplicate guard runs before scoring. | Full-domain independent family oracles and generator rejection reproduction for every admitted contract remain. |
| CON-003 | in progress | Invalid authored options/DAG/support contracts return invalidItem and no objective correctness; never a learner incorrect result. | Atomic replacement reservation, diagnostic seed/revision receipt and session family quarantine need complete runtime acceptance. |
| CON-004 | in progress | Four NFInstructionalRole cases; generated representation role arrays; independentRepresentations/independentContextText; conversion/Fermi/expected-value formulas are optionalPracticeHint. | Every representation/context/source/accessibility field needs independent editorial role review; hint exposure transaction and supplementary teaching assets remain. |
| CON-005 | in progress | Nearest-power-of-ten deliverable is explicit, including protected single-choice mode; state task explicitly asks end-state violation; boundary bug positive-input domain explicit. | All 58 wording-versus-assessed-deliverable audits, including generic workflows, remain. |
| CON-006 | in progress | Coordinates/numbers canonicalized; duplicate semantic choices invalidate item. Spatial symmetric-coordinate distractor replaced with origin reflection; structured transfer excludes equal slope/intercept values that duplicate a swapped-role answer. | General formula/prose equivalence annotations and all geometry boundary partitions still need full admission review. |
| CON-007 | in progress | NFClosedInterval validates finite ordered endpoints, distinguishes touching/overlap/disjoint; generated figure narrative derives from it. Separate supplied 95% mean intervals explicitly do not come from illustrative raw rows; no overlap-significance shortcut. | Independent statistics/editorial approval and richer contrast-interval B2–B4 tasks remain. |
| CON-008 | in progress | Removed counterexample key, zero-divisor conclusion, best-experiment solution, completed proof equation, compensation decomposition and solution-bearing spatial/loop alt text. Added protected-family role regression fixtures. | Exhaustive protected-manifest visual/VoiceOver/citation/nested-export inspection for every family and locale remains. Generated-authoring context roles are still absent, and its instructions field still aliases decisiveStep; the generated UI must not expose that field before response. |
| CON-009 | specified | Existing distractor IDs/error explanations retained; corrected coordinate alternative has origin-reflection misconception ID. | Every distractor needs authored derivation, comparable grammar/length and independent review; generic recognition distractors remain a blocker. |
| CON-010 | specified | Draft family matrix explicitly records real asset/operation requirements. Admission rejects missing required assets/locales rather than synthesizing them. | Current generic cloze/equation/figure/paper/workflow families still need substantive authored assets and accurate feature availability; no claim of full slice completion. |
| CON-011 | in progress | Removed targetDifficulty-based relabeling of unchanged generated demands. New editorialDemand is nil pending review, never inferred B1/B4. | Real band-bearing content and family demand progression remain for all unapproved bands. |
| CON-012 | in progress | Legacy difficulty now describes generated task, not requested metadata; adaptive owns reviewed demand types and availability selection. | Generator integration with real approved requested/realized band records and learner-visible unmet-demand route remains. |
| CON-013 | in progress | Typed editorial record is separate from legacy difficulty and includes calibration status/version; corrective records explicitly unreviewed. | Reviewed content adoption and empirical calibration dataset/model records remain; no calibration claim made. |
| CON-014 | in progress | Seven-lab listed quotas encoded exactly; retrieval’s listed900 versus required1000 contradiction fails closed; other lab totals are1000. Policies are in NFContentAdmission.proposedEditionPolicy, preserving 1000/lab floor; existing bank versioned v3. | Existing first-unique numerical bank is not a balanced new edition. Genuine family/structure production and approved admission are still required. |
| CON-015 | in progress | Deterministic min-cost max-flow assigns unique targets with exact family/field quotas, assets/locales, structure maxima and minimum distinct structures; first-structure reward is bounded at each family minimum. Exhaustive independent finite oracle passed. | Approved candidate production and integration into a signed edition manifest remain. |
| CON-016 | in progress | Retrieval fingerprints preserve target identity across response forms; admission has one capacity unit per semantic ID/fingerprint. | Generic Cycle Builder still must be separated from target knowledge; all compatible-rendering assets and review-lane runtime receipts remain. |
| CON-017 | in progress | Local reservation receipts and revision-checked atomic fixed launch now publish the cursor, exact first snapshot and run together; admission remains separate from reservation mutations. See Reservation_Implementation.md and Atomic_Fixed_Launch_Test_Run.txt. | Balanced reviewed whole-epoch ordering and the full expanded-family/adaptive selection acceptance remain; final execution of the newest atomic guard is pending. |
| CON-018 | in progress | Explicit unavailable-item state and focused-family tests prevent silent family switching on exhaustion; release-gate manifest names unavailable reviewed bands and quotas. | Full related/review alternatives and reviewed family-specific capacity acceptance remain. |
| CON-019 | specified | Existing no-replacement machinery retained without weakening capacity or signing gates. | Full cross-epoch eligibility, exact shorter-session alternatives, abandonment and concurrent windows acceptance still required. |
| CON-020 | in progress | New metadata provides semanticProblemID/fingerprint/objective/family/structure; fingerprint v2 includes typed authority, equivalent state, ordering dependency and support-bundle changes. | Canonicalization of all surface substitutions, geometry symmetries, pseudocode skins, locales and query roles remains. |
| CON-021 | in progress | Scorer v8, generator/template v4, bank v3, fingerprint v2; metadata records superseded generator v3. Pure correction predicates preserve original identities. | Granular per-contract supersession mapping for every changed authority and installed-ledger migration approval remain. |
| CON-022 | specified | Existing signed descriptor inventory/public key/gate unchanged; adaptive floor remains fail-closed because offline inventory coverage is nil. New release-gate JSON explicitly unsigned. | Authorized independent content review, manifest contentVersion update and offline release signature are required. No production signature generated or bypassed. |
| CON-023 | specified | All 58 draft matrix preserves prerequisites, demanded progression and family acceptance suites. | Independent named reviewers, dates, primary references, rendered samples and boundary/oracle review records remain external release requirements. |
| CON-024 | in progress | Removed several implementation/solution-bearing instructions; precision/science scope is explicit. Existing localization plumbing preserved. | New corrective copy and every accepted response need English/Japanese editorial and accessible layout review; default English fallback is not equivalent-language certification. |
| CON-025 | in progress | Scorer distinguishes clarification from conceptual error, exposes component results; existing hint/error explanations retained and protected solution leakage reduced. | Misconception-specific smallest corrective actions and fresh linked repair content for every family remain. |
| CON-026 | specified | Draft matrix flags workflow/calibration objective limitations and does not invent approved subject bands. | Generic Retrieval Cycle Builder and Source-Filter Trace still need target-specific tasks or an explicit learning-method-only migration and evidence route. |
| CON-027 | in progress | NFContentCorrectionPolicy now audits original evidence for duplicate choices, intervals, accepted state aliases, bounded F/f and prose, self-report, workflow, solution leakage, legacy demand and resume provenance; recommendations carry stable digests, rule revisions and exclusion scopes. See Historical_Content_Correction_Status.md. | Append-only corrections and effective history presentation are integrated and covered by 18 passing historical tests. Complete incident triage, exposure denominator reporting and per-contract editorial decisions remain. |
| CON-028 | in progress | Content_Family_Matrix_Draft.json contains exactly 58 stable family IDs and every QA-F01–58 requirement verbatim, with reviewedBands=[] and concrete missing work. | Complete reviewed supported-band contracts/assets and learner-facing unavailable-band enforcement remain. |
| CON-029 | specified | No numerical-range widening, label changes or cube symmetry aliases were counted as substantive advanced capacity. | Coherent new substructures and exact geometric/target canonical finite capacity for all expanded families remain. |
| CON-030 | in progress | Admission tests cover shared target assignment, required asset failure, deterministic permutation, exact quotas; existing bank census tests updated for honest self-report. | Full QA-B01–09 current inventory census, concurrent reservations, exhaustion, supersession and realized mix tests remain. |
| CON-031 | specified | Automated fixtures exist; no human review sessions claimed. | 28 four-band editorial sessions (or every actually advertised interim combination), keyboard-only and accessible reading plus nontechnical reviewer remain external. |
| CON-032 | in progress | Outcome/component/identity/role/version distinctions are serializable and available for structured local provenance. | Complete event counts, review signal denominator reporting and local quality dashboard remain; no new analytics transmission added. |
| CON-033 | in progress | C-A trust corrections and C-B bounded authority have executable changes; C-C identity scaffold and C-E admission foundation added. | C-D substantive family expansion and C-F actual editorial/signing/release evidence remain; dependency completion is not claimed. |
| CON-034 | in progress | Release and status artifacts separate repaired scoring from unimplemented content, review and signing. | Full intended edition is not accepted: family expansion, approved bands, feasible reviewed quotas, human review and integrated runtime/release gates remain. |
| SCO-001 | implemented | NFScoringOutcome correct/partial/incorrect/needsClarification/selfReported/skipped/invalidItem; optional objectiveCorrectness; accepted aliases earn1; partial never correct; self-report0/nil; legacy decode supported. | End-to-end new-record disposition/evidence verification owned by runtime/adaptive; skipped emitted by lifecycle, not invented by scorer. |
| SCO-002 | in progress | Versioned component results preserve submitted/parsed text, credit, outcome and codes; logic fields+invariant separated; deterministic side-effect-free scorer. | Multiple-choice/ordering/evidence still have aggregate component record rather than every diagnostic relationship; stronger parser AST provenance remains. |
| SCO-003 | in progress | Typed symbolic, exact-quantity, identifier, reviewed-prose and state Boolean/numeric/identifier/label authorities; case-sensitive unit symbols. | Every legacy short-text/numeric/tuple/sequence/chemical/code family needs reviewed typed adoption; no universal normalizer completeness claimed. |
| SCO-004 | implemented | All eight interaction enum cases preserved; optional typed authority and graph/set fields added compatibly. | Full UI/history journeys across all schemas are runtime/experience acceptance. |
| SCO-005 | in progress | Exact rational numeric parser retained; fractions/scientific notation/negative-zero work, known inline wrong units score semantic error, zero denominator/ambiguity clarify. | Explicit rounded precision, free equivalent-unit conversion, percentages/probability mode and approximation intervals need complete typed policy adoption. |
| SCO-006 | implemented | Scorer validates semantic numeric/tuple/display uniqueness before selected-ID scoring; unknown option IDs clarify; duplicate item is invalidItem with nil objective score. | General formula/prose semantic annotations and runtime replacement remain CON-006/003 work. |
| SCO-007 | implemented | Practice partial rule max(0,(correct selections−incorrect selections)/required); exact-set mode for rubric without partial/protected; explicit alternative accepted sets supported. | All authored practice weight appropriateness still needs editorial review. |
| SCO-008 | implemented | Optional DAG; all complete topological orders accepted; cycles/self-edges/unknown edges rejected; fraction of satisfied edges when partial enabled; empty-edge complete permutation1. | Family-specific proof inference truth needs independent content review. |
| SCO-009 | in progress | Explicit constrained prose/identifier/symbolic/quantity domains; length validated before score; uncovered reviewed prose produces needsClarification rather than false wrong. | Every legacy explanation needs migrated authority and explicit compare/report UI; full constrained-rubric coverage is not claimed. |
| SCO-010 | in progress | Exact polynomial normalization with declared variables/functions, rational coefficients, implicit/explicit/glyph multiplication, sums/grouping/powers; F/f/application roles distinct. | Variable-denominator rational functions, domain exclusions, alpha-equivalence, derivative primes and advanced domains deliberately unsupported and clarified. |
| SCO-011 | implemented | Local nonexecuting parser bounds 256 tokens, 32 depth, 16 symbols, exponents ±12, 2,048 terms; expanded monomial degree/work and arithmetic overflow bounded; unsupported syntax clarifies. | Limits are version1 engineering policy; configurable runtime policy/review adoption remains. |
| SCO-012 | in progress | Arithmetic-mean reviewed paraphrases accepted; explicit reversals/negated/add-then-deny claims rejected; unknown text clarifies. No added keyword-only acceptance. | Other legacy requiredTerms/constrainedConcepts rubrics need relation-safe reviewed migration; broad prose understanding not claimed. |
| SCO-013 | in progress | Bundled derivative, pure scalar and supported fixed-unit short-text computations gain typed authority; canonical reviewed aliases preserved. | All800 computation families need full tuple/sequence/binary/unit/formula modules and format-specific coverage. |
| SCO-014 | in progress | Every self-check score is selfReported with no objective correctness/credit; target-aware checklist adapter; optional fallback reflection. AI runtime uses attempt→reveal→rating; actual self-check comparison checkpoint export/restore passed in the golden runtime test. | Complete linked closed-reference retries and all lifecycle/device acceptance remain; all target-specific criteria remain editorial work. |
| SCO-015 | in progress | Accepted whole-state aliases give full 1; partial score uses best accepted state; explicit Boolean/integer domains and per-field results; malformed typed states clarify, invalid typed keys block; invariant80/20 retained. | Per-field authored arbitrary weights, enum domains, full schema validation and all composite policy adoption remain. |
| SCO-016 | in progress | Nearest-power-of-ten instruction explicit and protected deliverable matches control; locale and inline-unit handling distinguish syntax from semantic mistakes. | Numeric estimate/exact policy now compares the learner’s pair under an explicit 50% relative-error scale check; nonnumeric unit composite policies, free quantity conversion/rounding/estimate modes remain incomplete. |
| SCO-017 | in progress | Alternative sufficient evidence bundles supported; unsupported attachments penalized; stale IDs clarify; validator no longer reveals keyed relationship count or blocks scoreable omissions. | Every claim edge needs separate component explanation/weight and contradictions category; family scenario review remains. |
| SCO-018 | in progress | Ordinary feedback titles Correct/Check the answer; clarification is separately checkpointed editable guidance with no scored attempt; self-report distinct; protected feedback never reveals per-item keys even explicit legacy reveal argument. | Family-specific first feedback sentence and original-result linked replay validation remain. |
| SCO-019 | in progress | ContentImprovementRegressionTests cover meaning-preserving/meaning-changing derivatives,F/f,mean direction/negation,numeric spellings/units,aliases,sets,DAGs,intervals,duplicates,self-report and limits. | Full every-authoritative-answer adversarial corpus and signed-manifest census remain. |

## All 58 family promises and exact approval deficits

`Content_Family_Matrix_Draft.json` contains every QA-F01–QA-F58 row, stable ID, baseline mechanism, prerequisites, all four proposed demands, required family tests, corrective work and remaining asset/editorial requirements. All `reviewedBands` arrays are deliberately empty: no independent new-spec review was manufactured. The machine-readable matrix is a production checklist, not band availability.

`Content_Admission_Release_Gate.json` enumerates every exact proposed family quota and its approved-capacity deficit. **Specification defect:** CON-014’s nine retrieval rendering quotas sum to900, although the table claims1000 and its eight field quotas sum to1000. The missing100 rendering assignments have not been invented. Admission fails the inconsistent policy until the content owner records an explicit allocation decision. Approved capacity is zero under this new specification because no signed independent review records were supplied or produced. This is **not** a claim that there are zero app questions; the current numerical practice floor is verified at1,000 per lab in the actual-candidate census. The planned reviewed edition therefore has 7,000 approval/production slots still gated. An unsigned result, a bigger integer range, renamed setting or re-rendered target never fills a reviewed deficit.

The min-cost max-flow algorithm enforces unique semantic target capacity, compatible family/rendering, field totals, required assets/locales and per-family structure maxima. A capacity-one reward for the first item in each structure is bounded at each family’s required minimum; shortest augmenting paths can reassign shared targets. At maximum flow, maximizing the total bounded variety reward achieves every family minimum whenever a simultaneous feasible allocation exists. An independent exhaustive729-assignment oracle across64 candidate sets verifies the implemented quotas, field constraints, compatibility, caps and minima; this is finite verification, not an editorial content approval.

## Historical corrections and release handoff

`NFContentCorrectionPolicy.audit` and the compatibility `findings` API emit reproducible corrections without editing originals. The persistence adapter appends corrections, reconciles active policy membership and applies evidence exclusions. Saved history shows effective authority alongside original result/credit and rationale; invalidated keys are explicitly disputed and protected restrictions run first. All 18 historical policy/presentation tests passed in the latest run. This does not certify migration of every shipped store or reconstruct unavailable original content.

Before release, finish the remaining family production and compatibility assets, independently review the advertised scope, resolve the retrieval900/1000 quota contradiction, satisfy admission quotas or obtain an explicit smaller-scope product decision, execute integrated regressions and rendering/accessibility review, then follow `Documentation/ContentSigning.md` with the authorized offline release signer. Existing signed descriptors stay intact during development; this change must not be described as a newly approved content edition.


## Regression evidence map

| Acceptance probes | Executable coverage | Current evidence |
|---|---|---|
| QA-S01 | Numeric/tuple duplicate rejection,300 spatial seeds,200 structured-transfer table/function checks | Full current Xcode method passed |
| QA-S02 | Exhaustive1,000 interval parameter pairs;150 forced uncertainty-generator seeds; explicit18–26/27–35 disjoint oracle | Full current Xcode method passed |
| QA-S03–04 | Derivative equivalence, F/f/function application, reviewed mean paraphrase/reversal/negation/unknown prose, parser bounds | Full current Xcode methods and standalone production parser passed |
| QA-S05 | Every estimate alias, learner-pair plausibility, exact numeric/Boolean state fields, invariant partial credit, malformed typed state clarification | Full current Xcode method passed |
| QA-S06 | Independent/protected solution-scaffold absence and explicit representation roles | Full current Xcode method passed |
| QA-S07 | Fraction/decimal/scientific equality, malformed grouping/zero denominator, wrong unit versus invalid syntax | Full current Xcode method passed |
| QA-S08–09 | Set penalties/alternative support bundles, exact rubric, all valid DAG orders/cycles/empty edge set | Full current Xcode methods passed |
| QA-S10 | All self-check ratings have no objective correctness/credit and serialize faithfully | Full current Xcode method passed |
| QA-B08 | Shared targets, missing assets, exact quota policy, deterministic input order; independent64×729 allocation oracle with minima/caps/fields | Both current Xcode methods and standalone production admission oracles passed |
| Content census | Actual7,000 canonical contracts, distinct semantics under current fingerprint, zero schema-invalid contracts, actual family skew and all unreviewed band status | Full Xcode inventory suite passed; committed JSON census is unsigned evidence |
| DATA-010/MIG-006 | Actual portable export/restore immutability, all v14–18 legacy versions, nested protected evaluator/report canaries | All four Xcode tests passed at 00:09:50 on 5 September, including exact response-byte preservation, opaque private payloads and receipt-disposition exclusion; see Protected_Portable_Archive_Status.md |

The map records implemented probes and actual execution separately. It is not completion of the full QA family/renderer/editorial/privacy/device acceptance suite.

The latest completed root-owned Mac run (`/tmp/neuroforge-coordinated-recovery-unit-tests.log`) executed 784 tests with 16 failed assertions in four methods at 01:40:36 on 5 September. Passing suites include 19 content regressions, 21 historical policy/presentation tests, four portable-protection tests, six golden tests (including actual 10,000-attempt restore), six granular-history tests and all 28 session-integrity tests. Ten failures came from one generated Next test that deleted the accepted journal rather than restoring it before retry; that test now preserves and restores the exact directory. Five failures came from two focused-family tests expecting the previous unavailable-state behavior; one presentation-pin fixture threw invalidSequence. Rerun remains required; no current-source all-project pass is claimed. The installed UI run was identified as loading stale tests and is not acceptance evidence for current source.

## Resumed DATA-023/024 correction engineering

The bounded historical policy now covers every DATA-024 row with executable decisions or explicit unresolved boundaries; see [Historical_Content_Correction_Status.md](Historical_Content_Correction_Status.md). The 21 policy and effective-history presentation tests passed in the completed 784-test run, including scoped frozen estimate/exact alias cases. Append-only persistence and active correction projection are integrated; dismissal does not remove the detail amendment. See the historical status record for explicit unresolvable cases. The full current-source rerun remains pending.

## CORE-005 follow-up — compatibility tests passed; full rerun required

The schema review found that `schemaVersion > 0` admitted future generated exercise contracts and a generated draft could replay without a pinned scorer. The shared validator/scorer now support the known authored schema 1 and fallback schema 2 explicitly. New generated drafts independently pin scorer, presentation and selection versions. Missing legacy pins remain decodable but cannot establish writable evaluator compatibility. Future or unrecognized local draft bytes are retained, surfaced as unavailable, and cannot be mistaken for a new empty session. The recovery panel shows only independently decoded learner text, without consulting unsupported option labels, keys or feedback. Saved-set envelope/import checks are integrated by the repository owner; universal restore checks the nested schema as well.

Ten new `AIAndSourceTests` cover nested future schema refusal, explicit/missing/mismatched version pins, immutable future prepared-commit recovery, opaque future-run preservation and supported pinned replay. All ten compiled and passed within `AIAndSourceTests` (62 tests, zero failures) at 00:40:43 on 5 September in `/tmp/neuroforge-reviewed-recovery-unit-tests.log`. The same run passed all 14 content regressions at 00:40:53. That Mac run completed with 742 tests and the two failures described above. Installed iOS execution separately found actual finite-pool advancement defects; all four corresponding runtime regression probes now pass in the 28-test session-integrity suite. The latest AI suite ran 65 tests; its only failing method was the transient-write fixture described above. Unknown-version private metadata remains protected against overwrite and has a separate explanatory unavailable presentation; no future state is reconstructed from defaults.

The first compatibility build exposed an overly narrow schema-1-only gate; the actual producers emit authored schema 1 and fallback schema 2. The predicate now explicitly supports both, with a regression using each real producer and refusing future schemas. The interrupted build log is `/tmp/neuroforge-integrated-closure-unit-tests.log`; it is not a passing run or a completed full-count result. Existing provenance validator version 2 was verified against both producer assignments and the prior committed validator contract and remains unchanged.

Additional generated-runtime guards now validate response family/IDs without rejecting blank or conceptually wrong editable drafts, require a frozen response/result/attempt identity for prepared or feedback phases, and use duplicate-safe claim restoration. Lost writers cannot retry, advance or publish a checkpoint. A matching already-committed receipt can acknowledge its exact saved feedback after later quarantine without new scoring; uncommitted quarantined work and conflicting receipts remain blocked. The five added tests cover malformed/blank drafts, prepared/feedback binding, writer takeover, later-quarantine receipt recovery and conflicting receipt preservation. All five passed as part of the ten new generated compatibility/recovery tests in the completed 62-test suite above.

## Shared lifecycle correction — runtime regressions executed

Ordinary and generated practice now use `NFSessionLifecycleCoordinator` for typed phases, frozen response/result/attempt intent, prepared-save → attempt-save → feedback acknowledgement, reference exposure durability, and next-snapshot save before publication. Generated clarification is an optional, exact-response-bound private draft field rendered inline, not a failed-save alert or attempt. See [Shared_Lifecycle_Status.md](Shared_Lifecycle_Status.md) for the storage-adapter boundary, precise ordinary receipt retry semantics, all thirteen new tests, and remaining protected-assessment/skip distinctions. Four finite focused-family probes assert retained first-item feedback and explicit shortage rather than fabricating an editable second question. All ten new ordinary/shared methods, including three skip/reveal durability cases, passed in SessionIntegrityReviewTests. Generated reference and clarification methods passed; the generated Next obstruction fixture was corrected after the failure described above and requires rerun. No full-project acceptance is implied.

## Granular local history recovery — six tests passed

A malformed independent history snapshot now receives an unavailable marker while other valid snapshots remain readable. The bounded original is retained in private local recovery storage before any rewrite; automatic exports contain only safe unavailable references. Protected raw flags and markers retain history/export restrictions even with inconsistent attempt metadata. All six actual-file `GranularHistorySnapshotRecoveryTests` passed at 01:39:36 on 5 September; see [Granular_History_Recovery_Status.md](Granular_History_Recovery_Status.md) for exact bounds, privacy cleanup and explicit remaining portable/media limitations. The suite includes actual marker-only protected imports and reexports; broader portable/media acceptance remains incomplete.

## Latest verification update

The subsequent root-owned `/tmp/neuroforge-final-recovery-unit-tests.log` run completed at 01:49:57 on 5 September 2026 with **784 tests, zero failures**. It includes the corrected generated Next write-obstruction fixture and all content, historical, golden, shared lifecycle and granular protection tests described here. This supersedes the earlier 784/16 result above. The later scratchpad byte/geometry recovery changes are authored after that green run and await their own integrated execution; see [Scratchpad_Recovery_Status.md](Scratchpad_Recovery_Status.md). The successful Mac run is not certification of all editorial, physical-device, installed UI or performance release gates.

## Historical context follow-up, awaiting execution

UX-025/INT-006 now have original logic-state reconstruction, retained used-hint text and a local citation route that verifies saved identity/digest and explicitly handles changed or missing sources. It never supplies generic model answers as original excerpts or invents missing source version numbers. Five tests are authored after the784/0 baseline; see [Historical_Context_And_Citations_Status.md](Historical_Context_And_Citations_Status.md).

## Review-deferral follow-up (unverified batch)

The parent reported the preceding complete Mac baseline as 852/0. The newly implemented effective review queue/deferral integration and twelve added tests are documented in [Review_Queue_Status.md](Review_Queue_Status.md); parsing has passed, execution is pending. New retention selection explicitly understands shipped v3/v4 template mechanics and refuses exhausted matching pools or unknown families. The old matching test now requires a real oracle token. These changes do not assign reviewed bands or certify a reviewed retention interval ladder, and do not reinterpret existing exact saved exercises.

## 5 September: retrieval typed authority and method attribution

New ordinary retrieval pins optional recipe1 with generator9/schema6. Decimal quantity recall accepts exact decimal/fraction/scientific equivalents; binary numeral recall preserves digit meaning and insignificant leading zeros. A bounded eigenvector equation authority distinguishes matrix A, vector v and scalar lambda, preserves multiplication order and case, and permits valid scalar/equality reorderings. Unsupported grammar requests clarification. Three exactly authenticated older bundled target classes use the named compatible authority; original exercise bytes and committed feedback remain unchanged. Separate historical amendments correct only verifiable original evidence or withdraw disputed grading. The current scorer8 audit now reaches those targets and recognized EN/JA generic workflows.

Explicit retrieval-cycle and source-filter work remains method practice with separate identity and no unrelated subject target/citations. Ordinary mixed subject recall does not select a generic workflow in place of subject knowledge. Method correctness is retained while proficiency/independence/band/retention projections exclude it. All nine new actual-generator/store/cold tests and all48 other selected tests pass in the57/57 run on `5a86814939d1667411bae7866f9a30404f8bdd243b3b93ce610a791e2a9fe0b6`. Earlier fixture failure and its ModelContainer lifetime correction are retained. This is bounded typed authority, not a general matrix/prose solver or reviewed new content edition. Actual cloze/equation/figure asset expansion, all58family demand/oracle coverage and human review remain unfinished.
