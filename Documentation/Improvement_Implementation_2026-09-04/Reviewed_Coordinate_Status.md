# Reviewed coordinate protocol supplement

Depends on the frozen coordinate12/schema9 package at `/tmp/nf-coordinate-transforms-20260905/coordinate-transforms.patch` (SHA256 `10ab9158915013910764730969cb8d30b985ed151f09d7736fb2c64d40a10391`). This patch does not repeat that package or add source files/project membership.

## Production change

`Sources/Adaptive/NFEditorialPracticePolicy.swift`: the existing native ordinary commit protocol accepts a `.logicState` interaction only when `NFCoordinateTransformContract.make(exercise:)` validates the entire exact coordinate recipe. The prior practice/non-protected gate stays first. Other structured response workflows are not admitted. The external exact admission registry/digest/scorer/profile/receipt/capture guards remain unchanged, and the released registry stays empty.

No runtime, timing, selector, checkpoint schema or manifest changes. This permits the already generic immutable observation capture and adapter to work for a genuinely admitted coordinate contract. It grants no review to shipped coordinate assets.

## Regression fixtures

Six methods appended to existing `Tests/ContentImprovementRegressionTests.swift`, class `ReviewedCoordinateProtocolTests`:

1. `testOnlyCompleteOrdinaryCoordinateContractExtendsStructuredResponseProtocol`
2. `testActualCoordinateCommitCapturesFamilyBandAndNextUsesEligibleTypedObservation`
3. `testHalfCreditIsNotFullSuccessAndHelpIsExcludedWithoutChangingOriginalPoint`
4. `testHarderPreferenceAndColdFileResumeRetainActualBandWithoutInventingDemonstration`
5. `testInvalidTypedInputAndCurrentExclusionCannotContributeEligibleCoordinateEvidence`
6. `testReleasedEmptyRegistryKeepsCoordinatePracticeAvailableWithoutReviewedAuthority`

Fixtures materialize actual bank coordinate contracts and bind exact digests in a synthetic fixture admission registry. Both bands use the same actual catalog family. All caches, documents, metadata/history, rotation, local repository and temporary paths are isolated; the ModelContainer remains alive. The cold fixture reopens a real local-session file. Quarantine goes through the actual AppStore item-report and effective-observation paths.

Half credit remains valid partial evidence according to the established policy. These tests do not incorrectly exclude all partial responses: four half-credit responses alone cannot promote or demonstrate a band, and they are not full correctness/clean speed. Assisted responses are excluded from independent evidence. Invalid numeric input cannot produce an attempt; current quarantine excludes effective evidence while preserving the original record bytes and credit.

## Verification and limits

`swiftc -frontend -parse` passed for both changed scratch files. `git apply --check` passed against the dependent coordinate12 base. These checks are syntax/application checks, not typecheck, app builds, executed XCTest or editorial review. Root must integrate coordinate12 first and execute all six methods on the actual app test host. No working-tree edits or xcodebuild performed here.

Fluency routing belongs to the separate Adaptive patch. Its native protocol branch was reserved as non-overlapping. Unknown legacy history is not given missing coordinate metadata or a reviewed band. Other structured response native protocols remain explicit engineering follow-ups.


## Root executed evidence

All seven reviewed coordinate methods, including real detached preview/materialization, pass on dd374256. The full1,237-test run also passed all coordinate methods, with its two failures restricted to retrieval worker construction. Actual installed rotation/reflection journeys pass on34e8999e. The full request/manifest/capture gates and released-empty registry remain intact. This supersedes the earlier pending-execution notes for this scoped implementation.
