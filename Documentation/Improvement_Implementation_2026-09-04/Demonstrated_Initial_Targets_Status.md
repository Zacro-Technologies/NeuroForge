# Demonstrated initial targets — ADP-006

Scratch only. No repository files or project configuration were changed, and no build or tests were executed by this agent. Parser and `git apply --check` pass. This package preserves the current root canonical-day readiness repair, immutable-prompt fixture correction, coordinate12 guard, and background validator cancellation/isolation. It does not reapply the previous fluency patch.

## Behavior

New single-family/catalog launches pin EditorialLiveControllerV2; new mixed launches pin EditorialMixedControllerV2. Explicit user band still wins. Automatic selection uses the highest compatible demonstrated practice band that is not currently unsupported or affected by excluded evidence, then the latest independent full-credit practiced band when demonstrated coverage is insufficient. Cold starts remain B1. At 30 canonical local days since relevant independent practice, the reason is refreshProbe at the prior band. Protected evidence never enters this adapter; no protected observation producer or external approval is fabricated.

Compatibility is derived from exact admitted exercise materialization under the request: objective, family, band contract, scorer comparability, actual stimulus/response format, tool condition, locale comparability and practice protocol. Pacing remains separately recorded: it is not pooled across reducer groups and this projection never creates timing eligibility. Used inventory still contributes current compatible contract identities. Candidate availability is applied separately, so exhausting a demonstrated band cannot silently lower the target. First delivery must match the selected demonstrated/recent group. Current-profile captures alone enter new V2 decisions.

The optional `initialTargetDecision` records basis, exact group, supporting IDs, last relevant day, historical demonstration time and compatibility digest. Every later receipt in that family retains it. First-receipt validation verifies evidence/ID membership, retained exact exercise compatibility and historical demonstration time. Accept recomputes evidence and the whole decision under the existing archive CAS and atomic publication; changed source records cannot publish an outdated starting decision.

V1 live/mixed pins remain supported and retain their original recent-success initialization and mixed tie hash. The family projection preserves the pinned version. Unknown controller versions remain unavailable for continuation. Optional metadata is absent from V1 receipts; no old receipt or attempt is rewritten.

## Files

- `Sources/Adaptive/NFEditorialPracticePolicy.swift`: compatibility/basis projection, V2 pin dispatch, persisted initial basis, prelaunch shared projection, legacy mixed hash support.
- `Sources/Persistence/NFLocalSessionRepository.swift`: exact inventory compatibility, V2 profile filtering, receipt graph/version validation. No archive schema, checkpoint schema, SwiftData model or command-owner changes.
- `Tests/EditorialBandEvidenceTests.swift`: five pure initial-target fixtures.
- `Tests/LocalLearningLifecycleTests.swift`: five actual local publication/store fixtures and one existing direct mixed-decision fixture supplied explicit empty-history context.

## Verification requested from root

Run EditorialBandEvidenceTests, EditorialLiveControllerPersistenceTests and DefaultContentCatalogTests. Ten added fixtures cover demonstrated precedence over a lower success, explicit choice, insufficient coverage, contract/locale/protected exclusion, pacing separation, correction exclusion, 30-day/historical refresh, actual prepared capture/store persistence, Next/cold replay, stale evidence acceptance, finite shortage and V1/future compatibility. The explicit-clock history helper stages an authentic native capture before the secondary AppStore commit; its timestamps are preserved through the supported frozen-capture retry boundary. It does not change original written dates or backfill legacy rows.

## Remaining obligations

- The released admission registry is still empty; real signed content admission and protected protocol authority remain external gates.
- Prelaunch menu still needs to retain an unavailable selected identity and show its specific reason; that is the next bounded package.
- The richer non-answer-revealing task example/duration preview and truthful legacy slider explanation are still pending.
- Controller V2 selection still materializes bounded manifest inventory synchronously in this slice. The separate async archive/controller worker architecture is owned by Experience; no background-completeness claim is made here.
