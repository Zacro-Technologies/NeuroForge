# QA-F13 bounded solid/plane intersections — integration candidate

Frozen candidate: `solid-sections.patch`; SHA256 `10c80d5dbdb0032bca9e613353347b0f1748232bb9ec436bdc87d3736d79a5e7`.

This is a scratch-only patch against the current repository after the root's `makeDraft`/`assembleCandidate` generator split, `materializeAlternativeExercise` factory split, coordinate rubric/recovery fixes, and ADP-006 integration. No working-tree source/test files were changed by this package. No new source file or project membership is needed. Fifteen existing Swift files and one catalog file change; `base-hashes.json` records the exact input files.

## Result and authority

New ordinary spatial launches opt into `solidSectionPolicyVersion = 1`. Only the existing cross-section family takes generator 13 / schema 10 / new `.r13` item identity. A nil policy retains the old recipe, and already accepted snapshots remain exact. New requests cannot accept a missing, future, or mismatched section contract. Complete retained-contract validation owns the solid, plane, question, exact key, rubric, representations, feedback, fingerprint, locale, and provenance. Both public generator validators remain active. Unknown cylinder orientations are refused, not classified from a drawing.

The current family receives:

- Closed cube sections from exact edge/plane intersections, including triangle, rectangle, square, rhombus, pentagon and hexagon, plus empty/point/segment partitions.
- Closed sphere sections from exact center-to-plane distance; numeric questions ask squared radius with a rational key and exact fractional feedback, in square grid units.
- Finite closed cylinders with axial, translated and declared oblique planes, including full ellipse, cap-clipped ellipse, non-square rectangle, contact and empty cases. End caps are part of the authority.
- Explicit proposed-section checks requiring both the named nondegenerate shape and passage through a stated point. The learner may reject the proposed plane when either condition fails.

No schema column, saved-response shape, scorer version, old raw attempt, signature, admission manifest or reviewed band is rewritten. Choice and numeric scoring use existing typed protocols, with the exact section contract supplying their keys. The native reviewed catalog scope map accepts only a fully validated section contract for the exact family; it does not grant review authority.

## Representation and lifecycle

The same exact solid/plane paths feed native RealityKit and static Canvas. The source equations, units and closed-solid conventions remain visible with EN/JA text. Static front/side/top/authored projection is checked against the native orientation. Camera controls never move the object or modify the answer. The colored plane patch is explicitly described as a finite drawing of an infinite plane, not the true section boundary.

The computed green intersection is available only after committed feedback or in the saved-worked-solution phase. Independent input has no worked-section control. Reset changes inspection state only. Control labels contain actual minimum 44×58 point frames; native installed AX/hit-target verification remains pending. Symbolic equations are described as an accessible equivalent for this ordinary practice, not a validated substitute for a visual assessment.

Ordinary generated factory propagation, launch-only copies, profile launch defaults, repository save/read validation, exact runtime checkpoints, generated retained-authority rendering and shared history rendering use the same contract. Generated source/assessment creation is not broadened: the new opt-in generator guard is ordinary, ungrounded spatial practice only.

## Executed verification

`python3 verify.py` executed the actual pure Swift geometry with its exact rational type, then an independent Python Fraction/Cramer oracle and finite-cylinder support/cap oracle. All 370 bounded cases passed, including each rendered boundary sample against its original plane and solid. This is pure geometry execution, not an app build or XCTest result.

Syntax parsing passed for all 15 candidate Swift files. Catalog JSON decoding and `git apply --check` passed against the current tree. App compilation, the nine new XCTest methods, platform renderer execution and installed journeys have **not** been run by this agent. Root owns those runs.

The nine methods are in existing `Tests/ContentImprovementRegressionTests.swift`, class `SolidSectionContractTests`:

1. `testEveryFiniteGeometryCaseMatchesIndependentOracleAndItsRenderedBoundary`
2. `testAllDeliveredContractsHaveExactKeysExclusiveChoicesAndBothLocalizedRepresentations`
3. `testSymmetryAndRepeatedShapeClassesDoNotInflateCapacity`
4. `testFutureMalformedProtectedAndMissingRecipeCannotUseOrdinarySectionAuthority`
5. `testSectionReplayIsPostResponseOnlyAndResetPreservesOriginalGeometry`
6. `testActualOrdinaryColdResponseCommitAndNextKeepSameFamilyExactHistoryAndRecipe`
7. `testGeneratedRadiusKeepsExactRationalDraftAndUsesSameAuthorityAfterColdRestore`
8. `testDetachedProducerValidatesEveryNewSectionRepresentationWithoutChangingLegacyRecipe`
9. `testActualFactoryReachabilitySeparatesFocusedQuestionsFromLegacyMixedBankDescriptors`

The last test measures 50 focused no-repeat questions and all 1,000 legacy mixed-bank ordinals through the actual detached factory. Its `SOLID_SECTION_REACHABILITY` output must be recorded after execution. The legacy bank has only three section descriptors, so it cannot be represented as a 132-entry section bank. All store fixtures use retained in-memory ModelContainers, unique local roots and isolated caches/rotation/history/documents/temp dependencies, with shared widget publishing disabled.

## Coverage and outstanding obligations

QA-F13's concrete axis-aligned, translated/noncentral-radius, oblique-geometric and reject-under-multiple-constraints demands are represented. Tangency/degeneracy, mutually exclusive circle/ellipse categories, independent oracles and symmetry deduplication are encoded. This is an authored development domain; the B1–B4 instructional review and admission are still absent. A general inverse plane-construction task and broader solids/orientations are not claimed.

The domain contains 370 parameter cases and 132 conservative query classes: 92 cube, 28 sphere, 12 cylinder. Shape-only sphere changes and repeated parallel classifications collapse; radius questions retain substantive exact radius changes. This counts query/parameter classes, not 132 solid topologies. `Capacity.json` gives the reproducible census. The proposed section quota is 167; even before actual bank reachability and admission there is a minimum deficit of 35. Reviewed admissible capacity remains zero. More authored structures, balanced-bank descriptors, complete rubric/demand review and admission are actual remaining work; they are not silently labeled external approval-only tasks.

The other 57 family obligations and QA-F15 cube-net expansion are not closed by this package. Global 1,000-item balanced admission, export certification and human accessibility/visual review remain separate requirements.


## Root execution

All nine methods pass in the full1,257-test run on abb5348c; overall1,251pass/six unrelated fixture failures. Actual census:50/50focused fresh questions, zero unavailable, three distinct legacy mixed-bank classes,132finite authored classes. Both iOS and Mac compile. No reviewed admission is implied. Installed section geometry remains unverified.
