# Outstanding validation run sheets

Status: **not executed**. These forms make the remaining acceptance work concrete; blank results are never passes. Use synthetic sources and separate test accounts. Record build/commit, content/scorer/policy/archive versions, device/OS, locale, text size, appearance, cloud state, accessibility settings, and enabled capabilities with every run. Preserve exact steps, observed result, screenshot/trace path, severity and retry result. Do not include private source text or account secrets.

## G1/G4 editorial admission

For each of the58families, independently solve each supported band's representative before revealing the proposed key. Record the objective, actual reasoning/representation/scaffolding changes, duration range, parameter boundaries, answer alternatives, dependency/support graph, distractor derivation, essential accessible givens, hint stages, decisive explanation and fresh repair. Sign disagreements/resolution explicitly. Review every protected form for visible/spoken/export leakage. Regenerate the actual admitted edition and check all family/structure/field/band caps, feasibility, authentic assets and fingerprints. Resolve Retrieval's900/1,000quota discrepancy before signing a new edition.

## G2 store and recovery

Use every actual distributed prerelease store, not a current model array mislabeled old. Preserve a recoverable copy; upgrade, inspect old typed answers and dispositions, resume every recoverable phase, inspect unavailable legacy drafts, export, restore into empty and nonempty stores with explicit policy, repeat restore, and compare identities/hashes/counts. Interrupt before/after intent, attempt, acknowledgement, asset materialization and cleanup. Verify compatible recovery binary/package. Test two real windows, takeover, stale keyboard commands, account changes and inactive namespace deletion. Record any preserved predecessor or unfinished tombstone.

## G3 device/accessibility

On supported stable macOS and the audited beta, repeat History→answer→Sources and Document→Settings with large synthetic history; capture an Instruments trace if a stall returns. Run phone, iPad split-width and Mac at default/maximum type in EN/JA and light/dark. Capture all8response schemas, invalid input, hint/solution, feedback, pause, reference comparison, reflection, early/natural ending, source import and saved-set states. Perform keyboard-only, VoiceOver without sight, applicable voice/switch interaction, Pencil and unsupported drawing roundtrip. Record actual spoken equations/coordinates/relationships. Measure44pttouch targets and rendered4.5:1normal/3:1large-text contrast; do not infer those from color names.

## G5 signed integrations

| Capability | Required cases | Result |
|---|---|---|
| Question Writer/provider | signed install, success, unavailable provider/Shortcut, cancel/offline, oversized/malformed output, callback after relaunch, duplicate callback, exact consent scope | Not verified |
| CloudKit/progress | two signed devices, offline divergent writes/convergence, account change/sign-out, schema, deduplication/deletion, absence of new local blobs in cloud | Not verified |
| Notifications | denied/allowed/provisional, timezone, overdue, disabled reminders, valid/deleted deep link, no repeated stale alert | Not verified |
| Widgets/App Group | empty/completed/paused/stale, refresh, extension lifecycle and deep link | Not verified |
| Spotlight | index/update/delete and deleted reference routing with private source policy | Not verified |
| Import/OCR | text/PDF/scanned PDF, access denial, malformed/size boundary, cancellation, duplicate and deletion during indexing | Not verified |
| Export/delete | upgraded/fresh, inclusion preview, rollback, repeated restore, full purge and failed cleanup retry | Not verified |

## Performance

Release configuration, no debugger, named physical reference hardware and stable OS. Record warm-up/cache conditions,30unfiltered measured runs per latency operation, p50/p95/worst, and reasons for any exclusions. Targets: warm switch200ms; local resume/next/acknowledgement/history firstpage/source search500ms; cold usable Today2s. Use10,000synthetic attempts and1,000indexed synthetic sources. Record five repeated20-item runs, memory/thermal/allocation trend and stalls above100ms. These budgets have not been measured by the present Debug tests.

## Formative usability and outcome study

Run two iterative rounds of six consented participants with varied STEM experience and meaningful accessibility inclusion. Observe unaided: begin practice; use feedback/help; change challenge separately from timing; leave/continue; locate original answer; repair a mistake; import a synthetic source/save a set; explain progress/self-check. Record task numerator/denominator, wrong turns, assistance, disputed grading and participants' exact comments. Ask what was saved, what caused the level, what Matched means and what happens on return. Do not generalize a small formative sample to efficacy.

Before a separate learning efficacy study, pre-specify the claim, representative recruitment, consent/data handling, comparison/assignment, outcomes, uncertainty/sample-size rationale, delayed retention and held-out application. Keep evaluation targets/structures separate from training exposure. Report attrition, prior knowledge and accommodation effects. No learning-benefit result is supplied by this implementation.
