# Spatial assembly 16 / schema 13

Scratch-only package for QA-F12 B4 asymmetric, occluded proper orientation and QA-F14 B4 feasible reconstruction / underdetermination. No main source, project, store, build, or installed application was modified by this package preparation.

## Behavior and authority

- Explicit secondary Practice launch for `nf.default.spatial.object-rotation` and `nf.default.spatial.top-view`: **Asymmetric solids and reconstruction (untimed)**, `train-start-spatial-assembly`. Existing primary routes and default generator11 structures remain intact.
- `spatialAssemblyPolicyVersion: 1` pins generator16 / schema13, with new-only `.r16.` identities. Nil retains previous recipe bytes. Both request→contract and saved contract→request gates apply on construction, restore, and local validation. Unknown/missing sidecars do not acquire a guessed key.
- Complete known source solid: five face-connected cubes, four chiral mirror-pair structure classes with no proper rotational symmetry. Camera/static controls inspect only the fully specified original. Candidate cards supply fixed opaque front and side silhouettes. Correctness comes from a match with at least one of the 24 proper orientations **using those public two-view signatures**, not hidden candidate coordinates. Mirrored candidates with indistinguishable supplied signatures are rejected as authored candidates rather than marked false.
- Reconstruction: the public complete finite model is four vertical columns on a 2×2 footprint, heights0…3, supported from below, no gaps/overhangs, nonempty face-connected assembly. Givens pin the footprint, both projection maxima, and exact total cube count. All feasible arrangements are among the eight candidate solids/height tables. The learner selects the entire feasible set, or the explicit none option when inconsistent. No first-valid-only key.
- Ordinary, generated, retained history and cold drafts use the existing multiple-choice response and immutable snapshot envelopes. Exact typed binder validates source, purpose, versions, question/feedback/options/rubric, spatial givens, family and fingerprint. Full-set scoring is intentional and stated in the instructions.
- Computed candidate cells/worked explanation are exposed only after acknowledged feedback or saved worked solution. Separate original drawings remain unchanged. Reset clears the worked inspection. No precommit target transformation helper exists.
- Named native/static model containers use public-only accessibility descriptions. Targets have inspectable `spatial-assembly-candidate-i-front-model` / `side-model` IDs; original source uses `spatial-assembly-source-model`; worked models require allowed phase. All control labels have explicit bordered style and internal58pt minimum. Real device target sizes and visual layout still require installed execution.
- Full binder added to the narrow native multiple-choice protocol only. Released admission registry remains empty; no signed review or B4 evidence authority is fabricated. Current reviewed criterion routing is unchanged.

## Capacity

145 delivery forms, **82 semantic targets**:

| Family | Forms | Targets | Scope |
|---|---:|---:|---|
| Asymmetric proper orientation | 67 | 4 | Four connected chiral five-cube structure mirror pairs; pose/option order is not novelty |
| Feasible reconstruction | 78 | 78 | Canonicalized under all eight horizontal-grid symmetries |

Reconstruction target multiplicity:32 impossible,38 unique,3 with two valid arrangements,1 with three,3 with four,1 with six. The physical model contains237 valid connected arrangements among256 height assignments. These are bounded task/model counts, not reviewed bands or a claim about arbitrary polycubes.

## Checks and tests

Executed locally: `python3 verify.py` compiles actual new geometry with unmodified retained Cell/Square declarations under Swift6 complete strict concurrency, then executes an independent quarter-turn group closure and exhaustive nested-loop column/support oracle over all145 forms. It also verifies all237 models under all8 constraint symmetries. See `Geometry_Oracle.txt`.

`prepare.py` rebases only owned narrow changes against current main, preserves root fingerprint non-inlined branches / timing / async / source APIs, merges two EN/JA catalog entries, and generates `spatial-assembly.patch`. Syntax parse and current-tree apply check are recorded separately. No complete app-module typecheck, xcodebuild, XCTest run, or native geometry capture is claimed here.

Nine new methods in existing `ContentImprovementRegressionTests.swift`, class `SpatialAssemblyContractTests`:

1. `testEveryAsymmetricOrientationUsesIndependentProperRotationAndVisibleSignatureOracle`
2. `testEveryFeasibleReconstructionIncludingUnderdeterminedAndImpossibleSetsUsesIndependentEnumeration`
3. `testVisibleAmbiguityAndIncompleteAlternativesCannotAcquireAFalseKey`
4. `testOriginalOnlyInspectionResetAndProtectedOrFutureContractsNeverExposeComputedReconstructions`
5. `testNilPolicyPreservesLegacyAndStructure11WhileNewRecipeCannotResumeUnderMissingPin`
6. `testActualSecondaryLaunchColdCommitHistoryAndNextKeepOriginalGivensAndFamily`
7. `testGeneratedCompleteCandidateSetsStayExactThroughColdAsyncCommitAndHistory`
8. `testFrozenAssemblyCannotResumeUnderMissingPolicyOrWrongActivityAndPreservesOriginalBytes`
9. `testDetachedFiniteFactoryCollapsesCosmeticRotationsAndStopsAtActualTargets`

One new existing native-input worker method: `TypedGeometryArchiveWorkerTests.testSpatialAssemblyDraftsColdStartupAndAutosaveUseRealBackgroundWorkers`. It exercises both assembly families through the real repository startup/autosave actor; the mixed typed archive fixture also includes both kinds. No mock persistence and all stores/caches/history/temp paths remain disposable.

Independent source review by Experience covered only public/hidden display: no concrete candidate-coordinate disclosure found. Geometry/native/device execution was not part of that review.

## Remaining scope

This closes a bounded authored engineering slice, not all spatial content or external review. Arbitrary irregular solids, unsupported overhangs/free polycube reconstructions, unbounded viewpoints, open construction response instead of finite candidate sets, and wider item inventory remain absent. Per-band admission, signed content review, cross-format evidence comparability and all58-family coverage require their own actual evidence. Existing fixed legacy mixed-bank descriptors are intentionally not silently remapped to recipe16. Native launch/answer/Next/cold/geometry screenshot journeys remain pending root execution.

## Actual iOS worker execution

Source `11a664de` passed all 21 selected actual iOS tests (15.152 seconds): 13 generated unscored transaction tests and eight typed geometry archive-worker cases. Both new assembly families and their mixed archive entries pass cold startup and autosave. Full Mac and installed UI verification are separate.
