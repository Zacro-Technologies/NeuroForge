# Coordinate15 explicit installed branch coverage

Two existing files only: `Sources/App/NeuroForgeApp.swift` (DEBUG fixture branch) and `UITests/NavigationSmokeUITests.swift`. No main files, project settings, production catalog, released admission, or stored default recipes were changed during preparation. Existing public-secondary and native16 journeys are retained.

## Exact fixture boundary

The existing isolated `-ui-test-activity` harness accepts an additional explicit `-ui-test-coordinate-reasoning-query` of `inferAffine` or `orientationFixed`. Each value requires its exact existing ordinary catalog activity, a new disposable UUID store and reset flag. The fixture cannot run on a real learner store or silently override other activity fixtures.

A read-only proposal obtains the actual fixed launch plan, then a deterministic bounded search of seeds0…255 uses the production exercise factory to inspect **only the typed task kind**. It creates no attempts, reserves no slot, changes no ledger revision, and supplies no response. The real `beginSession` → `acceptFixedLaunch` boundary then publishes the selected seed and policy15. The harness asserts exact equality between the previewed exercise and the accepted immutable first checkpoint and requires its real fixed launch receipt. All ordinary write, scoring, history and cold restore paths stay active. Scoped rotation defaults are passed explicitly; no private AppStore property is exposed.

These are explicitly **fixture** journeys. They do not replace or claim the public secondary-launch coverage established by the separate two public coordinate tests, whose observed first questions were inverse problems.

## Native methods

- `testExplicitAffineInferenceFixtureSavesSixTypedCoefficientsThroughColdHistory`
- `testExplicitOrientationFixedLocusFixtureSavesBothScoredChoicesThroughColdHistory`

Both invoke the existing independent public-prompt solver. The affine test requires exactly six coefficients and a nondegenerate public correspondence matrix, enters mathematically equivalent decimal text ending in `.0`, and verifies the exact original strings through cold restart, actual Correct feedback, locked editors and readable history. The noncanonical text distinguishes the learner’s saved response from an expected-key rendering.

The orientation/fixed-locus test requires exactly two independently solved statements: determinant/handedness and global fixed-locus consistency. It verifies the selected set through cold restart, actual Correct feedback, locked original choices, Next, and one retained history record. Both preserve exact original givens, restrict worked content to acknowledged feedback, reset it, and read the saved first answer after a second cold launch. Full AX/oracle attachments distinguish original and worked content. Long text screenshots do not claim every line was simultaneously visible.

## Verification and limits

- Full current UITest file plus these methods directly typechecks under Swift6 complete strict concurrency against the real arm64 iOS simulator XCTest overlay/frameworks; no diagnostics.
- The new fixture helper separately typechecks as an extension against the **actual built NeuroForge app module**, with no model/API stubs, under Swift6 complete strict concurrency for both macOS26.4 and arm64 iOS26.4 simulator. This verifies the real production API signatures; it does not execute the store or launch.
- `NeuroForgeApp.swift` syntax parse and current-tree patch application pass.
- No xcodebuild, app launch, store execution or installed result is claimed. Root owns actual compilation/execution and final screenshots.

Patch SHA256: 1dfce00f7b8ba1477f7813a943c5d7c06b3e3d19dfb74e94a10cd3a5f8ecfc3f
